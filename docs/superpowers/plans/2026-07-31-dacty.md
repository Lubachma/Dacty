# Dacty — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construire Dacty, un jeu de speedrun dactylo local (SPA web) avec mode libre, mode Challenger (ranked par langue), classements et achievements.

**Architecture:** SPA React + TypeScript sans backend. Moteur de frappe pur (`engine/`) sans dépendance React, corpus JSON embarqué (`texts/`), scoring pur (`scoring/`), persistance IndexedDB via Dexie (`db/`), règles de succès déclaratives (`achievements/`), orchestration de fin de run (`game/`), UI React (`ui/`). Spec complète : `docs/superpowers/specs/2026-07-31-dacty-design.md`.

**Tech Stack:** Vite 7, React 19, TypeScript strict, Tailwind CSS v4, Framer Motion, Zustand, Dexie, zod, Vitest + Testing Library, fake-indexeddb.

## Global Constraints

- TypeScript `strict: true`, aucun `any` non justifié.
- Interface 100 % en français. Identifiants de code en anglais. Messages de commit : conventional commits (`feat:`, `test:`, `chore:`…), description en anglais.
- Thème sombre par défaut, clair optionnel, via `html[data-theme]` + variables CSS (pas de lib externe de thème).
- Pas de police externe : `ui-sans-serif, system-ui` pour l'UI, `ui-monospace` pour le texte à taper. Aucun fetch réseau au runtime.
- Dépendances autorisées (runtime) : `react`, `react-dom`, `react-router-dom`, `zustand`, `dexie`, `framer-motion`, `zod`. (dev) : `typescript`, `vite`, `@vitejs/plugin-react`, `tailwindcss@^4`, `@tailwindcss/vite`, `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`, `fake-indexeddb`, `@types/react`, `@types/react-dom`, `@types/node` (requis par `tsc --noEmit` pour l'import `node:url` de `vite.config.ts`). **Aucune autre dépendance.**
- Node ≥ 20, npm.
- Corpus prose (`fr`, `en`) : 30 textes libres (ids `xx-001`..`xx-030`, dont 5 avec `quote: true`) + 10 officiels Challenger (ids `xx-101`..`xx-110`). Longueurs : short 110-200 car., medium 320-480 car., long 650-950 car.
- Corpus code (`c`, `python`, extension mode Dev — tâches 20-25) : 15 extraits libres (ids `xx-001`..`xx-015`) + 10 officiels. Longueurs : short 60-300, medium 280-800, long 650-2500 car. Code toujours brut (pas de toggles, multiplicateur ×1,4), multi-lignes, indentation en espaces uniquement, auto-indentation à la frappe.
- WPM = (caractères corrects / 5) / minutes. Précision = (frappes − erreurs) / frappes (1 si aucune frappe). Points = `round(WPM × précision² × multiplicateur)`, multiplicateur = `1 + 0,1 × nb de toggles actifs`.
- Tiers de ligue : bronze 100, argent 400, or 750, platine 950, diamant 1100, challenger 1300.
- Timestamps : `Date.now()` (ms). L'engine reçoit `now` en paramètre (jamais d'appel direct à `Date.now()` dans `engine/`), ce qui le rend testable.
- Chaque tâche se termine par un commit. Ne jamais committer avec des tests rouges.

## Structure des fichiers

```
package.json  tsconfig.json  vite.config.ts  index.html  README.md
scripts/validateCorpus.mjs
src/
  main.tsx  App.tsx  index.css  vite-env.d.ts
  test/setup.ts
  engine/types.ts  engine/typingEngine.ts  engine/stats.ts  (+ *.test.ts)
  texts/types.ts  texts/normalize.ts  texts/corpus.ts  texts/fr.json  texts/en.json  (+ tests)
  scoring/points.ts  scoring/league.ts  (+ tests)
  db/types.ts  db/schemas.ts  db/db.ts  db/runsRepo.ts  db/profileRepo.ts  db/challengerRepo.ts  db/persistence.ts  (+ tests)
  achievements/definitions.ts  achievements/check.ts  (+ tests)
  game/runFlow.ts  (+ tests)
  state/settingsStore.ts  state/runStore.ts  state/toastStore.ts
  ui/sounds.ts
  ui/hooks/useFocusGuard.ts
  ui/components/Layout.tsx  Header.tsx  Toggle.tsx  TierBadge.tsx  ToastHost.tsx
  ui/components/TypingArea.tsx  ResultsScreen.tsx  Sparkline.tsx  (+ tests)
  ui/pages/HomePage.tsx  PlayPage.tsx  ChallengerPage.tsx  LeaderboardPage.tsx
  ui/pages/AchievementsPage.tsx  StatsPage.tsx  statsUtils.ts  SettingsPage.tsx
```

---

### Task 1: Scaffold du projet (Vite + React + TS + Tailwind + Vitest)

**Files:**
- Create: `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/index.css`, `src/vite-env.d.ts`, `src/test/setup.ts`, `.gitignore`

**Interfaces:**
- Produces: scripts npm `dev` / `build` / `test` ; alias d'import `@/` → `src/` ; utilitaires Tailwind `bg-bg`, `bg-surface`, `text-text`, `text-muted`, `text-accent`, `border-line` basés sur variables CSS.

- [ ] **Step 1: Créer les fichiers de config**

`package.json` :

```json
{
  "name": "dacty",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:corpus": "node scripts/validateCorpus.mjs",
    "prebuild": "node scripts/validateCorpus.mjs"
  },
  "dependencies": {
    "dexie": "^4.0.11",
    "framer-motion": "^12.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.6.0",
    "zod": "^3.25.0",
    "zustand": "^5.0.5"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.5.2",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^26.1.0",
    "tailwindcss": "^4.1.0",
    "typescript": "~5.8.3",
    "vitest": "^3.1.0"
  }
}
```

`tsconfig.json` :

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "types": ["vitest/globals", "@testing-library/jest-dom"],
    "baseUrl": ".",
    "paths": { "@/*": ["src/*"] }
  },
  "include": ["src", "vite.config.ts"]
}
```

`vite.config.ts` :

```ts
/// <reference types="vitest/config" />
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import { fileURLToPath } from 'node:url';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) },
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['src/test/setup.ts'],
    css: false,
  },
});
```

`src/test/setup.ts` :

```ts
import '@testing-library/jest-dom/vitest';
```

`.gitignore` :

```
node_modules
dist
*.local
.DS_Store
```

`index.html` :

```html
<!doctype html>
<html lang="fr" data-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dacty — Speedrun dactylo</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`src/index.css` :

```css
@import "tailwindcss";

@theme inline {
  --color-bg: var(--bg);
  --color-surface: var(--surface);
  --color-text: var(--text);
  --color-muted: var(--muted);
  --color-accent: var(--accent);
  --color-line: var(--line);
  --color-ok: var(--ok);
  --color-err: var(--err);
}

:root {
  --bg: #0b0f14;
  --surface: #141b24cc;
  --text: #e6edf3;
  --muted: #8b98a5;
  --accent: #7c5cff;
  --line: #232d3a;
  --ok: #3fb950;
  --err: #f85149;
}

html[data-theme="light"] {
  --bg: #f6f8fa;
  --surface: #ffffffcc;
  --text: #1f2328;
  --muted: #59636e;
  --accent: #6a4dff;
  --line: #d1d9e0;
  --ok: #1a7f37;
  --err: #d1242f;
}

body {
  background: var(--bg);
  color: var(--text);
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif;
}

.font-type {
  font-family: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
}
```

`src/vite-env.d.ts` :

```ts
/// <reference types="vite/client" />
```

`src/main.tsx` :

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';
import './index.css';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

`src/App.tsx` (placeholder, remplacé à la tâche 10) :

```tsx
export default function App() {
  return <h1 className="p-8 text-2xl font-bold">Dacty</h1>;
}
```

- [ ] **Step 2: Installer et vérifier**

Run: `npm install && npm test -- --passWithNoTests && npm run dev` (dev server démarre, Ctrl+C pour arrêter).
Expected: install OK, vitest OK ("no tests"), Vite sert la page « Dacty ».

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: scaffold Vite + React + TS + Tailwind + Vitest"
```

---

### Task 2: Engine — types et machine à états de frappe

**Files:**
- Create: `src/engine/types.ts`, `src/engine/typingEngine.ts`
- Test: `src/engine/typingEngine.test.ts`

**Interfaces:**
- Produces (consommé par tâches 3, 9, 11) :
  - `type CharStatus = 'pending' | 'correct' | 'incorrect'`
  - `interface KeystrokeEvent { at: number; kind: 'char' | 'backspace'; correct: boolean }`
  - `interface TypingState` (voir code)
  - `createRun(text: string): TypingState`
  - `typeChar(state: TypingState, char: string, now: number): TypingState` — `char` = exactement 1 caractère ; no-op si run finie ou en pause
  - `pressBackspace(state: TypingState, now: number): TypingState` — no-op si cursor 0, finie ou en pause
  - `pauseRun(state: TypingState, now: number): TypingState` / `resumeRun(state: TypingState, now: number): TypingState`
  - `isFinished(state: TypingState): boolean`
  - `elapsedMs(state: TypingState, now: number): number` — hors pauses

Règles : caractère faux → `statuses[cursor] = 'incorrect'`, `errors++`, cursor avance ; backspace → cursor recule, remet le statut à `'pending'`, l'erreur déjà comptée **reste comptée** ; finie quand `cursor === text.length` ET tous les statuts `'correct'` (il faut donc corriger pour finir). La 1re frappe initialise `startedAt = now`. Immutabilité : chaque fonction retourne un nouvel objet.

- [ ] **Step 1: Écrire le test qui échoue**

`src/engine/typingEngine.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import {
  createRun, typeChar, pressBackspace, pauseRun, resumeRun, isFinished, elapsedMs,
} from './typingEngine';

describe('typingEngine', () => {
  it('démarre à la première frappe et marque les caractères corrects', () => {
    let s = createRun('ab');
    expect(s.startedAt).toBeNull();
    s = typeChar(s, 'a', 1000);
    expect(s.startedAt).toBe(1000);
    expect(s.statuses).toEqual(['correct', 'pending']);
    expect(s.cursor).toBe(1);
    expect(s.keystrokes).toBe(1);
  });

  it('compte les erreurs même après correction par backspace', () => {
    let s = createRun('ab');
    s = typeChar(s, 'x', 1000); // erreur
    expect(s.errors).toBe(1);
    expect(s.statuses[0]).toBe('incorrect');
    s = pressBackspace(s, 1100);
    expect(s.cursor).toBe(0);
    expect(s.statuses[0]).toBe('pending');
    expect(s.errors).toBe(1); // reste comptée
    expect(s.backspaces).toBe(1);
    s = typeChar(s, 'a', 1200);
    expect(s.statuses[0]).toBe('correct');
    expect(s.keystrokes).toBe(2);
  });

  it("n'est finie que si tous les caractères sont corrects", () => {
    let s = createRun('ab');
    s = typeChar(s, 'a', 1000);
    s = typeChar(s, 'x', 1100); // erreur en fin
    expect(isFinished(s)).toBe(false);
    s = pressBackspace(s, 1200);
    s = typeChar(s, 'b', 1300);
    expect(isFinished(s)).toBe(true);
    expect(s.finishedAt).toBe(1300);
  });

  it('ignore les frappes après la fin', () => {
    let s = createRun('a');
    s = typeChar(s, 'a', 1000);
    const after = typeChar(s, 'b', 1100);
    expect(after).toBe(s);
  });

  it('met en pause, ignore les frappes en pause, exclut la pause du temps', () => {
    let s = createRun('ab');
    s = typeChar(s, 'a', 1000);
    s = pauseRun(s, 1500);
    expect(typeChar(s, 'b', 1600)).toBe(s);
    expect(elapsedMs(s, 1500)).toBe(500);
    s = resumeRun(s, 3000);
    expect(elapsedMs(s, 3500)).toBe(1000); // 1500ms de pause exclues
    s = typeChar(s, 'b', 4000);
    expect(isFinished(s)).toBe(true);
    expect(elapsedMs(s, 9999)).toBe(1500); // 4000-1000-1500
  });

  it('backspace au début est un no-op', () => {
    const s = createRun('ab');
    expect(pressBackspace(s, 1000)).toBe(s);
  });

  it('enregistre les événements de frappe', () => {
    let s = createRun('ab');
    s = typeChar(s, 'a', 1000);
    s = typeChar(s, 'x', 1100);
    s = pressBackspace(s, 1200);
    expect(s.events).toEqual([
      { at: 1000, kind: 'char', correct: true },
      { at: 1100, kind: 'char', correct: false },
      { at: 1200, kind: 'backspace', correct: true },
    ]);
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/engine/typingEngine.test.ts`
Expected: FAIL (module introuvable)

- [ ] **Step 3: Implémenter**

`src/engine/types.ts` :

```ts
export type CharStatus = 'pending' | 'correct' | 'incorrect';

export interface KeystrokeEvent {
  at: number;
  kind: 'char' | 'backspace';
  /** true pour un caractère correct ou un backspace (non une « réussite », juste pas une erreur) */
  correct: boolean;
}

export interface TypingState {
  text: string;
  statuses: CharStatus[];
  cursor: number;
  /** erreurs commises, y compris corrigées ensuite */
  errors: number;
  backspaces: number;
  /** frappes de caractères (hors backspace) */
  keystrokes: number;
  events: KeystrokeEvent[];
  startedAt: number | null;
  finishedAt: number | null;
  pausedMs: number;
  pauseStartedAt: number | null;
}
```

`src/engine/typingEngine.ts` :

```ts
import type { CharStatus, TypingState } from './types';

export function createRun(text: string): TypingState {
  if (text.length === 0) throw new Error('createRun: texte vide');
  return {
    text,
    statuses: Array<CharStatus>(text.length).fill('pending'),
    cursor: 0,
    errors: 0,
    backspaces: 0,
    keystrokes: 0,
    events: [],
    startedAt: null,
    finishedAt: null,
    pausedMs: 0,
    pauseStartedAt: null,
  };
}

function active(state: TypingState): boolean {
  return state.finishedAt === null && state.pauseStartedAt === null;
}

export function typeChar(state: TypingState, char: string, now: number): TypingState {
  if (!active(state) || state.cursor >= state.text.length || char.length !== 1) return state;
  const correct = state.text[state.cursor] === char;
  const statuses = state.statuses.slice();
  statuses[state.cursor] = correct ? 'correct' : 'incorrect';
  const next: TypingState = {
    ...state,
    statuses,
    cursor: state.cursor + 1,
    errors: correct ? state.errors : state.errors + 1,
    keystrokes: state.keystrokes + 1,
    startedAt: state.startedAt ?? now,
    events: [...state.events, { at: now, kind: 'char', correct }],
  };
  if (next.cursor === next.text.length && next.statuses.every((s) => s === 'correct')) {
    next.finishedAt = now;
  }
  return next;
}

export function pressBackspace(state: TypingState, now: number): TypingState {
  if (!active(state) || state.cursor === 0) return state;
  const statuses = state.statuses.slice();
  statuses[state.cursor - 1] = 'pending';
  return {
    ...state,
    statuses,
    cursor: state.cursor - 1,
    backspaces: state.backspaces + 1,
    events: [...state.events, { at: now, kind: 'backspace', correct: true }],
  };
}

export function pauseRun(state: TypingState, now: number): TypingState {
  if (state.finishedAt !== null || state.pauseStartedAt !== null || state.startedAt === null) {
    return state;
  }
  return { ...state, pauseStartedAt: now };
}

export function resumeRun(state: TypingState, now: number): TypingState {
  if (state.pauseStartedAt === null) return state;
  return {
    ...state,
    pausedMs: state.pausedMs + (now - state.pauseStartedAt),
    pauseStartedAt: null,
  };
}

export function isFinished(state: TypingState): boolean {
  return state.finishedAt !== null;
}

export function elapsedMs(state: TypingState, now: number): number {
  if (state.startedAt === null) return 0;
  const end = state.finishedAt ?? state.pauseStartedAt ?? now;
  return Math.max(0, end - state.startedAt - state.pausedMs);
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/engine/typingEngine.test.ts`
Expected: 7 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/engine/
git commit -m "feat(engine): typing state machine with pause and error tracking"
```

---

### Task 3: Engine — statistiques (WPM, précision, timeline)

**Files:**
- Create: `src/engine/stats.ts`
- Test: `src/engine/stats.test.ts`

**Interfaces:**
- Consumes: `TypingState`, `KeystrokeEvent`, `elapsedMs` (tâche 2).
- Produces (consommé par tâches 9, 11, 16) :
  - `computeWpm(correctChars: number, ms: number): number` — 0 si `ms <= 0`
  - `computeAccuracy(keystrokes: number, errors: number): number` — ratio 0..1, 1 si `keystrokes === 0`
  - `correctCharCount(state: TypingState): number`
  - `liveWpm(state: TypingState, now: number): number`
  - `wpmTimeline(events: KeystrokeEvent[], startedAt: number, endedAt: number, bucketMs = 1000): number[]` — WPM glissant par seau (caractères corrects du seau / 5, ramenés à la minute)

- [ ] **Step 1: Écrire le test qui échoue**

`src/engine/stats.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import { computeAccuracy, computeWpm, correctCharCount, liveWpm, wpmTimeline } from './stats';
import { createRun, typeChar } from './typingEngine';

describe('stats', () => {
  it('computeWpm = (caractères / 5) / minutes', () => {
    expect(computeWpm(50, 60_000)).toBe(10);
    expect(computeWpm(25, 30_000)).toBe(10);
    expect(computeWpm(10, 0)).toBe(0);
  });

  it('computeAccuracy = (frappes - erreurs) / frappes, 1 si aucune frappe', () => {
    expect(computeAccuracy(100, 5)).toBe(0.95);
    expect(computeAccuracy(0, 0)).toBe(1);
  });

  it('correctCharCount compte les statuts corrects', () => {
    let s = createRun('abc');
    s = typeChar(s, 'a', 1000);
    s = typeChar(s, 'x', 1100);
    expect(correctCharCount(s)).toBe(1);
  });

  it('liveWpm utilise le temps écoulé réel', () => {
    let s = createRun('abcdefghij'); // 10 car.
    for (let i = 0; i < 10; i++) s = typeChar(s, 'abcdefghij'[i], 1000 + i * 600);
    // 10 car. corrects en 5400ms -> (10/5)/(5.4/60) = 22.22 WPM
    expect(liveWpm(s, 6400)).toBeCloseTo(22.22, 1);
  });

  it('wpmTimeline agrège les caractères corrects par seau', () => {
    const events = [
      { at: 100, kind: 'char' as const, correct: true },
      { at: 400, kind: 'char' as const, correct: true },
      { at: 900, kind: 'char' as const, correct: false },
      { at: 1100, kind: 'char' as const, correct: true },
    ];
    // seau 0-1000 : 2 corrects -> (2/5)*60 = 24 WPM ; seau 1000-2000 : 1 correct -> 12 WPM
    expect(wpmTimeline(events, 0, 2000)).toEqual([24, 12]);
  });

  it('wpmTimeline borne le dernier seau à endedAt', () => {
    const events = [{ at: 2500, kind: 'char' as const, correct: true }];
    expect(wpmTimeline(events, 0, 2600)).toEqual([0, 0, 12]);
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/engine/stats.test.ts`
Expected: FAIL (module introuvable)

- [ ] **Step 3: Implémenter**

`src/engine/stats.ts` :

```ts
import type { KeystrokeEvent, TypingState } from './types';
import { elapsedMs } from './typingEngine';

export function computeWpm(correctChars: number, ms: number): number {
  if (ms <= 0) return 0;
  return correctChars / 5 / (ms / 60_000);
}

export function computeAccuracy(keystrokes: number, errors: number): number {
  if (keystrokes === 0) return 1;
  return Math.max(0, (keystrokes - errors) / keystrokes);
}

export function correctCharCount(state: TypingState): number {
  return state.statuses.filter((s) => s === 'correct').length;
}

export function liveWpm(state: TypingState, now: number): number {
  return computeWpm(correctCharCount(state), elapsedMs(state, now));
}

export function wpmTimeline(
  events: KeystrokeEvent[],
  startedAt: number,
  endedAt: number,
  bucketMs = 1000,
): number[] {
  if (endedAt <= startedAt) return [];
  const buckets = Math.ceil((endedAt - startedAt) / bucketMs);
  const correctPerBucket = Array<number>(buckets).fill(0);
  for (const e of events) {
    if (e.kind !== 'char' || !e.correct) continue;
    const idx = Math.min(buckets - 1, Math.floor((e.at - startedAt) / bucketMs));
    if (idx >= 0) correctPerBucket[idx] += 1;
  }
  return correctPerBucket.map((n) => n / 5 / (bucketMs / 60_000));
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/engine/stats.test.ts`
Expected: 6 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/engine/stats.ts src/engine/stats.test.ts
git commit -m "feat(engine): wpm, accuracy and per-second timeline stats"
```

---

### Task 4: Texts — types et normalisation (toggles de difficulté)

**Files:**
- Create: `src/texts/types.ts`, `src/texts/normalize.ts`
- Test: `src/texts/normalize.test.ts`

**Interfaces:**
- Produces (consommé par tâches 5, 6, 9, 12, 13) :
  - `type Language = 'fr' | 'en'`
  - `type TextLength = 'short' | 'medium' | 'long'`
  - `interface TextEntry { id: string; language: Language; length: TextLength; official: boolean; quote?: boolean; source?: string; text: string }`
  - `interface TextOptions { punctuation: boolean; specialChars: boolean; digits: boolean; accents: boolean }`
  - `const ALL_OPTIONS_ON: TextOptions`
  - `applyOptions(raw: string, options: TextOptions): string`

Règles de normalisation, appliquées dans cet ordre : `accents` off → NFD sans diacritiques ; `digits` off → chiffres supprimés ; `punctuation` off → apostrophes et traits d'union remplacés par un espace, autre ponctuation supprimée ; `specialChars` off → symboles (`\p{S}` : `&@#%€$£*+=<>{}[]\/|_~^`) supprimés. Toujours finir par collapse des espaces multiples + trim. `applyOptions` avec toutes les options à `true` retourne le texte normalisé en espaces seulement (collapse + trim).

- [ ] **Step 1: Écrire le test qui échoue**

`src/texts/normalize.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import { applyOptions, ALL_OPTIONS_ON } from './normalize';
import type { TextOptions } from './types';

const off = (patch: Partial<TextOptions>): TextOptions => ({ ...ALL_OPTIONS_ON, ...patch });

describe('applyOptions', () => {
  it('retourne le texte intact (hors espaces) quand tout est actif', () => {
    expect(applyOptions('  Bonjour   le  monde ! ', ALL_OPTIONS_ON)).toBe('Bonjour le monde !');
  });

  it('supprime les accents quand accents=false', () => {
    expect(applyOptions('Éléphant à Noël, cœur brisé', off({ accents: false })))
      .toBe('Elephant a Noel, cœur brise');
  });

  it('supprime les chiffres quand digits=false', () => {
    expect(applyOptions('Il a 12 ans et 3 chiens', off({ digits: false })))
      .toBe('Il a  ans et  chiens'.replace(/ +/g, ' ').trim());
  });

  it('ponctuation off : apostrophes/traits d’union → espace, le reste supprimé', () => {
    expect(applyOptions("L'arbre-porte est beau, vraiment !", off({ punctuation: false })))
      .toBe('L arbre porte est beau vraiment');
    expect(applyOptions('« Bonjour » dit-elle.', off({ punctuation: false })))
      .toBe('Bonjour dit elle');
  });

  it('supprime les caractères spéciaux quand specialChars=false', () => {
    expect(applyOptions('Prix : 12€ & 30% #promo @toi', off({ specialChars: false })))
      .toBe('Prix : 12 30 promo toi');
  });

  it('combine tous les toggles off', () => {
    expect(
      applyOptions("L'élève a 15€, soit 20% de « réduction » !", {
        punctuation: false, specialChars: false, digits: false, accents: false,
      }),
    ).toBe('L eleve a soit de reduction');
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/texts/normalize.test.ts`
Expected: FAIL (module introuvable)

- [ ] **Step 3: Implémenter**

`src/texts/types.ts` :

```ts
export type Language = 'fr' | 'en';
export type TextLength = 'short' | 'medium' | 'long';

export interface TextEntry {
  id: string;
  language: Language;
  length: TextLength;
  official: boolean;
  quote?: boolean;
  source?: string;
  text: string;
}

export interface TextOptions {
  punctuation: boolean;
  specialChars: boolean;
  digits: boolean;
  accents: boolean;
}
```

`src/texts/normalize.ts` :

```ts
import type { TextOptions } from './types';

export const ALL_OPTIONS_ON: TextOptions = {
  punctuation: true,
  specialChars: true,
  digits: true,
  accents: true,
};

const collapse = (s: string): string => s.replace(/\s+/g, ' ').trim();

export function applyOptions(raw: string, options: TextOptions): string {
  let out = raw;
  if (!options.accents) {
    out = out.normalize('NFD').replace(/\p{M}/gu, '');
  }
  if (!options.digits) {
    out = out.replace(/\p{N}/gu, '');
  }
  if (!options.punctuation) {
    // apostrophes et traits d'union soudent deux mots : on les remplace par un espace
    out = out.replace(/['’ʼ-]/g, ' ');
    out = out.replace(/[.,;:!?"«»‹›()–—…]/g, '');
  }
  if (!options.specialChars) {
    out = out.replace(/\p{S}/gu, '');
    out = out.replace(/[&@#%*+=<>{}[\]\\/|_~^`]/g, '');
  }
  return collapse(out);
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/texts/normalize.test.ts`
Expected: 6 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/texts/types.ts src/texts/normalize.ts src/texts/normalize.test.ts
git commit -m "feat(texts): text types and difficulty-toggle normalization"
```

---

### Task 5: Texts — corpus FR/EN + validateur au build

**Files:**
- Create: `src/texts/fr.json`, `src/texts/en.json`, `src/texts/corpus.ts`, `scripts/validateCorpus.mjs`
- Test: `src/texts/corpus.test.ts`

**Interfaces:**
- Consumes: `TextEntry`, `Language`, `TextLength` (tâche 4).
- Produces (consommé par tâches 9, 12, 13, 14) :
  - `CORPUS: TextEntry[]`
  - `getTexts(language: Language): TextEntry[]` — les 30 textes libres (`official: false`)
  - `getOfficialTexts(language: Language): TextEntry[]` — les 10 officiels, triés par id
  - `getTextById(id: string): TextEntry` — throw si inconnu
  - `pickText(language: Language, choice: TextLength | 'quote'): TextEntry` — aléatoire dans la catégorie

Format JSON par fichier : `{ "language": "fr", "texts": [ { "id", "length", "official", "quote"?, "source"?, "text" } ] }` (le champ `language` est injecté par `corpus.ts`, pas répété dans le JSON).

Validateur (`scripts/validateCorpus.mjs`, exécuté en `prebuild`) : ids uniques et format `^(fr|en)-\d{3}$` ; 30 libres (ids 001-030, dont exactement 5 `quote: true`) et 10 officiels (ids 101-110) par langue ; fourchettes short 110-200 / medium 320-480 / long 650-950 caractères ; chaque officiel contient au moins un signe de ponctuation ; exit code 1 avec message explicite sinon.

- [ ] **Step 1: Écrire le corpus FR**

`src/texts/fr.json` (complet — 30 libres + 10 officiels) :

```json
{
  "language": "fr",
  "texts": [
    { "id": "fr-001", "length": "short", "official": false, "text": "Le chat est un animal domestique très apprécié pour son indépendance, sa souplesse et sa capacité à chasser les rongeurs autour de la maison familiale." },
    { "id": "fr-002", "length": "short", "official": false, "text": "La Provence attire chaque été des milliers de visiteurs venus admirer les champs de lavande, les villages perchés et les marchés colorés du sud." },
    { "id": "fr-003", "length": "short", "official": false, "text": "Le mont Blanc culmine à quatre mille huit cent huit mètres d'altitude et attire chaque année des alpinistes venus du monde entier." },
    { "id": "fr-004", "length": "short", "official": false, "quote": true, "source": "Voltaire, Candide", "text": "Il faut cultiver notre jardin, écrivait Voltaire, car le travail éloigne de nous trois grands maux : l'ennui, le vice et le besoin." },
    { "id": "fr-005", "length": "short", "official": false, "quote": true, "source": "Victor Hugo", "text": "La liberté commence où l'ignorance finit, disait Victor Hugo, et chaque page tournée est une victoire contre l'ombre." },
    { "id": "fr-006", "length": "short", "official": false, "quote": true, "source": "Attribué à Albert Einstein", "text": "La vie est comme une bicyclette, il faut avancer pour ne pas perdre l'équilibre, répétait Albert Einstein à ses étudiants." },
    { "id": "fr-007", "length": "short", "official": false, "quote": true, "source": "Antoine de Saint-Exupéry", "text": "On ne voit bien qu'avec le cœur, écrivait Saint-Exupéry dans Le Petit Prince, car l'essentiel est invisible pour les yeux." },
    { "id": "fr-008", "length": "short", "official": false, "quote": true, "source": "Jean de La Fontaine", "text": "Rien ne sert de courir, il faut partir à point, nous enseigne La Fontaine dans sa fable du lièvre et de la tortue." },
    { "id": "fr-009", "length": "short", "official": false, "text": "Un croissant au beurre, un café serré et le journal du matin : voilà le rituel préféré de nombreux Parisiens pressés mais gourmands." },
    { "id": "fr-010", "length": "short", "official": false, "text": "La forêt amazonienne produit une part importante de l'oxygène de la planète et abrite des millions d'espèces encore inconnues." },
    { "id": "fr-011", "length": "medium", "official": false, "text": "La dactylographie est née avec la machine à écrire au dix-neuvième siècle. Les premiers claviers adoptaient déjà des dispositions pensées pour éviter le blocage des marteaux. Aujourd'hui, taper vite et sans faute reste une compétence précieuse, que l'on écrive du code, des courriels ou des romans. Comme souvent, c'est la régularité de l'entraînement qui fait la différence." },
    { "id": "fr-012", "length": "medium", "official": false, "text": "Le phare de Cordouan, situé à l'embouchure de la Gironde, est le plus ancien phare de France encore en activité. Construit à la fin du seizième siècle, il guidait les navires à travers des passes dangereuses. Les gardiens y vivaient parfois plusieurs semaines sans retourner sur la terre ferme, isolés au milieu des flots, entre le ciel et l'estuaire." },
    { "id": "fr-013", "length": "medium", "official": false, "text": "Le pain au levain demande de la patience. Il faut nourrir le levain chaque jour, surveiller sa consistance et son odeur, puis pétrir la pâte et la laisser lever lentement. Cette lenteur donne au pain sa mie alvéolée et son goût légèrement acide. Les boulangers disent que le levain est un être vivant capricieux qui récompense ceux qui prennent le temps." },
    { "id": "fr-014", "length": "medium", "official": false, "text": "En informatique, un algorithme de tri organise des éléments dans un ordre précis. Certains tris sont simples à comprendre mais lents, comme le tri à bulles. D'autres, comme le tri fusion, découpent le problème en morceaux plus petits pour aller beaucoup plus vite. Le choix du bon algorithme dépend de la taille des données et du contexte d'utilisation." },
    { "id": "fr-015", "length": "medium", "official": false, "text": "Les vignobles de Bourgogne s'étendent sur de petites parcelles appelées climats, chacune avec son exposition et son sol particuliers. Deux parcelles voisines peuvent produire des vins très différents. Cette mosaïque, façonnée par des siècles de travail des moines puis des vignerons, est inscrite au patrimoine mondial de l'humanité depuis deux mille quinze." },
    { "id": "fr-016", "length": "medium", "official": false, "text": "La course à pied ne demande presque aucun matériel, et c'est sans doute ce qui explique son succès. Une paire de chaussures adaptée suffit pour débuter. Les entraîneurs conseillent de progresser lentement, d'alterner marche et course, et d'écouter son corps. La plupart des blessures surviennent quand on augmente les distances trop vite, par excès d'enthousiasme." },
    { "id": "fr-017", "length": "medium", "official": false, "text": "Le cinéma des frères Lumière a commencé par des vues courtes filmées à Lyon, comme la sortie des usines ou l'arrivée d'un train en gare. Le public de l'époque découvrait avec stupeur des images animées sur grand écran. Un siècle plus tard, leurs films de quelques secondes restent des documents précieux sur la vie quotidienne de la fin du dix-neuvième siècle." },
    { "id": "fr-018", "length": "medium", "official": false, "text": "Apprendre une langue étrangère repose sur trois piliers : le vocabulaire, la grammaire et surtout la pratique régulière. Quinze minutes par jour valent mieux que trois heures le dimanche. Les erreurs font partie du processus, et les linguistes rappellent que parler imparfaitement vaut toujours mieux que se taire parfaitement. La régularité transforme l'effort en automatisme." },
    { "id": "fr-019", "length": "medium", "official": false, "text": "La Méditerranée doit son nom aux Romains, qui la considéraient comme la mer au milieu des terres. Presque fermée, elle se renouvelle lentement et reste fragile face à la pollution. Ses fonds abritent des prairies de posidonies, des plantes marines qui stockent le carbone et servent de nurseries à de nombreuses espèces de poissons méditerranéens." },
    { "id": "fr-020", "length": "medium", "official": false, "text": "Les échecs sont un jeu de stratégie vieux de quinze siècles, né en Inde avant de conquérir la Perse puis l'Europe. Chaque pièce possède ses propres déplacements, et la partie se gagne en mettant le roi adverse échec et mat. Les grands maîtres mémorisent des milliers de positions et calculent plusieurs coups à l'avance, mais tout repose sur des règles simples." },
    { "id": "fr-021", "length": "medium", "official": false, "text": "Le thé est la boisson la plus bue au monde après l'eau. Originaire de Chine, il a conquis le Japon, l'Inde puis l'Europe au fil des siècles. Vert, noir ou fumé, chaque thé demande une température et un temps d'infusion différents. Au Japon, la cérémonie du thé transforme sa préparation en un art codifié où chaque geste compte." },
    { "id": "fr-022", "length": "medium", "official": false, "text": "Les abeilles jouent un rôle essentiel dans la pollinisation des cultures. Une colonie peut compter jusqu'à cinquante mille ouvrières en été, dirigées par une seule reine. Pour indiquer une source de nectar à leurs sœurs, les butineuses exécutent une danse en forme de huit qui code la direction et la distance. Sans elles, une grande partie de nos fruits disparaîtrait." },
    { "id": "fr-023", "length": "long", "official": false, "text": "L'imprimerie a transformé l'Europe en quelques décennies. Avant Gutenberg, chaque livre était recopié à la main par des moines ou des copistes, ce qui le rendait rare et précieux. Avec les caractères mobiles, un même texte pouvait être reproduit des centaines de fois, rapidement et à moindre coût. Les idées ont alors circulé à une vitesse inédite : traités scientifiques, traductions, pamphlets et journaux ont touché un public de plus en plus large. Cette révolution technique a favorisé l'alphabétisation, standardisé les langues et préparé le terrain des grandes transformations intellectuelles des siècles suivants. Chaque fois qu'un nouveau moyen de diffuser l'information apparaît, de la presse à internet, les historiens le comparent à cette invention décisive du quinzième siècle." },
    { "id": "fr-024", "length": "long", "official": false, "text": "Le Tour de France est né en mille neuf cent trois, imaginé par un journal pour augmenter ses ventes. Les premières éditions étaient d'une rudesse inouïe : des étapes de plus de quatre cents kilomètres, des vélos lourds sans dérailleur, des routes non goudronnées et des réparations effectuées par les coureurs eux-mêmes, parfois au milieu de la nuit. Au fil des décennies, la course est devenue un spectacle mondial, avec ses cols mythiques comme le Galibier ou le mont Ventoux, ses maillots distinctifs et ses légendes. Chaque juillet, des millions de spectateurs se massent sur les routes de France pour voir passer le peloton quelques secondes. Le Tour reste à la fois une épreuve sportive extrême et une fête populaire, profondément ancrée dans le paysage culturel français." },
    { "id": "fr-025", "length": "long", "official": false, "text": "La photosynthèse est le processus par lequel les plantes transforment la lumière du soleil en énergie chimique. Dans les feuilles, la chlorophylle capte les photons et utilise leur énergie pour assembler du dioxyde de carbone et de l'eau en sucres, tout en rejetant de l'oxygène. Ce mécanisme, apparu il y a des milliards d'années chez des bactéries primitives, a littéralement façonné l'atmosphère de la planète et rendu possible la vie telle que nous la connaissons. Sans lui, pas de forêts, pas de récoltes, pas de chaîne alimentaire. Les chercheurs tentent encore d'imiter artificiellement ce processus pour produire des carburants propres, mais la nature garde une longueur d'avance : aucune technologie ne rivalise encore avec l'efficacité discrète d'une simple feuille exposée au soleil." },
    { "id": "fr-026", "length": "long", "official": false, "text": "Versailles n'était au départ qu'un modeste pavillon de chasse bâti par Louis XIII dans une vallée marécageuse. Son fils, Louis XIV, décida d'en faire le symbole éclatant de son pouvoir absolu. Pendant des décennies, architectes, peintres et jardiniers transformèrent le site : la galerie des Glaces éblouissait les ambassadeurs, les jardins à la française dessinaient des perspectives parfaites, et la cour entière vivait selon un étiquette minutieuse réglée par le roi lui-même. Tenir son rang à Versailles était un art de tous les instants. Le château devint le centre politique de la France jusqu'à la Révolution, et son modèle fut copié par les cours de toute l'Europe. Aujourd'hui encore, des millions de visiteurs parcourent ses salons dorés et ses allées, témoins d'une époque où l'architecture était un langage du pouvoir." },
    { "id": "fr-027", "length": "long", "official": false, "text": "Le sommeil reste l'un des grands mystères de la biologie. Chaque nuit, notre cerveau traverse plusieurs cycles composés de sommeil lent et de sommeil paradoxal, cette phase étrange où les rêves surgissent et où le corps est presque paralysé. Pendant le sommeil profond, le cerveau consolide les souvenirs accumulés dans la journée et élimine des déchets métaboliques. Les études montrent qu'un déficit chronique de sommeil dégrade la mémoire, l'humeur et le système immunitaire. Pourtant, nos nuits sont de plus en plus courtes, grignotées par les écrans et les rythmes de vie décalés. Les spécialistes conseillent des horaires réguliers, une chambre fraîche et sombre, et une coupure des écrans avant le coucher. Dormir suffisamment n'est pas un luxe : c'est un besoin biologique aussi fondamental que manger ou respirer." },
    { "id": "fr-028", "length": "long", "official": false, "text": "La soie a façonné l'histoire des échanges entre l'Orient et l'Occident pendant plus de mille ans. Produite par le ver à soie, dont le cocon se dévide en un filament unique pouvant dépasser neuf cents mètres, elle était si précieuse en Chine que son secret de fabrication était gardé sous peine de mort. Des caravanes entières parcouraient les pistes d'Asie centrale pour l'apporter jusqu'en Méditerranée, donnant son nom à la célèbre route de la soie. Avec les tissus voyageaient aussi les religions, les techniques et les langues. Plus tard, la ville de Lyon devint la capitale européenne de la soierie, et ses canuts inventèrent des métiers à tisser de plus en plus ingénieux. Leur histoire montre comment une fibre presque immatérielle a pu déplacer des marchands, fonder des fortunes et tisser des liens entre des civilisations très éloignées." },
    { "id": "fr-029", "length": "long", "official": false, "text": "Les volcans fascinent autant qu'ils effraient. Sous la croûte terrestre, le magma s'accumule dans des chambres jusqu'à ce que la pression devienne trop forte : l'éruption projette alors cendres, gaz et coulées de lave. Certaines éruptions ont marqué l'histoire, comme celle du Vésuve qui figea Pompéi sous une pluie de cendres, offrant aux archéologues une photographie unique de la vie romaine. D'autres, plus discrètes, façonnent lentement les paysages en créant des îles entières. Les volcans ne sont pas que destructeurs : leurs sols sont parmi les plus fertiles du monde, leurs sources chaudes fournissent de l'énergie, et ils nous renseignent sur les entrailles de la planète. La volcanologie moderne surveille les secousses et les déformations du sol pour anticiper les réveils, mais prévoir l'heure exacte d'une éruption reste aujourd'hui encore un défi scientifique." },
    { "id": "fr-030", "length": "long", "official": false, "text": "L'art du bonsaï consiste à cultiver des arbres miniatures en pot, en guidant leur croissance pendant des années, parfois des générations. Né en Chine avant d'être raffiné au Japon, il demande un mélange rare de patience, de technique et de sens artistique. Le pratiquant taille les racines et les branches, ligature les rameaux avec du fil pour orienter leur pente, et choisit avec soin le pot qui accompagnera l'arbre. Contrairement à une idée reçue, un bonsaï n'est pas une espèce particulière : presque n'importe quel arbre peut être miniaturisé, de l'érable au pin. L'objectif n'est pas de faire petit, mais de suggérer en quelques dizaines de centimètres la force d'un arbre majestueux sculpté par le temps et les intempéries. Un beau bonsaï raconte une histoire, celle d'une nature que l'homme accompagne sans jamais vraiment la dominer." },
    { "id": "fr-101", "length": "short", "official": true, "text": "Paris, capitale de la France, compte plus de deux millions d'habitants. La Seine traverse la ville du sud-est au nord-ouest, passant au pied de la tour Eiffel et de Notre-Dame." },
    { "id": "fr-102", "length": "short", "official": true, "text": "Le printemps revient, les jours s'allongent : les cerisiers fleurissent, les oiseaux chantent dès l'aube, et les terrasses des cafés se remplissent à nouveau." },
    { "id": "fr-103", "length": "short", "official": true, "text": "En 1789, la Révolution française a bouleversé le pays : la Bastille est prise le 14 juillet, et la Déclaration des droits de l'homme est adoptée quelques semaines plus tard." },
    { "id": "fr-104", "length": "medium", "official": true, "text": "Le marathon mesure exactement 42,195 kilomètres. Cette distance, fixée lors des Jeux olympiques de 1908 à Londres, reliait le château de Windsor à la ligne d'arrivée du stade. Depuis, des millions de coureurs tentent chaque année de boucler cette épreuve mythique, des rues de Paris à celles de New York. Le record du monde avoisine désormais les deux heures." },
    { "id": "fr-105", "length": "medium", "official": true, "text": "Le mont Everest culmine à 8 849 mètres : c'est le point le plus haut de la planète. Edmund Hillary et Tensing Norgay atteignirent son sommet le 29 mai 1953, une première historique. Depuis, plus de 6 000 alpinistes ont réussi l'ascension, mais l'altitude rend chaque tentative dangereuse : au-delà de 8 000 mètres, le corps humain dépérit." },
    { "id": "fr-106", "length": "medium", "official": true, "text": "L'électricité est devenue indispensable en moins d'un siècle : éclairage, chauffage, transports, communications... En France, près de 65 % de la production provient du nucléaire, le reste de l'hydraulique, de l'éolien et du solaire. Le défi des prochaines décennies sera de produire plus, tout en émettant moins de CO2." },
    { "id": "fr-107", "length": "medium", "official": true, "text": "Le code Morse, inventé dans les années 1830, attribue à chaque lettre une suite de points et de traits : la lettre E vaut un simple point, le fameux SOS se note ... --- ... Utilisé par les télégraphistes puis les marins, il a sauvé d'innombrables vies en mer. Aujourd'hui, quelques radioamateurs le pratiquent encore, par nostalgie ou par défi." },
    { "id": "fr-108", "length": "long", "official": true, "text": "Le 21 juillet 1969, à 2 h 56 temps universel, Neil Armstrong posait le pied sur la Lune : « C'est un petit pas pour l'homme, un bond de géant pour l'humanité. » La mission Apollo 11 avait décollé 4 jours plus tôt de Cap Canaveral, emportant 3 astronautes à près de 384 000 kilomètres de la Terre. Michael Collins resta en orbite pendant qu'Armstrong et Aldrin descendaient sur la mer de la Tranquillité. Ils y passèrent environ 21 heures, plantèrent un drapeau, rapportèrent 21,5 kg de roches et décollèrent pour rejoindre le module de commande. Au total, le programme Apollo coûta plus de 25 milliards de dollars et mobilisa jusqu'à 400 000 personnes. Douze hommes ont marché sur la Lune entre 1969 et 1972 ; depuis, personne n'y est retourné." },
    { "id": "fr-109", "length": "long", "official": true, "text": "Internet est né d'un projet militaire américain : l'ARPANET, mis en service en 1969, reliait 4 universités. Le principe était révolutionnaire — découper les messages en paquets, les envoyer par des routes différentes, puis les réassembler à l'arrivée. En 1983, le protocole TCP/IP s'imposa comme langue commune du réseau. Mais c'est Tim Berners-Lee qui, en 1989 au CERN, inventa le Web : les URL, le HTTP et le HTML, 3 briques qui rendirent le réseau accessible à tous. Le premier site web, publié en 1991, expliquait... comment créer un site web. Trente ans plus tard, plus de 5 milliards de personnes sont connectées, et le trafic mondial se mesure en centaines d'exaoctets — des milliards de milliards d'octets échangés chaque année." },
    { "id": "fr-110", "length": "long", "official": true, "text": "Le prix Nobel, créé par le testament d'Alfred Nobel en 1895, récompense chaque année des travaux « au plus grand bénéfice de l'humanité » dans 6 disciplines : physique, chimie, médecine, littérature, paix et économie. L'inventeur de la dynamite, riche de 355 brevets, voulait sans doute racheter sa postérité. La première cérémonie eut lieu en 1901 ; Marie Curie reste la seule personne à avoir reçu 2 prix dans 2 sciences différentes (physique en 1903, chimie en 1911). Chaque lauréat reçoit une médaille d'or, un diplôme et une somme d'environ 11 millions de couronnes suédoises. Les nominations restent secrètes pendant 50 ans : impossible de savoir qui fut pressenti, combien de fois, ni pourquoi certains grands noms — Tolstoï, Joyce, Gandhi — ne furent jamais choisis." }
  ]
}
```

- [ ] **Step 2: Écrire le corpus EN**

`src/texts/en.json` (complet — 30 libres + 10 officiels) :

```json
{
  "language": "en",
  "texts": [
    { "id": "en-001", "length": "short", "official": false, "text": "The honeybee can visit up to five thousand flowers in a single day, flying from dawn to dusk to feed its colony." },
    { "id": "en-002", "length": "short", "official": false, "text": "A good cup of coffee starts with fresh beans, clean water and a grind that matches the brewing method you prefer." },
    { "id": "en-003", "length": "short", "official": false, "text": "The Pacific Ocean is so vast that it covers more surface area than all the continents of the Earth combined." },
    { "id": "en-004", "length": "short", "official": false, "quote": true, "source": "William Shakespeare, Hamlet", "text": "To be, or not to be, that is the question, wrote Shakespeare, giving Hamlet the most famous line in all of theatre." },
    { "id": "en-005", "length": "short", "official": false, "quote": true, "source": "Charles Dickens, A Tale of Two Cities", "text": "It was the best of times, it was the worst of times, wrote Dickens, opening his novel of London and Paris." },
    { "id": "en-006", "length": "short", "official": false, "quote": true, "source": "Jules Verne", "text": "Anything one man can imagine, other men can make real, wrote Jules Verne, the father of science fiction." },
    { "id": "en-007", "length": "short", "official": false, "quote": true, "source": "Maya Angelou", "text": "You will face many defeats in life, but never let yourself be defeated, wrote Maya Angelou in her letters." },
    { "id": "en-008", "length": "short", "official": false, "quote": true, "source": "Mark Twain", "text": "The secret of getting ahead is getting started, said Mark Twain; the secret of starting is breaking big tasks into small steps." },
    { "id": "en-009", "length": "short", "official": false, "text": "Octopuses have three hearts and blue blood, and two of those hearts stop beating whenever the animal swims." },
    { "id": "en-010", "length": "short", "official": false, "text": "The Great Wall of China stretches over thousands of miles of mountains, deserts and plains across the north." },
    { "id": "en-011", "length": "medium", "official": false, "text": "Touch typing means writing without looking at the keyboard. Each finger rests on a home row key and reaches only for the keys assigned to it. The method feels slow at first, but after a few weeks of daily practice, muscle memory takes over and speed climbs steadily. Accuracy always comes before speed." },
    { "id": "en-012", "length": "medium", "official": false, "text": "The printing press changed the world in a single generation. Books that once took months to copy by hand could suddenly be printed by the hundreds. Ideas spread faster than ever before, literacy rose, and the price of books fell. Many historians consider it the most important invention of the second millennium." },
    { "id": "en-013", "length": "medium", "official": false, "text": "Lighthouses have guided sailors for more than two thousand years. The Pharos of Alexandria, one of the seven wonders of the ancient world, stood over a hundred meters tall and burned fires visible for miles. Modern lighthouses use powerful electric lamps and precise lenses, but their mission remains exactly the same." },
    { "id": "en-014", "length": "medium", "official": false, "text": "Chess arrived in Europe from India through Persia more than a thousand years ago. Each piece moves according to simple rules, yet the number of possible games is greater than the number of atoms in the observable universe. Grandmasters train for decades to recognize patterns that beginners cannot even see." },
    { "id": "en-015", "length": "medium", "official": false, "text": "Sourdough bread requires nothing but flour, water, salt and time. The starter, a living culture of wild yeast and bacteria, must be fed every day like a pet. Bakers learn to read its smell and texture. The slow fermentation gives the bread its open crumb, deep flavor and a crust that crackles." },
    { "id": "en-016", "length": "medium", "official": false, "text": "Running is one of the simplest sports: put on shoes, step outside, and go. Coaches advise beginners to start slowly, mixing walking and jogging, and to increase distance by no more than ten percent per week. Most injuries come from doing too much too soon. Consistency, not intensity, builds the runner." },
    { "id": "en-017", "length": "medium", "official": false, "text": "The Amazon rainforest produces a significant share of the oxygen we breathe and hosts one in ten known species on Earth. Its rivers carry more water than the next seven largest rivers combined. Scientists estimate that millions of species living under its canopy have yet to be discovered and named." },
    { "id": "en-018", "length": "medium", "official": false, "text": "Learning a language rests on three pillars: vocabulary, grammar and daily practice. Fifteen minutes every day beats three hours on Sunday. Mistakes are not failures but stepping stones, and linguists agree that speaking imperfectly always beats staying silent perfectly. Regularity turns effort into instinct." },
    { "id": "en-019", "length": "medium", "official": false, "text": "Tea is the most consumed drink in the world after water. Born in China, it conquered Japan, India and finally Europe over the centuries. Green, black or smoked, every tea asks for its own water temperature and steeping time. In Japan, the tea ceremony turns its preparation into a precise art." },
    { "id": "en-020", "length": "medium", "official": false, "text": "A colony of bees may hold fifty thousand workers in summer, all serving a single queen. To tell their sisters where nectar lies, foragers perform a figure-eight dance that encodes direction and distance. Without their pollination, a large share of the fruits and vegetables we eat would simply disappear." },
    { "id": "en-021", "length": "medium", "official": false, "text": "The first bicycles had no pedals at all: riders pushed the ground with their feet. By the end of the nineteenth century, the chain drive and equal-sized wheels created the safety bicycle we know today. The bike gave millions of people, especially women, a new and revolutionary freedom of movement." },
    { "id": "en-022", "length": "medium", "official": false, "text": "The Mediterranean earned its name from the Romans, who saw it as the sea in the middle of the lands. Almost enclosed, it renews its waters slowly and stays fragile. Its floor hosts vast meadows of seagrass that store carbon and shelter young fish, nurseries on which much of its marine life depends." },
    { "id": "en-023", "length": "long", "official": false, "text": "The transcontinental railroad stitched the United States together in the nineteenth century. Before it, crossing the country took months of dangerous travel by wagon; after it, the journey shrank to about a week. Thousands of workers, many of them immigrants from China and Ireland, blasted tunnels through granite mountains and laid track across deserts and plains. The two companies racing from opposite coasts finally met at Promontory Summit, Utah, where a golden spike marked the moment. The railroad transformed the economy: mail, goods and people suddenly moved at unprecedented speed, towns sprang up along the line, and the vast interior of the continent was opened to settlement. Few engineering projects have ever reshaped a nation so completely and so quickly." },
    { "id": "en-024", "length": "long", "official": false, "text": "Sleep remains one of biology's great mysteries. Every night, the brain cycles through deep slow-wave sleep and the strange phase called REM, when dreams appear and the body lies almost paralyzed. During deep sleep, the brain consolidates the memories gathered through the day and flushes out metabolic waste. Studies show that chronic sleep loss erodes memory, mood and immune defenses alike. Yet our nights keep getting shorter, nibbled away by glowing screens and irregular schedules. Specialists recommend fixed hours, a cool dark room and a screen-free buffer before bed. Sleeping enough is not a luxury or a sign of laziness; it is a biological need as fundamental as food, and no amount of coffee can fully replace it." },
    { "id": "en-025", "length": "long", "official": false, "text": "Volcanoes fascinate as much as they frighten. Beneath the crust, magma gathers in chambers until the pressure grows too great, and the eruption hurls ash, gas and rivers of lava into the sky. Some eruptions shaped history: when Vesuvius buried Pompeii under ash, it froze a Roman city in time and gave archaeologists an unmatched window into daily life two thousand years ago. Others quietly build new landscapes, raising entire islands from the sea. Volcanoes are not only destructive; their soils rank among the most fertile on Earth, their hot springs provide energy, and their chemistry teaches us about the planet's interior. Modern science watches their tremors and swelling slopes, but predicting the exact hour of an eruption remains beyond our reach." },
    { "id": "en-026", "length": "long", "official": false, "text": "Silk shaped the history of trade between East and West for more than a thousand years. Spun by a humble moth caterpillar whose cocoon unwinds into a single filament hundreds of meters long, silk was so precious in China that revealing its secrets was punishable by death. Caravans crossed the deserts and mountains of Central Asia to carry it to the Mediterranean, lending their name to the famous Silk Road. Along with fabrics traveled religions, technologies and languages. Centuries later, the city of Lyon became Europe's silk capital, and its weavers built looms of growing ingenuity. The story of silk shows how an almost weightless fiber could move merchants across continents, found fortunes, and bind together civilizations that would otherwise never have met." },
    { "id": "en-027", "length": "long", "official": false, "text": "The bonsai is the art of growing miniature trees in pots, guiding their growth over years and sometimes generations. Born in China and refined in Japan, it demands a rare blend of patience, technique and artistic sense. The practitioner prunes roots and branches, wires twigs to bend their line, and chooses the pot with as much care as the tree itself. Despite a common belief, a bonsai is not a special species: almost any tree can be miniaturized, from maple to pine. The goal is not merely to keep a tree small but to suggest, within a few dozen centimeters, the strength of an ancient giant sculpted by wind and time. A fine bonsai tells a story of nature that humans accompany and shape but never truly command." },
    { "id": "en-028", "length": "long", "official": false, "text": "Photosynthesis is the process by which plants turn sunlight into chemical energy. In the leaves, chlorophyll captures photons and uses their power to assemble carbon dioxide and water into sugars, releasing oxygen as a byproduct. This mechanism, which first appeared in primitive bacteria billions of years ago, literally built the atmosphere of our planet and made life as we know it possible. Without it there would be no forests, no harvests, no food chains at all. Researchers still dream of copying the process artificially to produce clean fuels, yet nature keeps a comfortable lead: no technology rivals the quiet efficiency of a single green leaf tilted toward the sun. Every breath you take is, in a very real sense, a gift from photosynthesis." },
    { "id": "en-029", "length": "long", "official": false, "text": "The Tour de France began in nineteen hundred and three as a stunt to sell newspapers. The early editions were almost unimaginably brutal: stages longer than four hundred kilometers, heavy bicycles without gears, unpaved mountain roads, and riders repairing their own machines in the dark of night. Over the decades the race grew into a global spectacle with mythical climbs like the Galibier and Mont Ventoux, its distinctive jerseys and its legends written in sweat. Every July, millions of spectators line the roads of France just to watch the peloton flash past for a few seconds. The Tour remains at once an extreme athletic ordeal and a popular festival, woven so deeply into French culture that villages compete for the honor of hosting a stage start." },
    { "id": "en-030", "length": "long", "official": false, "text": "Versailles began as a modest hunting lodge built by Louis the Thirteenth in a marshy valley west of Paris. His son, Louis the Fourteenth, chose to make it the dazzling symbol of absolute power. For decades, architects, painters and gardeners transformed the site: the Hall of Mirrors dazzled ambassadors, the formal gardens drew perfect perspectives, and the entire court lived by a minute etiquette regulated by the king himself. Holding one's rank at Versailles was an art of every instant. The palace became the political heart of France until the Revolution, and courts across Europe copied its model. Today millions of visitors walk its gilded halls and gravelled alleys, witnesses of an age when architecture itself was a language of power, written in stone, gold and glass." },
    { "id": "en-101", "length": "short", "official": true, "text": "London, the capital of England, sits on the river Thames. The city has been a major settlement for two thousand years, since the Romans founded Londinium." },
    { "id": "en-102", "length": "short", "official": true, "text": "Spring returns at last: the days grow longer, the cherry trees blossom, and the first swallows trace swift circles in the warm evening sky." },
    { "id": "en-103", "length": "short", "official": true, "text": "In 1969, humans walked on the Moon for the first time. The Apollo 11 mission carried three astronauts across 384,000 kilometers of empty space." },
    { "id": "en-104", "length": "medium", "official": true, "text": "The marathon covers exactly 42.195 kilometers. The distance was set at the 1908 London Olympics, running from Windsor Castle to the stadium finish line. Since then, millions of runners have tested themselves against the mythic distance, from the streets of Boston to those of Tokyo. The world record now flirts with the two-hour mark, a barrier long thought unbreakable." },
    { "id": "en-105", "length": "medium", "official": true, "text": "Mount Everest rises 8,849 meters above sea level, the highest point on Earth. Edmund Hillary and Tenzing Norgay first reached its summit on May 29, 1953. More than 6,000 climbers have followed since, yet the mountain stays deadly: above 8,000 meters, the human body slowly shuts down." },
    { "id": "en-106", "length": "medium", "official": true, "text": "Electricity became indispensable in less than a century: lighting, heating, transport, communications... In the United States, about 60% of power still comes from fossil fuels, while nuclear, wind and solar gain ground every year. The challenge of the next decades is to produce more while emitting less CO2." },
    { "id": "en-107", "length": "medium", "official": true, "text": "Morse code, invented in the 1830s, assigns each letter a pattern of dots and dashes: the letter E is a single dot, and the famous SOS reads ... --- ... Telegraph operators and sailors used it for over a century, saving countless lives at sea. A few radio amateurs still practice it today, for nostalgia or sport." },
    { "id": "en-108", "length": "long", "official": true, "text": "On July 21, 1969, at 02:56 UTC, Neil Armstrong set foot on the Moon: \"That's one small step for man, one giant leap for mankind.\" Apollo 11 had launched 4 days earlier from Cape Canaveral, carrying 3 astronauts nearly 384,000 kilometers from Earth. Michael Collins remained in orbit while Armstrong and Aldrin descended to the Sea of Tranquility. They spent about 21 hours on the surface, planted a flag, collected 21.5 kg of rock samples, and lifted off to rejoin the command module. The Apollo program cost over $25 billion and employed as many as 400,000 people. Twelve men walked on the Moon between 1969 and 1972; no one has returned since." },
    { "id": "en-109", "length": "long", "official": true, "text": "The Internet grew out of an American military project: ARPANET, switched on in 1969, linked just 4 universities. Its principle was revolutionary — split messages into packets, route them along different paths, and reassemble them on arrival. In 1983, the TCP/IP protocol became the network's common tongue. But it was Tim Berners-Lee who, in 1989 at CERN, invented the Web: URLs, HTTP and HTML, the 3 building blocks that opened the network to everyone. The first website, published in 1991, simply explained how to build a website. Thirty years later, more than 5 billion people are online, and global traffic is measured in hundreds of exabytes — billions of billions of bytes exchanged every single year." },
    { "id": "en-110", "length": "long", "official": true, "text": "The Nobel Prize, created by Alfred Nobel's will in 1895, honors each year work conferring \"the greatest benefit to humankind\" in 6 fields: physics, chemistry, medicine, literature, peace and economics. The inventor of dynamite, holder of 355 patents, perhaps wished to redeem his legacy. The first ceremony took place in 1901; Marie Curie remains the only person to win 2 prizes in 2 different sciences (physics in 1903, chemistry in 1911). Each laureate receives a gold medal, a diploma and about 11 million Swedish kronor. Nominations stay secret for 50 years: there is no way to know who was shortlisted, how often, or why giants like Tolstoy, Joyce or Gandhi were never chosen." }
  ]
}
```

- [ ] **Step 3: Écrire le validateur**

`scripts/validateCorpus.mjs` :

```js
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const root = join(dirname(fileURLToPath(import.meta.url)), '..');
const RANGES = { short: [110, 200], medium: [320, 480], long: [650, 950] };
const errors = [];

const allIds = new Set();
for (const lang of ['fr', 'en']) {
  const file = join(root, 'src', 'texts', `${lang}.json`);
  const data = JSON.parse(readFileSync(file, 'utf8'));
  const texts = data.texts;
  const free = texts.filter((t) => !t.official);
  const official = texts.filter((t) => t.official);

  if (free.length !== 30) errors.push(`${lang}: ${free.length} textes libres (30 attendus)`);
  if (official.length !== 10) errors.push(`${lang}: ${official.length} textes officiels (10 attendus)`);
  if (free.filter((t) => t.quote).length !== 5) errors.push(`${lang}: il faut exactement 5 citations (quote: true)`);

  for (const t of texts) {
    if (!new RegExp(`^${lang}-\\d{3}$`).test(t.id)) errors.push(`${lang}: id invalide ${t.id}`);
    if (allIds.has(t.id)) errors.push(`${lang}: id dupliqué ${t.id}`);
    allIds.add(t.id);
    const [min, max] = RANGES[t.length] ?? [0, 0];
    if (!RANGES[t.length]) errors.push(`${t.id}: length invalide "${t.length}"`);
    else if (t.text.length < min || t.text.length > max) {
      errors.push(`${t.id}: ${t.text.length} caractères, hors fourchette ${t.length} [${min}-${max}]`);
    }
    if (t.official && !/[.,;:!?]/.test(t.text)) errors.push(`${t.id}: texte officiel sans ponctuation`);
  }

  const freeIds = free.map((t) => t.id).sort();
  const officialIds = official.map((t) => t.id).sort();
  if (freeIds[0] !== `${lang}-001` || freeIds[29] !== `${lang}-030`) {
    errors.push(`${lang}: les ids libres doivent couvrir 001-030`);
  }
  if (officialIds[0] !== `${lang}-101` || officialIds[9] !== `${lang}-110`) {
    errors.push(`${lang}: les ids officiels doivent couvrir 101-110`);
  }
}

if (errors.length > 0) {
  console.error('Corpus invalide :');
  for (const e of errors) console.error(`  - ${e}`);
  process.exit(1);
}
console.log('Corpus valide : 2 langues × (30 libres + 10 officiels).');
```

- [ ] **Step 4: Valider le corpus et corriger si besoin**

Run: `npm run validate:corpus`
Expected: « Corpus valide : 2 langues × (30 libres + 10 officiels). »
Si des textes sont hors fourchette de longueur : allonger ou raccourcir **uniquement les textes signalés** (en gardant du français/anglais naturel), puis relancer jusqu'au succès.

- [ ] **Step 5: Écrire le test du module corpus**

`src/texts/corpus.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import { CORPUS, getOfficialTexts, getTextById, getTexts, pickText } from './corpus';

describe('corpus', () => {
  it('contient 80 textes avec le champ language injecté', () => {
    expect(CORPUS).toHaveLength(80);
    expect(CORPUS.every((t) => t.language === 'fr' || t.language === 'en')).toBe(true);
  });

  it('getTexts retourne les 30 textes libres de la langue', () => {
    expect(getTexts('fr')).toHaveLength(30);
    expect(getTexts('fr').every((t) => !t.official && t.language === 'fr')).toBe(true);
  });

  it('getOfficialTexts retourne les 10 officiels triés par id', () => {
    const official = getOfficialTexts('en');
    expect(official).toHaveLength(10);
    expect(official[0].id).toBe('en-101');
    expect(official[9].id).toBe('en-110');
  });

  it('getTextById trouve un texte et throw sinon', () => {
    expect(getTextById('fr-001').language).toBe('fr');
    expect(() => getTextById('xx-999')).toThrow('xx-999');
  });

  it('pickText respecte la catégorie demandée', () => {
    expect(pickText('fr', 'short').length).toBe('short');
    expect(pickText('en', 'long').length).toBe('long');
    expect(pickText('fr', 'quote').quote).toBe(true);
  });
});
```

- [ ] **Step 6: Vérifier que le test échoue**

Run: `npx vitest run src/texts/corpus.test.ts`
Expected: FAIL (module introuvable)

- [ ] **Step 7: Implémenter `corpus.ts`**

`src/texts/corpus.ts` :

```ts
import frData from './fr.json';
import enData from './en.json';
import type { Language, TextEntry, TextLength } from './types';

interface RawEntry {
  id: string;
  length: TextLength;
  official: boolean;
  quote?: boolean;
  source?: string;
  text: string;
}

function load(language: Language, raw: { texts: RawEntry[] }): TextEntry[] {
  return raw.texts.map((t) => ({ ...t, language }));
}

export const CORPUS: TextEntry[] = [
  // les imports JSON sont typés `string` partout : on caste après validation au build
  ...load('fr', frData as unknown as { texts: RawEntry[] }),
  ...load('en', enData as unknown as { texts: RawEntry[] }),
];

export function getTexts(language: Language): TextEntry[] {
  return CORPUS.filter((t) => t.language === language && !t.official);
}

export function getOfficialTexts(language: Language): TextEntry[] {
  return CORPUS.filter((t) => t.language === language && t.official)
    .sort((a, b) => a.id.localeCompare(b.id));
}

export function getTextById(id: string): TextEntry {
  const entry = CORPUS.find((t) => t.id === id);
  if (!entry) throw new Error(`Texte inconnu : ${id}`);
  return entry;
}

export function pickText(language: Language, choice: TextLength | 'quote'): TextEntry {
  const pool = getTexts(language).filter((t) =>
    choice === 'quote' ? t.quote === true : t.length === choice,
  );
  if (pool.length === 0) throw new Error(`Aucun texte ${language}/${choice}`);
  return pool[Math.floor(Math.random() * pool.length)];
}
```

- [ ] **Step 8: Vérifier que les tests passent**

Run: `npx vitest run src/texts/`
Expected: tous les tests PASS

- [ ] **Step 9: Commit**

```bash
git add src/texts/ scripts/
git commit -m "feat(texts): fr/en corpus (40 texts each) with build-time validation"
```

---

### Task 6: Scoring — points Challenger et ligue

**Files:**
- Create: `src/scoring/points.ts`, `src/scoring/league.ts`
- Test: `src/scoring/points.test.ts`, `src/scoring/league.test.ts`

**Interfaces:**
- Consumes: `TextOptions` (tâche 4).
- Produces (consommé par tâches 7, 9, 13) :
  - `difficultyMultiplier(options: TextOptions): number` — `1 + 0.1 × nb de toggles actifs`
  - `challengerPoints(wpm: number, accuracy: number, options: TextOptions): number` — `Math.round(wpm × accuracy² × multiplicateur)`
  - `const TIERS: readonly ['bronze','argent','or','platine','diamant','challenger']`
  - `type Tier`
  - `TIER_THRESHOLDS: Record<Tier, number>` = bronze 100, argent 400, or 750, platine 950, diamant 1100, challenger 1300
  - `tierForPoints(total: number): Tier | null` — `null` si < 100
  - `tierRank(tier: Tier): number` — index dans TIERS
  - `nextTier(current: Tier | null): { tier: Tier; threshold: number } | null` — `null` si déjà challenger

- [ ] **Step 1: Écrire les tests qui échouent**

`src/scoring/points.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import { challengerPoints, difficultyMultiplier } from './points';
import { ALL_OPTIONS_ON } from '@/texts/normalize';

describe('points', () => {
  it('multiplicateur = 1 + 0.1 par toggle actif', () => {
    expect(difficultyMultiplier(ALL_OPTIONS_ON)).toBeCloseTo(1.4);
    expect(difficultyMultiplier({ punctuation: false, specialChars: false, digits: false, accents: false }))
      .toBeCloseTo(1);
    expect(difficultyMultiplier({ punctuation: true, specialChars: false, digits: true, accents: false }))
      .toBeCloseTo(1.2);
  });

  it('points = round(wpm × précision² × multiplicateur)', () => {
    // 60 WPM, 97%, x1.4 = 60 × 0.9409 × 1.4 = 79.03 -> 79
    expect(challengerPoints(60, 0.97, ALL_OPTIONS_ON)).toBe(79);
    // 100 WPM, 99%, x1.4 = 137.2 -> 137
    expect(challengerPoints(100, 0.99, ALL_OPTIONS_ON)).toBe(137);
    // sans toggles : 50 × 1 × 1 = 50
    expect(challengerPoints(50, 1, { punctuation: false, specialChars: false, digits: false, accents: false }))
      .toBe(50);
  });
});
```

`src/scoring/league.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import { nextTier, tierForPoints, tierRank, TIERS } from './league';

describe('league', () => {
  it('attribue le bon tier selon le total', () => {
    expect(tierForPoints(0)).toBeNull();
    expect(tierForPoints(99)).toBeNull();
    expect(tierForPoints(100)).toBe('bronze');
    expect(tierForPoints(400)).toBe('argent');
    expect(tierForPoints(750)).toBe('or');
    expect(tierForPoints(949)).toBe('or');
    expect(tierForPoints(950)).toBe('platine');
    expect(tierForPoints(1100)).toBe('diamant');
    expect(tierForPoints(1300)).toBe('challenger');
    expect(tierForPoints(9999)).toBe('challenger');
  });

  it('tierRank donne l\'ordre des tiers', () => {
    expect(tierRank('bronze')).toBe(0);
    expect(tierRank('challenger')).toBe(TIERS.length - 1);
  });

  it('nextTier donne le palier suivant ou null au max', () => {
    expect(nextTier(null)).toEqual({ tier: 'bronze', threshold: 100 });
    expect(nextTier('or')).toEqual({ tier: 'platine', threshold: 950 });
    expect(nextTier('challenger')).toBeNull();
  });
});
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run src/scoring/`
Expected: FAIL (modules introuvables)

- [ ] **Step 3: Implémenter**

`src/scoring/points.ts` :

```ts
import type { TextOptions } from '@/texts/types';

export function difficultyMultiplier(options: TextOptions): number {
  const active = [
    options.punctuation, options.specialChars, options.digits, options.accents,
  ].filter(Boolean).length;
  return 1 + 0.1 * active;
}

export function challengerPoints(wpm: number, accuracy: number, options: TextOptions): number {
  return Math.round(wpm * accuracy * accuracy * difficultyMultiplier(options));
}
```

`src/scoring/league.ts` :

```ts
export const TIERS = ['bronze', 'argent', 'or', 'platine', 'diamant', 'challenger'] as const;
export type Tier = (typeof TIERS)[number];

export const TIER_THRESHOLDS: Record<Tier, number> = {
  bronze: 100,
  argent: 400,
  or: 750,
  platine: 950,
  diamant: 1100,
  challenger: 1300,
};

export function tierRank(tier: Tier): number {
  return TIERS.indexOf(tier);
}

export function tierForPoints(total: number): Tier | null {
  let result: Tier | null = null;
  for (const tier of TIERS) {
    if (total >= TIER_THRESHOLDS[tier]) result = tier;
  }
  return result;
}

export function nextTier(current: Tier | null): { tier: Tier; threshold: number } | null {
  const next = current === null ? TIERS[0] : TIERS[tierRank(current) + 1];
  if (!next) return null;
  return { tier: next, threshold: TIER_THRESHOLDS[next] };
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/scoring/`
Expected: 5 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/scoring/
git commit -m "feat(scoring): challenger points formula and league tiers"
```

---

### Task 7: DB — couche Dexie, repositories, contrôle de persistance

**Files:**
- Create: `src/db/types.ts`, `src/db/schemas.ts`, `src/db/db.ts`, `src/db/persistence.ts`, `src/db/runsRepo.ts`, `src/db/profileRepo.ts`, `src/db/challengerRepo.ts`
- Test: `src/db/runsRepo.test.ts`, `src/db/challengerRepo.test.ts`, `src/db/profileRepo.test.ts`

**Interfaces:**
- Consumes: `Language`, `TextOptions` (tâche 4) ; `Tier`, `tierForPoints`, `tierRank` (tâche 6) ; `getOfficialTexts` (tâche 5).
- Produces (consommé par tâches 8, 9, 10, 12-17) :
  - `type GameMode = 'free' | 'challenger'`
  - `interface RunRecord { id?: number; date: number; mode: GameMode; language: Language; textId: string; options: TextOptions; durationMs: number; wpm: number; accuracy: number; points: number; errors: number; backspaces: number; chars: number; noBackspace: boolean }`
  - `interface Profile { id: 'default'; pseudo: string; theme: 'dark' | 'light'; sounds: boolean; defaultLanguage: Language; focusTimeoutSec: number; createdAt: number; lastActiveAt: number }`
  - `interface AchievementUnlock { id: string; unlockedAt: number }`
  - `interface TierChange { tier: Tier; at: number }`
  - `interface ChallengerProgress { language: Language; bestByText: Record<string, number>; total: number; tier: Tier | null; tierHistory: TierChange[] }`
  - `db: DactyDB` ; `checkPersistence(): Promise<boolean>`
  - `saveRun(run: RunRecord): Promise<number>`
  - `topRuns(filter: { mode: GameMode; language: Language; textId?: string }, limit?: number): Promise<RunRecord[]>` — tri : `points` desc en challenger, `wpm` desc en free (égalité : date asc)
  - `rankFor(filter: { mode: GameMode; language: Language; textId: string }, key: number): Promise<number>` — rang 1-based d'un score `key` (points ou wpm selon le mode)
  - `allRuns(): Promise<RunRecord[]>` ; `runCount(): Promise<number>`
  - `personalBests(): Promise<{ bestWpm: RunRecord | null; bestAccuracy: RunRecord | null; longestRun: RunRecord | null }>` — bestAccuracy parmi les runs ≥ 10 s
  - `DEFAULT_PROFILE: Profile` ; `getProfile(): Promise<Profile>` (crée le défaut si absent) ; `updateProfile(patch: Partial<Profile>): Promise<void>`
  - `getProgress(language: Language): Promise<ChallengerProgress>` (défaut vide si absent)
  - `recordChallengerResult(language: Language, textId: string, points: number, now: number): Promise<{ progress: ChallengerProgress; tierUp: Tier | null }>`
  - Lecture DB validée par zod : donnée corrompue → valeur par défaut (jamais de crash).

- [ ] **Step 1: Écrire les tests qui échouent**

`src/db/runsRepo.test.ts` :

```ts
import 'fake-indexeddb/auto';
import { beforeEach, describe, expect, it } from 'vitest';
import { db } from './db';
import { allRuns, personalBests, rankFor, runCount, saveRun, topRuns } from './runsRepo';
import type { RunRecord } from './types';

const opts = { punctuation: true, specialChars: true, digits: true, accents: true };

function makeRun(patch: Partial<RunRecord>): RunRecord {
  return {
    date: 1000, mode: 'free', language: 'fr', textId: 'fr-001', options: opts,
    durationMs: 30_000, wpm: 50, accuracy: 0.97, points: 66, errors: 3,
    backspaces: 2, chars: 250, noBackspace: false, ...patch,
  };
}

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
});

describe('runsRepo', () => {
  it('sauvegarde et compte les runs', async () => {
    await saveRun(makeRun({}));
    await saveRun(makeRun({ date: 2000 }));
    expect(await runCount()).toBe(2);
    expect((await allRuns()).map((r) => r.date).sort()).toEqual([1000, 2000]);
  });

  it('topRuns trie par wpm desc en free, points desc en challenger', async () => {
    await saveRun(makeRun({ wpm: 40 }));
    await saveRun(makeRun({ wpm: 60 }));
    await saveRun(makeRun({ wpm: 50 }));
    const top = await topRuns({ mode: 'free', language: 'fr' });
    expect(top.map((r) => r.wpm)).toEqual([60, 50, 40]);

    await saveRun(makeRun({ mode: 'challenger', textId: 'fr-101', points: 80, wpm: 45 }));
    await saveRun(makeRun({ mode: 'challenger', textId: 'fr-101', points: 90, wpm: 44 }));
    const topC = await topRuns({ mode: 'challenger', language: 'fr', textId: 'fr-101' });
    expect(topC.map((r) => r.points)).toEqual([90, 80]);
  });

  it('topRuns respecte la limite et le filtre texte/langue', async () => {
    for (let i = 0; i < 12; i++) await saveRun(makeRun({ wpm: 30 + i, date: i }));
    await saveRun(makeRun({ language: 'en', textId: 'en-001', wpm: 99 }));
    expect(await topRuns({ mode: 'free', language: 'fr' }, 10)).toHaveLength(10);
    const en = await topRuns({ mode: 'free', language: 'en' });
    expect(en).toHaveLength(1);
    expect(en[0].wpm).toBe(99);
  });

  it('rankFor donne le rang 1-based d\'un score', async () => {
    await saveRun(makeRun({ wpm: 60 }));
    await saveRun(makeRun({ wpm: 50 }));
    expect(await rankFor({ mode: 'free', language: 'fr', textId: 'fr-001' }, 55)).toBe(2);
    expect(await rankFor({ mode: 'free', language: 'fr', textId: 'fr-001' }, 70)).toBe(1);
  });

  it('personalBests ignore la précision des runs < 10 s', async () => {
    await saveRun(makeRun({ wpm: 55, accuracy: 0.9, chars: 300, durationMs: 30_000 }));
    await saveRun(makeRun({ wpm: 70, accuracy: 1, chars: 100, durationMs: 5_000 }));
    const bests = await personalBests();
    expect(bests.bestWpm?.wpm).toBe(70);
    expect(bests.bestAccuracy?.accuracy).toBe(0.9);
    expect(bests.longestRun?.chars).toBe(300);
  });
});
```

`src/db/challengerRepo.test.ts` :

```ts
import 'fake-indexeddb/auto';
import { beforeEach, describe, expect, it } from 'vitest';
import { db } from './db';
import { getProgress, recordChallengerResult } from './challengerRepo';

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
});

describe('challengerRepo', () => {
  it('retourne une progression vide par défaut', async () => {
    const p = await getProgress('fr');
    expect(p).toEqual({ language: 'fr', bestByText: {}, total: 0, tier: null, tierHistory: [] });
  });

  it('garde le meilleur score par texte et calcule le total', async () => {
    await recordChallengerResult('fr', 'fr-101', 50, 1000);
    const r = await recordChallengerResult('fr', 'fr-101', 40, 2000); // moins bien
    expect(r.progress.bestByText['fr-101']).toBe(50);
    const r2 = await recordChallengerResult('fr', 'fr-102', 60, 3000);
    expect(r2.progress.total).toBe(110);
    expect(r2.progress.tier).toBe('bronze');
  });

  it('détecte la montée de tier et l\'historise', async () => {
    await recordChallengerResult('fr', 'fr-101', 50, 1000);
    const r = await recordChallengerResult('fr', 'fr-102', 60, 3000); // total 110 -> bronze
    expect(r.tierUp).toBe('bronze');
    expect(r.progress.tierHistory).toEqual([{ tier: 'bronze', at: 3000 }]);
    // re-run sans changement de tier
    const r2 = await recordChallengerResult('fr', 'fr-103', 80, 4000); // total 190
    expect(r2.tierUp).toBeNull();
    expect(r2.progress.tierHistory).toHaveLength(1);
  });
});
```

`src/db/profileRepo.test.ts` :

```ts
import 'fake-indexeddb/auto';
import { beforeEach, describe, expect, it } from 'vitest';
import { db } from './db';
import { DEFAULT_PROFILE, getProfile, updateProfile } from './profileRepo';

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
});

describe('profileRepo', () => {
  it('crée le profil par défaut au premier accès', async () => {
    const p = await getProfile();
    expect(p.pseudo).toBe(DEFAULT_PROFILE.pseudo);
    expect(p.theme).toBe('dark');
    expect(await db.profile.count()).toBe(1);
  });

  it('met à jour partiellement le profil', async () => {
    await updateProfile({ pseudo: 'Ludo', theme: 'light' });
    const p = await getProfile();
    expect(p.pseudo).toBe('Ludo');
    expect(p.theme).toBe('light');
    expect(p.sounds).toBe(true);
  });

  it('ignore une donnée corrompue au lieu de planter', async () => {
    await db.profile.put({ id: 'default', pseudo: 42 } as never);
    const p = await getProfile();
    expect(p.pseudo).toBe(DEFAULT_PROFILE.pseudo);
  });
});
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run src/db/`
Expected: FAIL (modules introuvables)

- [ ] **Step 3: Implémenter**

`src/db/types.ts` :

```ts
import type { Language, TextOptions } from '@/texts/types';
import type { Tier } from '@/scoring/league';

export type GameMode = 'free' | 'challenger';

export interface RunRecord {
  id?: number;
  date: number;
  mode: GameMode;
  language: Language;
  textId: string;
  options: TextOptions;
  durationMs: number;
  wpm: number;
  accuracy: number;
  points: number;
  errors: number;
  backspaces: number;
  /** nombre de caractères du texte tapé */
  chars: number;
  noBackspace: boolean;
}

export interface Profile {
  id: 'default';
  pseudo: string;
  theme: 'dark' | 'light';
  sounds: boolean;
  defaultLanguage: Language;
  focusTimeoutSec: number;
  createdAt: number;
  lastActiveAt: number;
}

export interface AchievementUnlock {
  id: string;
  unlockedAt: number;
}

export interface TierChange {
  tier: Tier;
  at: number;
}

export interface ChallengerProgress {
  language: Language;
  bestByText: Record<string, number>;
  total: number;
  tier: Tier | null;
  tierHistory: TierChange[];
}
```

`src/db/db.ts` :

```ts
import Dexie, { type EntityTable } from 'dexie';
import type { AchievementUnlock, ChallengerProgress, Profile, RunRecord } from './types';

export class DactyDB extends Dexie {
  profile!: EntityTable<Profile, 'id'>;
  runs!: EntityTable<RunRecord, 'id'>;
  achievements!: EntityTable<AchievementUnlock, 'id'>;
  challenger!: EntityTable<ChallengerProgress, 'language'>;

  constructor() {
    super('dacty');
    this.version(1).stores({
      profile: 'id',
      runs: '++id, date, mode, language, textId, [mode+language+textId], wpm',
      achievements: 'id, unlockedAt',
      challenger: 'language',
    });
  }
}

export const db = new DactyDB();
```

`src/db/schemas.ts` :

```ts
import { z } from 'zod';

const languageSchema = z.enum(['fr', 'en']);
const tierSchema = z.enum(['bronze', 'argent', 'or', 'platine', 'diamant', 'challenger']);

export const profileSchema = z.object({
  id: z.literal('default'),
  pseudo: z.string().min(1).max(30),
  theme: z.enum(['dark', 'light']),
  sounds: z.boolean(),
  defaultLanguage: languageSchema,
  focusTimeoutSec: z.number().min(1).max(60),
  createdAt: z.number(),
  lastActiveAt: z.number(),
});

export const challengerProgressSchema = z.object({
  language: languageSchema,
  bestByText: z.record(z.string(), z.number()),
  total: z.number(),
  tier: tierSchema.nullable(),
  tierHistory: z.array(z.object({ tier: tierSchema, at: z.number() })),
});
```

`src/db/persistence.ts` :

```ts
import { db } from './db';

/** Vérifie qu'IndexedDB est réellement utilisable (false en navigation privée bloquée). */
export async function checkPersistence(): Promise<boolean> {
  try {
    await db.profile.limit(1).toArray();
    return true;
  } catch {
    return false;
  }
}
```

`src/db/runsRepo.ts` :

```ts
import { db } from './db';
import type { GameMode, RunRecord } from './types';
import type { Language } from '@/texts/types';

export async function saveRun(run: RunRecord): Promise<number> {
  return db.runs.add(run);
}

const keyOf = (mode: GameMode, r: RunRecord): number => (mode === 'challenger' ? r.points : r.wpm);

export async function topRuns(
  filter: { mode: GameMode; language: Language; textId?: string },
  limit = 10,
): Promise<RunRecord[]> {
  const runs = await db.runs
    .where(filter.textId ? '[mode+language+textId]' : 'mode')
    .equals(filter.textId ? [filter.mode, filter.language, filter.textId] : filter.mode)
    .toArray();
  return runs
    .filter((r) => r.language === filter.language && (!filter.textId || r.textId === filter.textId))
    .sort((a, b) => keyOf(filter.mode, b) - keyOf(filter.mode, a) || a.date - b.date)
    .slice(0, limit);
}

export async function rankFor(
  filter: { mode: GameMode; language: Language; textId: string },
  key: number,
): Promise<number> {
  const better = await db.runs
    .where('[mode+language+textId]')
    .equals([filter.mode, filter.language, filter.textId])
    .filter((r) => keyOf(filter.mode, r) > key)
    .count();
  return better + 1;
}

export function allRuns(): Promise<RunRecord[]> {
  return db.runs.toArray();
}

export function runCount(): Promise<number> {
  return db.runs.count();
}

export async function personalBests(): Promise<{
  bestWpm: RunRecord | null;
  bestAccuracy: RunRecord | null;
  longestRun: RunRecord | null;
}> {
  const runs = await db.runs.toArray();
  const max = (list: RunRecord[], key: (r: RunRecord) => number): RunRecord | null =>
    list.length === 0 ? null : list.reduce((a, b) => (key(b) > key(a) ? b : a));
  return {
    bestWpm: max(runs, (r) => r.wpm),
    bestAccuracy: max(runs.filter((r) => r.durationMs >= 10_000), (r) => r.accuracy),
    longestRun: max(runs, (r) => r.chars),
  };
}
```

`src/db/profileRepo.ts` :

```ts
import { db } from './db';
import { profileSchema } from './schemas';
import type { Profile } from './types';

export const DEFAULT_PROFILE: Profile = {
  id: 'default',
  pseudo: 'Joueur',
  theme: 'dark',
  sounds: true,
  defaultLanguage: 'fr',
  focusTimeoutSec: 5,
  createdAt: 0,
  lastActiveAt: 0,
};

export async function getProfile(): Promise<Profile> {
  const raw = await db.profile.get('default');
  if (raw) {
    const parsed = profileSchema.safeParse(raw);
    if (parsed.success) return parsed.data;
    // donnée corrompue : on réinitialise sans planter
  }
  const now = Date.now();
  const fresh: Profile = { ...DEFAULT_PROFILE, createdAt: now, lastActiveAt: now };
  await db.profile.put(fresh);
  return fresh;
}

export async function updateProfile(patch: Partial<Profile>): Promise<void> {
  const current = await getProfile();
  await db.profile.put({ ...current, ...patch, id: 'default', lastActiveAt: Date.now() });
}
```

`src/db/challengerRepo.ts` :

```ts
import { db } from './db';
import { challengerProgressSchema } from './schemas';
import type { ChallengerProgress } from './types';
import type { Language } from '@/texts/types';
import { getOfficialTexts } from '@/texts/corpus';
import { tierForPoints, tierRank, type Tier } from '@/scoring/league';

const empty = (language: Language): ChallengerProgress => ({
  language, bestByText: {}, total: 0, tier: null, tierHistory: [],
});

export async function getProgress(language: Language): Promise<ChallengerProgress> {
  const raw = await db.challenger.get(language);
  if (!raw) return empty(language);
  const parsed = challengerProgressSchema.safeParse(raw);
  return parsed.success ? parsed.data : empty(language);
}

export async function recordChallengerResult(
  language: Language,
  textId: string,
  points: number,
  now: number,
): Promise<{ progress: ChallengerProgress; tierUp: Tier | null }> {
  const current = await getProgress(language);
  const bestByText = {
    ...current.bestByText,
    [textId]: Math.max(current.bestByText[textId] ?? 0, points),
  };
  const officialIds = new Set(getOfficialTexts(language).map((t) => t.id));
  const total = Object.entries(bestByText)
    .filter(([id]) => officialIds.has(id))
    .reduce((sum, [, p]) => sum + p, 0);
  const tier = tierForPoints(total);
  const tierUp = tier !== null && (current.tier === null || tierRank(tier) > tierRank(current.tier))
    ? tier
    : null;
  const tierHistory = tierUp ? [...current.tierHistory, { tier, at: now }] : current.tierHistory;
  const progress: ChallengerProgress = { language, bestByText, total, tier, tierHistory };
  await db.challenger.put(progress);
  return { progress, tierUp };
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/db/`
Expected: 10 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/db/
git commit -m "feat(db): dexie layer with runs/profile/challenger repositories"
```

---

### Task 8: Achievements — règles déclaratives et moteur de déblocage

**Files:**
- Create: `src/achievements/definitions.ts`, `src/achievements/check.ts`
- Test: `src/achievements/definitions.test.ts`, `src/achievements/check.test.ts`

**Interfaces:**
- Consumes: `RunRecord`, `ChallengerProgress`, `getProgress`, `allRuns`, `db` (tâche 7) ; `tierRank`, `Tier` (tâche 6).
- Produces (consommé par tâches 9, 15) :
  - `interface AchievementContext { newRun: RunRecord; runs: RunRecord[]; totalChars: number; streakDays: number; progress: Record<Language, ChallengerProgress>; now: number }`
  - `type AchievementCategory = 'vitesse' | 'precision' | 'volume' | 'challenger' | 'fun'`
  - `interface AchievementDef { id: string; category: AchievementCategory; title: string; description: string; target?: number; isUnlocked(ctx: AchievementContext): boolean; progress?(ctx: AchievementContext): number }`
  - `ACHIEVEMENTS: AchievementDef[]` — 25 succès, ids stables (liste ci-dessous)
  - `buildContext(newRun: RunRecord): Promise<AchievementContext>`
  - `unlockNew(ctx: AchievementContext): Promise<AchievementDef[]>` — persiste les nouveaux en DB, retourne uniquement les nouveaux
  - `computeStreak(dates: number[], now: number): number`

Liste des 25 succès (id / titre / règle / target) :
- `wpm-40` « Échauffement » / `wpm-60` « Doigts agiles » / `wpm-80` « Machine » / `wpm-100` « Cent à l'heure » / `wpm-120` « Supersonique » / `wpm-140` « Inhumain » : meilleur WPM ≥ seuil (progress = meilleur WPM arrondi, target = seuil)
- `perfect-run` « Sans faute » : une run ≥ 50 car. à 100 % (progress = meilleure précision ×100, target 100)
- `sharpshooter-10` « Précision chirurgicale » : 10 runs ≥ 98 % / `sharpshooter-50` « Œil de lynx » : 50 runs ≥ 98 %
- `runs-10` « Premiers pas » / `runs-50` « Habitué » / `runs-100` « Centurion » / `runs-500` « Marathonien des touches » : nombre de runs (progress = nb runs)
- `chars-100k` « Cent mille signes » : 100 000 caractères tapés / `chars-1m` « Le million » : 1 000 000 (progress = totalChars)
- `enter-league` « En lice » : un tier dans une langue / `gold-any` « En or » : tier ≥ or / `diamond-any` « Diamanté » : tier ≥ diamant / `challenger-any` « Challenger » : tier challenger / `gold-both` « Bilingue d'or » : tier ≥ or dans fr ET en
- `no-backspace` « Droit au but » : run ≥ 30 car. sans backspace / `night-owl` « Oiseau de nuit » : run terminée entre 3h et 4h du matin / `streak-7` « Une semaine de feu » : 7 jours d'affilée (progress = streakDays, target 7) / `marathon` « Fond de course » : une run ≥ 800 car. / `polyglotte` « Polyglotte » : une run fr ET une run en le même jour

- [ ] **Step 1: Écrire les tests qui échouent**

`src/achievements/definitions.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import { ACHIEVEMENTS, type AchievementContext } from './definitions';
import type { RunRecord } from '@/db/types';
import type { Language } from '@/texts/types';

const opts = { punctuation: true, specialChars: true, digits: true, accents: true };

function run(patch: Partial<RunRecord>): RunRecord {
  return {
    date: 1_000_000, mode: 'free', language: 'fr', textId: 'fr-001', options: opts,
    durationMs: 30_000, wpm: 50, accuracy: 0.97, points: 66, errors: 3,
    backspaces: 1, chars: 250, noBackspace: false, ...patch,
  };
}

const emptyProgress = (language: Language) => ({
  language, bestByText: {}, total: 0, tier: null, tierHistory: [],
});

function ctx(patch: Partial<AchievementContext>): AchievementContext {
  const r = run({});
  return {
    newRun: r, runs: [r], totalChars: 250, streakDays: 1,
    progress: { fr: emptyProgress('fr'), en: emptyProgress('en') },
    now: 1_000_000, ...patch,
  };
}

const def = (id: string) => {
  const d = ACHIEVEMENTS.find((a) => a.id === id);
  if (!d) throw new Error(`succès inconnu: ${id}`);
  return d;
};

describe('definitions', () => {
  it('contient 25 succès aux ids uniques', () => {
    expect(ACHIEVEMENTS).toHaveLength(25);
    expect(new Set(ACHIEVEMENTS.map((a) => a.id)).size).toBe(25);
  });

  it('wpm-60 se débloque à 60 WPM', () => {
    expect(def('wpm-60').isUnlocked(ctx({ newRun: run({ wpm: 59 }) }))).toBe(false);
    expect(def('wpm-60').isUnlocked(ctx({ newRun: run({ wpm: 60 }) }))).toBe(true);
  });

  it('perfect-run exige 100% sur au moins 50 caractères', () => {
    expect(def('perfect-run').isUnlocked(ctx({ newRun: run({ accuracy: 1, chars: 30 }) }))).toBe(false);
    expect(def('perfect-run').isUnlocked(ctx({ newRun: run({ accuracy: 1, chars: 50 }) }))).toBe(true);
  });

  it('sharpshooter-10 compte les runs à 98% ou plus', () => {
    const runs = Array.from({ length: 9 }, () => run({ accuracy: 0.99 }));
    expect(def('sharpshooter-10').isUnlocked(ctx({ runs }))).toBe(false);
    expect(def('sharpshooter-10').isUnlocked(ctx({ runs: [...runs, run({ accuracy: 0.98 })] }))).toBe(true);
  });

  it('runs-10 compte le volume de runs', () => {
    expect(def('runs-10').isUnlocked(ctx({ runs: Array.from({ length: 10 }, () => run({})) }))).toBe(true);
  });

  it('chars-100k utilise le total de caractères', () => {
    expect(def('chars-100k').isUnlocked(ctx({ totalChars: 99_999 }))).toBe(false);
    expect(def('chars-100k').isUnlocked(ctx({ totalChars: 100_000 }))).toBe(true);
  });

  it('gold-any et gold-both lisent les tiers des deux langues', () => {
    const gold = { ...emptyProgress('fr' as Language), tier: 'or' as const };
    expect(def('gold-any').isUnlocked(ctx({ progress: { fr: gold, en: emptyProgress('en') } }))).toBe(true);
    expect(def('gold-both').isUnlocked(ctx({ progress: { fr: gold, en: emptyProgress('en') } }))).toBe(false);
    expect(def('gold-both').isUnlocked(ctx({ progress: { fr: gold, en: { ...gold, language: 'en' as Language } } }))).toBe(true);
  });

  it('no-backspace exige zéro backspace sur au moins 30 caractères', () => {
    expect(def('no-backspace').isUnlocked(ctx({ newRun: run({ backspaces: 0, chars: 30 }) }))).toBe(true);
    expect(def('no-backspace').isUnlocked(ctx({ newRun: run({ backspaces: 0, chars: 29 }) }))).toBe(false);
  });

  it('night-owl vérifie l\'heure locale de fin de run', () => {
    const at3am = new Date(2026, 5, 15, 3, 30).getTime();
    const atNoon = new Date(2026, 5, 15, 12, 0).getTime();
    expect(def('night-owl').isUnlocked(ctx({ newRun: run({ date: at3am }) }))).toBe(true);
    expect(def('night-owl').isUnlocked(ctx({ newRun: run({ date: atNoon }) }))).toBe(false);
  });

  it('polyglotte exige fr + en le même jour', () => {
    const day = new Date(2026, 5, 15, 10, 0).getTime();
    const runs = [run({ date: day, language: 'fr' }), run({ date: day, language: 'en' })];
    expect(def('polyglotte').isUnlocked(ctx({ runs, newRun: runs[1] }))).toBe(true);
    expect(def('polyglotte').isUnlocked(ctx({ runs: [runs[0]], newRun: runs[0] }))).toBe(false);
  });

  it('marathon exige une run d\'au moins 800 caractères', () => {
    expect(def('marathon').isUnlocked(ctx({ newRun: run({ chars: 799 }) }))).toBe(false);
    expect(def('marathon').isUnlocked(ctx({ newRun: run({ chars: 800 }) }))).toBe(true);
  });
});
```

`src/achievements/check.test.ts` :

```ts
import 'fake-indexeddb/auto';
import { beforeEach, describe, expect, it } from 'vitest';
import { db } from '@/db/db';
import { buildContext, computeStreak, unlockNew } from './check';
import type { RunRecord } from '@/db/types';

const opts = { punctuation: true, specialChars: true, digits: true, accents: true };

function run(patch: Partial<RunRecord>): RunRecord {
  return {
    date: Date.now(), mode: 'free', language: 'fr', textId: 'fr-001', options: opts,
    durationMs: 30_000, wpm: 60, accuracy: 0.97, points: 79, errors: 3,
    backspaces: 1, chars: 250, noBackspace: false, ...patch,
  };
}

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
});

describe('computeStreak', () => {
  const day = (offset: number) => {
    const d = new Date();
    d.setHours(12, 0, 0, 0);
    return d.getTime() - offset * 86_400_000;
  };

  it('compte les jours consécutifs en remontant depuis aujourd\'hui', () => {
    expect(computeStreak([day(0), day(1), day(2), day(4)], Date.now())).toBe(3);
    expect(computeStreak([day(1), day(2)], Date.now())).toBe(2); // streak active sans run aujourd'hui
    expect(computeStreak([day(5)], Date.now())).toBe(0);
    expect(computeStreak([], Date.now())).toBe(0);
  });

  it('ne compte qu\'une fois plusieurs runs le même jour', () => {
    expect(computeStreak([day(0), day(0), day(1)], Date.now())).toBe(2);
  });
});

describe('unlockNew', () => {
  it('débloque une seule fois, persiste en DB', async () => {
    const newRun = run({ wpm: 65 });
    const ctx1 = await buildContext(newRun);
    const first = await unlockNew(ctx1);
    expect(first.map((a) => a.id)).toContain('wpm-60');
    expect(first.map((a) => a.id)).not.toContain('wpm-80');

    const ctx2 = await buildContext(run({ wpm: 66 }));
    const second = await unlockNew(ctx2);
    expect(second.map((a) => a.id)).not.toContain('wpm-60'); // déjà débloqué
    expect(await db.achievements.count()).toBe(first.length);
  });
});
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run src/achievements/`
Expected: FAIL (modules introuvables)

- [ ] **Step 3: Implémenter les définitions**

`src/achievements/definitions.ts` :

```ts
import type { ChallengerProgress, RunRecord } from '@/db/types';
import { tierRank, type Tier } from '@/scoring/league';
import type { Language } from '@/texts/types';

export interface AchievementContext {
  newRun: RunRecord;
  runs: RunRecord[];
  totalChars: number;
  streakDays: number;
  progress: Record<Language, ChallengerProgress>;
  now: number;
}

export type AchievementCategory = 'vitesse' | 'precision' | 'volume' | 'challenger' | 'fun';

export interface AchievementDef {
  id: string;
  category: AchievementCategory;
  title: string;
  description: string;
  target?: number;
  isUnlocked(ctx: AchievementContext): boolean;
  progress?(ctx: AchievementContext): number;
}

const bestWpm = (ctx: AchievementContext): number =>
  Math.round(Math.max(0, ...ctx.runs.map((r) => r.wpm)));
const bestAccuracyPct = (ctx: AchievementContext): number =>
  Math.round(100 * Math.max(0, ...ctx.runs.map((r) => r.accuracy)));
const tierAtLeast = (p: ChallengerProgress, tier: Tier): boolean =>
  p.tier !== null && tierRank(p.tier) >= tierRank(tier);

const wpmAchievement = (wpm: number, title: string): AchievementDef => ({
  id: `wpm-${wpm}`,
  category: 'vitesse',
  title,
  description: `Atteindre ${wpm} WPM sur une run`,
  target: wpm,
  isUnlocked: (ctx) => ctx.newRun.wpm >= wpm,
  progress: bestWpm,
});

export const ACHIEVEMENTS: AchievementDef[] = [
  wpmAchievement(40, 'Échauffement'),
  wpmAchievement(60, 'Doigts agiles'),
  wpmAchievement(80, 'Machine'),
  wpmAchievement(100, "Cent à l'heure"),
  wpmAchievement(120, 'Supersonique'),
  wpmAchievement(140, 'Inhumain'),
  {
    id: 'perfect-run', category: 'precision', title: 'Sans faute',
    description: 'Terminer une run d\'au moins 50 caractères à 100 % de précision',
    target: 100,
    isUnlocked: (ctx) => ctx.newRun.accuracy === 1 && ctx.newRun.chars >= 50,
    progress: bestAccuracyPct,
  },
  {
    id: 'sharpshooter-10', category: 'precision', title: 'Précision chirurgicale',
    description: 'Réussir 10 runs à 98 % de précision ou plus',
    target: 10,
    isUnlocked: (ctx) => ctx.runs.filter((r) => r.accuracy >= 0.98).length >= 10,
    progress: (ctx) => ctx.runs.filter((r) => r.accuracy >= 0.98).length,
  },
  {
    id: 'sharpshooter-50', category: 'precision', title: 'Œil de lynx',
    description: 'Réussir 50 runs à 98 % de précision ou plus',
    target: 50,
    isUnlocked: (ctx) => ctx.runs.filter((r) => r.accuracy >= 0.98).length >= 50,
    progress: (ctx) => ctx.runs.filter((r) => r.accuracy >= 0.98).length,
  },
  ...([10, 50, 100, 500] as const).map((n): AchievementDef => ({
    id: `runs-${n}`,
    category: 'volume',
    title: { 10: 'Premiers pas', 50: 'Habitué', 100: 'Centurion', 500: 'Marathonien des touches' }[n],
    description: `Terminer ${n} runs`,
    target: n,
    isUnlocked: (ctx) => ctx.runs.length >= n,
    progress: (ctx) => ctx.runs.length,
  })),
  {
    id: 'chars-100k', category: 'volume', title: 'Cent mille signes',
    description: 'Taper 100 000 caractères au total',
    target: 100_000,
    isUnlocked: (ctx) => ctx.totalChars >= 100_000,
    progress: (ctx) => ctx.totalChars,
  },
  {
    id: 'chars-1m', category: 'volume', title: 'Le million',
    description: 'Taper 1 000 000 de caractères au total',
    target: 1_000_000,
    isUnlocked: (ctx) => ctx.totalChars >= 1_000_000,
    progress: (ctx) => ctx.totalChars,
  },
  {
    id: 'enter-league', category: 'challenger', title: 'En lice',
    description: 'Atteindre le tier Bronze dans une langue',
    isUnlocked: (ctx) => ctx.progress.fr.tier !== null || ctx.progress.en.tier !== null,
  },
  {
    id: 'gold-any', category: 'challenger', title: 'En or',
    description: 'Atteindre le tier Or dans une langue',
    isUnlocked: (ctx) => tierAtLeast(ctx.progress.fr, 'or') || tierAtLeast(ctx.progress.en, 'or'),
  },
  {
    id: 'diamond-any', category: 'challenger', title: 'Diamanté',
    description: 'Atteindre le tier Diamant dans une langue',
    isUnlocked: (ctx) => tierAtLeast(ctx.progress.fr, 'diamant') || tierAtLeast(ctx.progress.en, 'diamant'),
  },
  {
    id: 'challenger-any', category: 'challenger', title: 'Challenger',
    description: 'Atteindre le tier Challenger dans une langue',
    isUnlocked: (ctx) => tierAtLeast(ctx.progress.fr, 'challenger') || tierAtLeast(ctx.progress.en, 'challenger'),
  },
  {
    id: 'gold-both', category: 'challenger', title: 'Bilingue d\'or',
    description: 'Atteindre le tier Or en français ET en anglais',
    isUnlocked: (ctx) => tierAtLeast(ctx.progress.fr, 'or') && tierAtLeast(ctx.progress.en, 'or'),
  },
  {
    id: 'no-backspace', category: 'fun', title: 'Droit au but',
    description: 'Terminer une run d\'au moins 30 caractères sans utiliser backspace',
    isUnlocked: (ctx) => ctx.newRun.backspaces === 0 && ctx.newRun.chars >= 30,
  },
  {
    id: 'night-owl', category: 'fun', title: 'Oiseau de nuit',
    description: 'Terminer une run entre 3h et 4h du matin',
    isUnlocked: (ctx) => new Date(ctx.newRun.date).getHours() === 3,
  },
  {
    id: 'streak-7', category: 'fun', title: 'Une semaine de feu',
    description: 'Jouer 7 jours d\'affilée',
    target: 7,
    isUnlocked: (ctx) => ctx.streakDays >= 7,
    progress: (ctx) => ctx.streakDays,
  },
  {
    id: 'marathon', category: 'fun', title: 'Fond de course',
    description: 'Terminer une run d\'au moins 800 caractères',
    isUnlocked: (ctx) => ctx.newRun.chars >= 800,
  },
  {
    id: 'polyglotte', category: 'fun', title: 'Polyglotte',
    description: 'Terminer une run en français et une en anglais le même jour',
    isUnlocked: (ctx) => {
      const day = new Date(ctx.newRun.date).toDateString();
      const sameDay = ctx.runs.filter((r) => new Date(r.date).toDateString() === day);
      return sameDay.some((r) => r.language === 'fr') && sameDay.some((r) => r.language === 'en');
    },
  },
];
```

- [ ] **Step 4: Implémenter le moteur de déblocage**

`src/achievements/check.ts` :

```ts
import { db } from '@/db/db';
import { allRuns } from '@/db/runsRepo';
import { getProgress } from '@/db/challengerRepo';
import type { RunRecord } from '@/db/types';
import {
  ACHIEVEMENTS, type AchievementContext, type AchievementDef,
} from './definitions';

const dayKey = (ts: number): string => new Date(ts).toDateString();

/** Jours consécutifs de jeu, la série pouvant se terminer aujourd'hui ou hier. */
export function computeStreak(dates: number[], now: number): number {
  const days = new Set(dates.map(dayKey));
  const cursor = new Date(now);
  cursor.setHours(12, 0, 0, 0);
  if (!days.has(cursor.toDateString())) cursor.setTime(cursor.getTime() - 86_400_000);
  let streak = 0;
  while (days.has(cursor.toDateString())) {
    streak += 1;
    cursor.setTime(cursor.getTime() - 86_400_000);
  }
  return streak;
}

export async function buildContext(newRun: RunRecord): Promise<AchievementContext> {
  const [runs, fr, en] = await Promise.all([allRuns(), getProgress('fr'), getProgress('en')]);
  return {
    newRun,
    runs,
    totalChars: runs.reduce((sum, r) => sum + r.chars, 0),
    streakDays: computeStreak(runs.map((r) => r.date), Date.now()),
    progress: { fr, en },
    now: Date.now(),
  };
}

/** Persiste et retourne uniquement les succès nouvellement débloqués. */
export async function unlockNew(ctx: AchievementContext): Promise<AchievementDef[]> {
  const already = new Set((await db.achievements.toArray()).map((a) => a.id));
  const fresh = ACHIEVEMENTS.filter((a) => !already.has(a.id) && a.isUnlocked(ctx));
  await db.achievements.bulkAdd(fresh.map((a) => ({ id: a.id, unlockedAt: ctx.now })));
  return fresh;
}
```

- [ ] **Step 5: Vérifier que les tests passent**

Run: `npx vitest run src/achievements/`
Expected: 13 tests PASS

- [ ] **Step 6: Commit**

```bash
git add src/achievements/
git commit -m "feat(achievements): 25 declarative achievements with unlock engine"
```

---

### Task 9: Game — orchestration de fin de run

**Files:**
- Create: `src/game/runFlow.ts`
- Test: `src/game/runFlow.test.ts`

**Interfaces:**
- Consumes: `TypingState`, `elapsedMs` (tâche 2) ; `computeWpm`, `computeAccuracy`, `wpmTimeline` (tâche 3) ; `challengerPoints` (tâche 6) ; `saveRun`, `personalBests`, `recordChallengerResult`, `RunRecord`, `GameMode`, `ChallengerProgress` (tâche 7) ; `buildContext`, `unlockNew`, `AchievementDef` (tâche 8) ; `Tier` (tâche 6) ; `Language`, `TextOptions` (tâche 4).
- Produces (consommé par tâches 10, 12, 13) :
  - `interface RunConfig { mode: GameMode; language: Language; textId: string; options: TextOptions }`
  - `type RecordKind = 'wpm' | 'accuracy' | 'longest'`
  - `interface RunResult { run: RunRecord; timeline: number[]; newAchievements: AchievementDef[]; tierUp: Tier | null; progress: ChallengerProgress | null; newRecords: RecordKind[] }`
  - `buildRunRecord(state: TypingState, config: RunConfig, now: number): RunRecord` — partie pure (aucune DB), utilisée par `completeRun` et par le fallback sans persistance (tâche 11)
  - `completeRun(state: TypingState, config: RunConfig, now: number): Promise<RunResult>`

Règles : WPM calculé sur `state.text.length` caractères (une run finie est 100 % correcte) ; `points` calculés dans les deux modes ; records détectés **avant** `saveRun` (comparaison aux bests précédents ; toute première run débloque les 3 records) ; `progress`/`tierUp` non nuls seulement en mode challenger.

- [ ] **Step 1: Écrire le test qui échoue**

`src/game/runFlow.test.ts` :

```ts
import 'fake-indexeddb/auto';
import { beforeEach, describe, expect, it } from 'vitest';
import { db } from '@/db/db';
import { createRun, typeChar } from '@/engine/typingEngine';
import { completeRun, type RunConfig } from './runFlow';
import { ALL_OPTIONS_ON } from '@/texts/normalize';
import { allRuns } from '@/db/runsRepo';

const config: RunConfig = {
  mode: 'free', language: 'fr', textId: 'fr-001', options: ALL_OPTIONS_ON,
};

/** Tape tout le texte, 600ms par caractère, sans erreur. */
function finishClean(text: string) {
  let s = createRun(text);
  for (let i = 0; i < text.length; i++) s = typeChar(s, text[i], 1000 + i * 600);
  return s;
}

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
});

describe('completeRun', () => {
  it('sauvegarde la run avec wpm, précision et points corrects', async () => {
    const text = 'abcdefghij'; // 10 car. en 5.4s -> 22.22 WPM
    const state = finishClean(text);
    const result = await completeRun(state, config, 9999);
    expect(result.run.wpm).toBeCloseTo(22.22, 1);
    expect(result.run.accuracy).toBe(1);
    expect(result.run.points).toBe(Math.round(22.22 * 1.4));
    expect(result.run.chars).toBe(10);
    expect(result.run.noBackspace).toBe(true);
    expect(await allRuns()).toHaveLength(1);
    expect(result.newRecords).toEqual(['wpm', 'accuracy', 'longest']); // 1re run
  });

  it('pénalise la précision si des erreurs ont été corrigées', async () => {
    let s = createRun('ab');
    s = typeChar(s, 'x', 1000); // erreur
    const { pressBackspace } = await import('@/engine/typingEngine');
    s = pressBackspace(s, 1200);
    s = typeChar(s, 'a', 1800);
    s = typeChar(s, 'b', 2400);
    const result = await completeRun(s, config, 9999);
    expect(result.run.accuracy).toBeCloseTo(2 / 3, 2); // 3 frappes, 1 erreur
    expect(result.run.noBackspace).toBe(false);
    expect(result.timeline.length).toBeGreaterThan(0);
  });

  it('ne détecte pas de record si un meilleur wpm existe déjà', async () => {
    await completeRun(finishClean('abcdefghij'), config, 9999); // ~22 WPM
    const slow = createRun('ab');
    let s = typeChar(slow, 'a', 1000);
    s = typeChar(s, 'b', 11_000); // 2 car. en 10s -> 2.4 WPM
    const result = await completeRun(s, config, 99_999);
    expect(result.newRecords).not.toContain('wpm');
  });

  it('en mode challenger, met à jour la ligue et retourne tierUp', async () => {
    const c: RunConfig = { ...config, mode: 'challenger', textId: 'fr-101' };
    // run très rapide : 200 car. en 11.94s -> (200/5)/0.199min = 201 WPM -> round(201×1.4) = 281 pts -> bronze direct
    const text = 'a'.repeat(200);
    let s = createRun(text);
    for (let i = 0; i < 200; i++) s = typeChar(s, 'a', 1000 + i * 60);
    const result = await completeRun(s, c, 9999);
    expect(result.progress?.total).toBe(281);
    expect(result.tierUp).toBe('bronze');
    expect(result.newAchievements.map((a) => a.id)).toContain('enter-league');
    expect(result.newAchievements.map((a) => a.id)).toContain('wpm-140');
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/game/runFlow.test.ts`
Expected: FAIL (module introuvable)

- [ ] **Step 3: Implémenter**

`src/game/runFlow.ts` :

```ts
import type { TypingState } from '@/engine/types';
import { elapsedMs } from '@/engine/typingEngine';
import { computeAccuracy, computeWpm, wpmTimeline } from '@/engine/stats';
import { challengerPoints } from '@/scoring/points';
import type { Tier } from '@/scoring/league';
import type { ChallengerProgress, GameMode, RunRecord } from '@/db/types';
import { personalBests, saveRun } from '@/db/runsRepo';
import { recordChallengerResult } from '@/db/challengerRepo';
import { buildContext, unlockNew } from '@/achievements/check';
import type { AchievementDef } from '@/achievements/definitions';
import type { Language, TextOptions } from '@/texts/types';

export interface RunConfig {
  mode: GameMode;
  language: Language;
  textId: string;
  options: TextOptions;
}

export type RecordKind = 'wpm' | 'accuracy' | 'longest';

export interface RunResult {
  run: RunRecord;
  timeline: number[];
  newAchievements: AchievementDef[];
  tierUp: Tier | null;
  progress: ChallengerProgress | null;
  newRecords: RecordKind[];
}

export function buildRunRecord(state: TypingState, config: RunConfig, now: number): RunRecord {
  const durationMs = elapsedMs(state, now);
  const wpm = computeWpm(state.text.length, durationMs);
  const accuracy = computeAccuracy(state.keystrokes, state.errors);
  return {
    date: now,
    mode: config.mode,
    language: config.language,
    textId: config.textId,
    options: config.options,
    durationMs,
    wpm,
    accuracy,
    points: challengerPoints(wpm, accuracy, config.options),
    errors: state.errors,
    backspaces: state.backspaces,
    chars: state.text.length,
    noBackspace: state.backspaces === 0,
  };
}

export async function completeRun(state: TypingState, config: RunConfig, now: number): Promise<RunResult> {
  const run = buildRunRecord(state, config, now);
  const { durationMs, wpm, accuracy, points } = run;

  const bests = await personalBests();
  const newRecords: RecordKind[] = [];
  if (bests.bestWpm === null) {
    newRecords.push('wpm', 'accuracy', 'longest');
  } else {
    if (wpm > bests.bestWpm.wpm) newRecords.push('wpm');
    if (durationMs >= 10_000 && accuracy > (bests.bestAccuracy?.accuracy ?? 0)) {
      newRecords.push('accuracy');
    }
    if (state.text.length > (bests.longestRun?.chars ?? 0)) newRecords.push('longest');
  }

  const id = await saveRun(run);
  run.id = id;

  let tierUp: Tier | null = null;
  let progress: ChallengerProgress | null = null;
  if (config.mode === 'challenger') {
    const r = await recordChallengerResult(config.language, config.textId, points, now);
    tierUp = r.tierUp;
    progress = r.progress;
  }

  const ctx = await buildContext(run);
  const newAchievements = await unlockNew(ctx);

  const timeline = wpmTimeline(state.events, state.startedAt ?? now, state.finishedAt ?? now);

  return { run, timeline, newAchievements, tierUp, progress, newRecords };
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/game/`
Expected: 4 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/game/
git commit -m "feat(game): end-of-run orchestration (scoring, records, league, achievements)"
```

---

### Task 10: Shell applicatif — router, layout, thème, stores settings/toasts, sons

**Files:**
- Create: `src/state/settingsStore.ts`, `src/state/toastStore.ts`, `src/ui/sounds.ts`, `src/ui/components/Layout.tsx`, `src/ui/components/Header.tsx`, `src/ui/components/Toggle.tsx`, `src/ui/components/TierBadge.tsx`, `src/ui/components/ToastHost.tsx`
- Modify: `src/App.tsx` (remplace le placeholder)
- Test: `src/state/settingsStore.test.ts`, `src/state/toastStore.test.ts`, `src/ui/components/Layout.test.tsx`

**Interfaces:**
- Consumes: `getProfile`, `updateProfile`, `DEFAULT_PROFILE`, `checkPersistence`, `Profile` (tâche 7) ; `Tier` (tâche 6).
- Produces (consommé par tâches 11-18) :
  - `useSettings` (zustand) : `{ profile: Profile; loaded: boolean; load(): Promise<void>; update(patch: Partial<Profile>): Promise<void> }` ; `applyTheme(theme: 'dark' | 'light'): void`
  - `useToasts` (zustand) : `{ toasts: Toast[]; push(t: Omit<Toast,'id'>): void; dismiss(id: number): void }` ; `interface Toast { id: number; title: string; description?: string; kind: 'achievement' | 'info' }` (auto-dismiss 5 s)
  - `playKey()`, `playError()`, `playSuccess()`, `setSoundsEnabled(v: boolean)` (WebAudio, aucun asset ; silencieux si audio indisponible)
  - `<Layout>` : header nav + `<Outlet/>` + `<ToastHost/>` + bandeau « progression non sauvegardée » si IndexedDB indisponible
  - `<Toggle checked onChange label/>`, `<TierBadge tier/>`, `<ToastHost/>`
  - `App` : BrowserRouter + routes des 7 pages + `useSettings.load()` au montage

- [ ] **Step 1: Écrire les tests qui échouent**

`src/state/settingsStore.test.ts` :

```ts
import 'fake-indexeddb/auto';
import { beforeEach, describe, expect, it } from 'vitest';
import { db } from '@/db/db';
import { useSettings } from './settingsStore';

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
  useSettings.setState({ profile: { ...useSettings.getState().profile, pseudo: 'Joueur' }, loaded: false });
});

describe('settingsStore', () => {
  it('charge le profil par défaut puis persiste les modifications', async () => {
    await useSettings.getState().load();
    expect(useSettings.getState().loaded).toBe(true);
    expect(useSettings.getState().profile.pseudo).toBe('Joueur');
    await useSettings.getState().update({ pseudo: 'Ludo', sounds: false });
    expect(useSettings.getState().profile.pseudo).toBe('Ludo');
    const { getProfile } = await import('@/db/profileRepo');
    expect((await getProfile()).sounds).toBe(false);
  });
});
```

`src/state/toastStore.test.ts` :

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest';
import { useToasts } from './toastStore';

beforeEach(() => {
  useToasts.setState({ toasts: [] });
});

describe('toastStore', () => {
  it('empile et retire les toasts, auto-dismiss après 5s', () => {
    vi.useFakeTimers();
    useToasts.getState().push({ title: 'Succès débloqué', description: 'Machine', kind: 'achievement' });
    expect(useToasts.getState().toasts).toHaveLength(1);
    const id = useToasts.getState().toasts[0].id;
    useToasts.getState().dismiss(id);
    expect(useToasts.getState().toasts).toHaveLength(0);
    useToasts.getState().push({ title: 'Info', kind: 'info' });
    vi.advanceTimersByTime(5100);
    expect(useToasts.getState().toasts).toHaveLength(0);
    vi.useRealTimers();
  });
});
```

`src/ui/components/Layout.test.tsx` :

```tsx
import 'fake-indexeddb/auto';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { describe, expect, it } from 'vitest';
import { Layout } from './Layout';

describe('Layout', () => {
  it('affiche la navigation principale', () => {
    render(
      <MemoryRouter>
        <Layout />
      </MemoryRouter>,
    );
    expect(screen.getByRole('link', { name: 'Entraînement' })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: 'Challenger' })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: 'Classements' })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: 'Succès' })).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run src/state/ src/ui/components/Layout.test.tsx`
Expected: FAIL (modules introuvables)

- [ ] **Step 3: Implémenter les stores et les sons**

`src/state/settingsStore.ts` :

```ts
import { create } from 'zustand';
import { DEFAULT_PROFILE, getProfile, updateProfile } from '@/db/profileRepo';
import type { Profile } from '@/db/types';
import { setSoundsEnabled } from '@/ui/sounds';

export function applyTheme(theme: 'dark' | 'light'): void {
  document.documentElement.dataset.theme = theme;
}

interface SettingsStore {
  profile: Profile;
  loaded: boolean;
  load(): Promise<void>;
  update(patch: Partial<Profile>): Promise<void>;
}

export const useSettings = create<SettingsStore>((set, get) => ({
  profile: DEFAULT_PROFILE,
  loaded: false,
  async load() {
    let profile = DEFAULT_PROFILE;
    try {
      profile = await getProfile();
    } catch {
      // IndexedDB indisponible : session en mémoire avec le profil par défaut
    }
    applyTheme(profile.theme);
    setSoundsEnabled(profile.sounds);
    set({ profile, loaded: true });
  },
  async update(patch) {
    try {
      await updateProfile(patch);
    } catch {
      // persistance indisponible : le réglage ne vaut que pour la session
    }
    set({ profile: { ...get().profile, ...patch } });
    if (patch.theme) applyTheme(patch.theme);
    if (patch.sounds !== undefined) setSoundsEnabled(patch.sounds);
  },
}));
```

`src/state/toastStore.ts` :

```ts
import { create } from 'zustand';

export interface Toast {
  id: number;
  title: string;
  description?: string;
  kind: 'achievement' | 'info';
}

let nextId = 1;

interface ToastStore {
  toasts: Toast[];
  push(t: Omit<Toast, 'id'>): void;
  dismiss(id: number): void;
}

export const useToasts = create<ToastStore>((set) => ({
  toasts: [],
  push(t) {
    const id = nextId++;
    set((s) => ({ toasts: [...s.toasts, { ...t, id }] }));
    setTimeout(() => {
      set((s) => ({ toasts: s.toasts.filter((x) => x.id !== id) }));
    }, 5000);
  },
  dismiss(id) {
    set((s) => ({ toasts: s.toasts.filter((x) => x.id !== id) }));
  },
}));
```

`src/ui/sounds.ts` :

```ts
let ctx: AudioContext | null = null;
let enabled = true;

export function setSoundsEnabled(v: boolean): void {
  enabled = v;
}

function beep(freq: number, durationMs: number, type: OscillatorType, gainValue: number): void {
  if (!enabled) return;
  try {
    ctx ??= new AudioContext();
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    osc.type = type;
    osc.frequency.value = freq;
    gain.gain.value = gainValue;
    gain.gain.exponentialRampToValueAtTime(0.0001, ctx.currentTime + durationMs / 1000);
    osc.connect(gain);
    gain.connect(ctx.destination);
    osc.start();
    osc.stop(ctx.currentTime + durationMs / 1000);
  } catch {
    // audio indisponible : silencieux
  }
}

export const playKey = (): void => beep(600, 30, 'sine', 0.02);
export const playError = (): void => beep(160, 80, 'square', 0.03);
export const playSuccess = (): void => {
  beep(520, 90, 'sine', 0.04);
  setTimeout(() => beep(780, 120, 'sine', 0.04), 90);
};
```

- [ ] **Step 4: Implémenter les composants du shell**

`src/ui/components/Header.tsx` :

```tsx
import { NavLink } from 'react-router-dom';

const LINKS = [
  { to: '/', label: 'Accueil', end: true },
  { to: '/play', label: 'Entraînement' },
  { to: '/challenger', label: 'Challenger' },
  { to: '/leaderboard', label: 'Classements' },
  { to: '/achievements', label: 'Succès' },
  { to: '/stats', label: 'Stats' },
  { to: '/settings', label: 'Réglages' },
];

export function Header() {
  return (
    <header className="sticky top-0 z-10 border-b border-line bg-bg/80 backdrop-blur">
      <nav className="mx-auto flex max-w-5xl items-center gap-1 px-4 py-3">
        <NavLink to="/" className="mr-4 text-lg font-extrabold tracking-tight text-accent">
          Dacty
        </NavLink>
        {LINKS.slice(1).map(({ to, label }) => (
          <NavLink
            key={to}
            to={to}
            className={({ isActive }) =>
              `rounded-lg px-3 py-1.5 text-sm transition-colors ${
                isActive ? 'bg-surface text-text' : 'text-muted hover:text-text'
              }`
            }
          >
            {label}
          </NavLink>
        ))}
      </nav>
    </header>
  );
}
```

`src/ui/components/Toggle.tsx` :

```tsx
interface ToggleProps {
  checked: boolean;
  onChange: (v: boolean) => void;
  label: string;
}

export function Toggle({ checked, onChange, label }: ToggleProps) {
  return (
    <button
      type="button"
      role="switch"
      aria-checked={checked}
      aria-label={label}
      onClick={() => onChange(!checked)}
      className={`relative h-6 w-11 rounded-full transition-colors ${checked ? 'bg-accent' : 'bg-line'}`}
    >
      <span
        className={`absolute top-0.5 h-5 w-5 rounded-full bg-white transition-transform ${
          checked ? 'translate-x-[22px]' : 'translate-x-0.5'
        }`}
      />
    </button>
  );
}
```

`src/ui/components/TierBadge.tsx` :

```tsx
import type { Tier } from '@/scoring/league';

const LABELS: Record<Tier, string> = {
  bronze: 'Bronze',
  argent: 'Argent',
  or: 'Or',
  platine: 'Platine',
  diamant: 'Diamant',
  challenger: 'Challenger',
};

const COLORS: Record<Tier, string> = {
  bronze: 'bg-amber-800/30 text-amber-500 border-amber-700',
  argent: 'bg-slate-500/20 text-slate-300 border-slate-500',
  or: 'bg-yellow-500/20 text-yellow-400 border-yellow-600',
  platine: 'bg-cyan-500/20 text-cyan-300 border-cyan-600',
  diamant: 'bg-blue-500/20 text-blue-300 border-blue-500',
  challenger: 'bg-purple-500/20 text-purple-300 border-purple-500',
};

export function TierBadge({ tier }: { tier: Tier }) {
  return (
    <span className={`rounded-full border px-3 py-0.5 text-xs font-bold uppercase tracking-wide ${COLORS[tier]}`}>
      {LABELS[tier]}
    </span>
  );
}

export function tierLabel(tier: Tier): string {
  return LABELS[tier];
}
```

`src/ui/components/ToastHost.tsx` :

```tsx
import { AnimatePresence, motion } from 'framer-motion';
import { useToasts } from '@/state/toastStore';

export function ToastHost() {
  const toasts = useToasts((s) => s.toasts);
  const dismiss = useToasts((s) => s.dismiss);
  return (
    <div className="pointer-events-none fixed right-4 bottom-4 z-50 flex w-80 flex-col gap-2">
      <AnimatePresence>
        {toasts.map((t) => (
          <motion.button
            key={t.id}
            type="button"
            onClick={() => dismiss(t.id)}
            initial={{ opacity: 0, x: 60 }}
            animate={{ opacity: 1, x: 0 }}
            exit={{ opacity: 0, x: 60 }}
            className="pointer-events-auto rounded-xl border border-line bg-surface p-4 text-left shadow-lg backdrop-blur"
          >
            <p className="text-xs font-semibold uppercase tracking-wide text-accent">
              {t.kind === 'achievement' ? 'Succès débloqué' : 'Info'}
            </p>
            <p className="font-bold">{t.title}</p>
            {t.description && <p className="text-sm text-muted">{t.description}</p>}
          </motion.button>
        ))}
      </AnimatePresence>
    </div>
  );
}
```

`src/ui/components/Layout.tsx` :

```tsx
import { useEffect, useState } from 'react';
import { Outlet } from 'react-router-dom';
import { Header } from './Header';
import { ToastHost } from './ToastHost';
import { checkPersistence } from '@/db/persistence';

export function Layout() {
  const [persistent, setPersistent] = useState(true);
  useEffect(() => {
    void checkPersistence().then(setPersistent);
  }, []);
  return (
    <div className="min-h-screen">
      <Header />
      {!persistent && (
        <div className="bg-err/15 px-4 py-2 text-center text-sm text-err">
          Stockage indisponible : ta progression ne sera pas sauvegardée (navigation privée ?).
        </div>
      )}
      <main className="mx-auto max-w-5xl px-4 py-8">
        <Outlet />
      </main>
      <ToastHost />
    </div>
  );
}
```

- [ ] **Step 5: Brancher le router dans App**

`src/App.tsx` (remplacement complet) :

```tsx
import { useEffect } from 'react';
import { BrowserRouter, Route, Routes } from 'react-router-dom';
import { Layout } from '@/ui/components/Layout';
import { HomePage } from '@/ui/pages/HomePage';
import { PlayPage } from '@/ui/pages/PlayPage';
import { ChallengerPage } from '@/ui/pages/ChallengerPage';
import { LeaderboardPage } from '@/ui/pages/LeaderboardPage';
import { AchievementsPage } from '@/ui/pages/AchievementsPage';
import { StatsPage } from '@/ui/pages/StatsPage';
import { SettingsPage } from '@/ui/pages/SettingsPage';
import { useSettings } from '@/state/settingsStore';

export default function App() {
  const load = useSettings((s) => s.load);
  useEffect(() => {
    void load();
  }, [load]);

  return (
    <BrowserRouter>
      <Routes>
        <Route element={<Layout />}>
          <Route path="/" element={<HomePage />} />
          <Route path="/play" element={<PlayPage />} />
          <Route path="/challenger" element={<ChallengerPage />} />
          <Route path="/leaderboard" element={<LeaderboardPage />} />
          <Route path="/achievements" element={<AchievementsPage />} />
          <Route path="/stats" element={<StatsPage />} />
          <Route path="/settings" element={<SettingsPage />} />
        </Route>
      </Routes>
    </BrowserRouter>
  );
}
```

Créer des placeholders temporaires pour les pages (chacune sera implémentée dans sa tâche) — fichier `src/ui/pages/Placeholders.tsx` **temporaire**… Non : créer directement chaque page avec un stub minimal à compléter dans sa tâche. Exemple pour toutes :

```tsx
export function PlayPage() {
  return <h1 className="text-2xl font-bold">Entraînement</h1>;
}
```

Créer ainsi `HomePage.tsx`, `PlayPage.tsx`, `ChallengerPage.tsx`, `LeaderboardPage.tsx`, `AchievementsPage.tsx`, `StatsPage.tsx`, `SettingsPage.tsx` (nom de fonction = nom de fichier sans `.tsx`).

- [ ] **Step 6: Vérifier tests + build**

Run: `npx vitest run src/state/ src/ui/components/Layout.test.tsx && npm run build`
Expected: tests PASS, build OK

- [ ] **Step 7: Commit**

```bash
git add src/
git commit -m "feat(shell): router, layout, theme, settings/toast stores, sounds"
```

---

### Task 11: Run — store de frappe, garde de focus, zone de saisie

**Files:**
- Create: `src/state/runStore.ts`, `src/ui/hooks/useFocusGuard.ts`, `src/ui/components/TypingArea.tsx`
- Test: `src/state/runStore.test.ts`, `src/ui/hooks/useFocusGuard.test.ts`, `src/ui/components/TypingArea.test.tsx`

**Interfaces:**
- Consumes: engine (tâche 2), `completeRun`, `RunConfig`, `RunResult` (tâche 9), `useSettings` (tâche 10).
- Produces (consommé par tâches 12, 13) :
  - `useRunStore` (zustand) : `{ status: 'idle'|'running'|'paused'|'finished'|'invalidated'; config: RunConfig|null; typing: TypingState|null; result: RunResult|null; start(config, text): void; key(char): void; backspace(): void; pause(): void; resume(): void; invalidate(): void; reset(): void }` — à la fin de la frappe, lance `completeRun` et remplit `result`
  - `useFocusGuard(): void` — blur fenêtre → `pause()` + timer `focusTimeoutSec` → `invalidate()` ; focus → annule le timer + `resume()`
  - `<TypingArea state disabled onChar onBackspace />` : rendu des caractères colorés, caret animé (framer-motion), clavier capturé par un input invisible, collage bloqué, touches modificatrices ignorées

- [ ] **Step 1: Écrire les tests qui échouent**

`src/state/runStore.test.ts` :

```ts
import 'fake-indexeddb/auto';
import { beforeEach, describe, expect, it } from 'vitest';
import { db } from '@/db/db';
import { useRunStore } from './runStore';
import { ALL_OPTIONS_ON } from '@/texts/normalize';

const config = { mode: 'free' as const, language: 'fr' as const, textId: 'fr-001', options: ALL_OPTIONS_ON };

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
  useRunStore.getState().reset();
});

describe('runStore', () => {
  it('démarre une run et avance à chaque frappe', () => {
    useRunStore.getState().start(config, 'ab');
    expect(useRunStore.getState().status).toBe('running');
    useRunStore.getState().key('a');
    expect(useRunStore.getState().typing?.cursor).toBe(1);
    useRunStore.getState().backspace();
    expect(useRunStore.getState().typing?.cursor).toBe(0);
  });

  it('termine la run et remplit le résultat', async () => {
    useRunStore.getState().start(config, 'ab');
    useRunStore.getState().key('a');
    useRunStore.getState().key('b');
    expect(useRunStore.getState().status).toBe('finished');
    await vi.waitFor(() => expect(useRunStore.getState().result).not.toBeNull());
    expect(useRunStore.getState().result?.run.chars).toBe(2);
    expect(useRunStore.getState().result?.run.mode).toBe('free');
  });

  it('ignore les frappes quand la run est en pause ou invalidée', () => {
    useRunStore.getState().start(config, 'ab');
    useRunStore.getState().key('a');
    useRunStore.getState().pause();
    useRunStore.getState().key('b');
    expect(useRunStore.getState().typing?.cursor).toBe(1);
    useRunStore.getState().resume();
    useRunStore.getState().invalidate();
    useRunStore.getState().key('b');
    expect(useRunStore.getState().status).toBe('invalidated');
  });
});
```

`src/ui/hooks/useFocusGuard.test.ts` :

```tsx
import 'fake-indexeddb/auto';
import { act, renderHook } from '@testing-library/react';
import { beforeEach, describe, expect, it, vi } from 'vitest';
import { db } from '@/db/db';
import { useRunStore } from '@/state/runStore';
import { useSettings } from '@/state/settingsStore';
import { useFocusGuard } from './useFocusGuard';
import { ALL_OPTIONS_ON } from '@/texts/normalize';

const config = { mode: 'free' as const, language: 'fr' as const, textId: 'fr-001', options: ALL_OPTIONS_ON };

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
  useRunStore.getState().reset();
  useSettings.setState((s) => ({ profile: { ...s.profile, focusTimeoutSec: 5 } }));
});

describe('useFocusGuard', () => {
  it('met en pause au blur, reprend au focus, invalide après le délai', () => {
    vi.useFakeTimers();
    useRunStore.getState().start(config, 'ab');
    useRunStore.getState().key('a'); // démarre le chrono
    renderHook(() => useFocusGuard());

    act(() => { window.dispatchEvent(new Event('blur')); });
    expect(useRunStore.getState().status).toBe('paused');

    act(() => { window.dispatchEvent(new Event('focus')); });
    expect(useRunStore.getState().status).toBe('running');

    act(() => { window.dispatchEvent(new Event('blur')); });
    act(() => { vi.advanceTimersByTime(5100); });
    expect(useRunStore.getState().status).toBe('invalidated');
    vi.useRealTimers();
  });
});
```

`src/ui/components/TypingArea.test.tsx` :

```tsx
import { fireEvent, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { describe, expect, it, vi } from 'vitest';
import { createRun, typeChar } from '@/engine/typingEngine';
import { TypingArea } from './TypingArea';

describe('TypingArea', () => {
  it('affiche les caractères avec leurs statuts', () => {
    const state = typeChar(createRun('ab'), 'x', 1000); // erreur sur 'a'
    render(<TypingArea state={state} onChar={() => {}} onBackspace={() => {}} />);
    const chars = screen.getByTestId('typing-area').querySelectorAll('[data-char]');
    expect(chars).toHaveLength(2);
    expect(chars[0].className).toContain('text-err');
    expect(chars[1].className).toContain('text-muted');
  });

  it('transmet les frappes et le backspace', async () => {
    const onChar = vi.fn();
    const onBackspace = vi.fn();
    render(<TypingArea state={createRun('ab')} onChar={onChar} onBackspace={onBackspace} />);
    const input = screen.getByLabelText('Zone de saisie');
    input.focus();
    await userEvent.keyboard('a');
    expect(onChar).toHaveBeenCalledWith('a');
    await userEvent.keyboard('{Backspace}');
    expect(onBackspace).toHaveBeenCalled();
  });

  it('bloque le collage', () => {
    render(<TypingArea state={createRun('ab')} onChar={() => {}} onBackspace={() => {}} />);
    const input = screen.getByLabelText('Zone de saisie');
    // fireEvent retourne false quand preventDefault a été appelé
    const notPrevented = fireEvent.paste(input, { clipboardData: { getData: () => 'ab' } });
    expect(notPrevented).toBe(false);
  });

  it('ignore les frappes quand disabled', async () => {
    const onChar = vi.fn();
    render(<TypingArea state={createRun('ab')} disabled onChar={onChar} onBackspace={() => {}} />);
    screen.getByLabelText('Zone de saisie').focus();
    await userEvent.keyboard('a');
    expect(onChar).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run src/state/runStore.test.ts src/ui/hooks/ src/ui/components/TypingArea.test.tsx`
Expected: FAIL (modules introuvables)

- [ ] **Step 3: Implémenter le store de run**

`src/state/runStore.ts` :

```ts
import { create } from 'zustand';
import {
  createRun, isFinished, pauseRun, pressBackspace, resumeRun, typeChar,
} from '@/engine/typingEngine';
import { wpmTimeline } from '@/engine/stats';
import type { TypingState } from '@/engine/types';
import { buildRunRecord, completeRun, type RunConfig, type RunResult } from '@/game/runFlow';
import { useToasts } from './toastStore';

export type RunStatus = 'idle' | 'running' | 'paused' | 'finished' | 'invalidated';

interface RunStore {
  status: RunStatus;
  config: RunConfig | null;
  typing: TypingState | null;
  result: RunResult | null;
  start(config: RunConfig, text: string): void;
  key(char: string): void;
  backspace(): void;
  pause(): void;
  resume(): void;
  invalidate(): void;
  reset(): void;
}

export const useRunStore = create<RunStore>((set, get) => ({
  status: 'idle',
  config: null,
  typing: null,
  result: null,

  start(config, text) {
    set({ config, typing: createRun(text), result: null, status: 'running' });
  },

  key(char) {
    const { typing, config, status } = get();
    if (!typing || !config || status !== 'running') return;
    const next = typeChar(typing, char, Date.now());
    if (isFinished(next)) {
      set({ typing: next, status: 'finished' });
      void completeRun(next, config, Date.now())
        .then((result) => set({ result }))
        .catch(() => {
          // IndexedDB indisponible (navigation privée) : résultats affichés, rien n'est persisté
          const now = Date.now();
          set({
            result: {
              run: buildRunRecord(next, config, now),
              timeline: wpmTimeline(next.events, next.startedAt ?? now, next.finishedAt ?? now),
              newAchievements: [],
              tierUp: null,
              progress: null,
              newRecords: [],
            },
          });
          useToasts.getState().push({
            title: 'Sauvegarde impossible',
            description: 'Le stockage local est indisponible : cette run ne sera pas enregistrée.',
            kind: 'info',
          });
        });
    } else if (next !== typing) {
      set({ typing: next });
    }
  },

  backspace() {
    const { typing, status } = get();
    if (!typing || status !== 'running') return;
    const next = pressBackspace(typing, Date.now());
    if (next !== typing) set({ typing: next });
  },

  pause() {
    const { typing, status } = get();
    if (!typing || status !== 'running') return;
    set({ typing: pauseRun(typing, Date.now()), status: 'paused' });
  },

  resume() {
    const { typing, status } = get();
    if (!typing || status !== 'paused') return;
    set({ typing: resumeRun(typing, Date.now()), status: 'running' });
  },

  invalidate() {
    set({ status: 'invalidated', typing: null, result: null });
  },

  reset() {
    set({ status: 'idle', config: null, typing: null, result: null });
  },
}));
```

- [ ] **Step 4: Implémenter la garde de focus**

`src/ui/hooks/useFocusGuard.ts` :

```ts
import { useEffect } from 'react';
import { useRunStore } from '@/state/runStore';
import { useSettings } from '@/state/settingsStore';

/** Pause au blur de la fenêtre ; invalide la run si le focus ne revient pas sous `focusTimeoutSec`. */
export function useFocusGuard(): void {
  const status = useRunStore((s) => s.status);
  const timeoutSec = useSettings((s) => s.profile.focusTimeoutSec);

  useEffect(() => {
    if (status !== 'running' && status !== 'paused') return;
    const { pause, resume, invalidate } = useRunStore.getState();
    let timer: ReturnType<typeof setTimeout> | null = null;
    const onBlur = () => {
      pause();
      timer = setTimeout(invalidate, timeoutSec * 1000);
    };
    const onFocus = () => {
      if (timer) clearTimeout(timer);
      timer = null;
      resume();
    };
    window.addEventListener('blur', onBlur);
    window.addEventListener('focus', onFocus);
    return () => {
      window.removeEventListener('blur', onBlur);
      window.removeEventListener('focus', onFocus);
      if (timer) clearTimeout(timer);
    };
  }, [status, timeoutSec]);
}
```

- [ ] **Step 5: Implémenter la zone de frappe**

`src/ui/components/TypingArea.tsx` :

```tsx
import { useLayoutEffect, useRef, useState } from 'react';
import { motion } from 'framer-motion';
import type { TypingState } from '@/engine/types';

interface TypingAreaProps {
  state: TypingState;
  disabled?: boolean;
  onChar: (c: string) => void;
  onBackspace: () => void;
}

export function TypingArea({ state, disabled = false, onChar, onBackspace }: TypingAreaProps) {
  const containerRef = useRef<HTMLDivElement>(null);
  const inputRef = useRef<HTMLInputElement>(null);
  const [caret, setCaret] = useState({ x: 0, y: 0, h: 28 });

  useLayoutEffect(() => {
    const container = containerRef.current;
    if (!container) return;
    const chars = container.querySelectorAll<HTMLSpanElement>('[data-char]');
    if (chars.length === 0) return;
    const idx = Math.min(state.cursor, chars.length - 1);
    const target = chars[idx];
    const cRect = container.getBoundingClientRect();
    const tRect = target.getBoundingClientRect();
    setCaret({
      x: (state.cursor >= chars.length ? tRect.right : tRect.left) - cRect.left,
      y: tRect.top - cRect.top,
      h: tRect.height,
    });
  }, [state.cursor, state.text]);

  return (
    <div
      ref={containerRef}
      data-testid="typing-area"
      onClick={() => inputRef.current?.focus()}
      className="relative cursor-text rounded-xl border border-line bg-surface p-6 font-type text-xl leading-relaxed break-words backdrop-blur select-none"
    >
      <input
        ref={inputRef}
        aria-label="Zone de saisie"
        className="absolute h-1 w-1 opacity-0"
        autoFocus
        value=""
        onChange={() => undefined}
        onKeyDown={(e) => {
          if (disabled) return;
          if (e.key === 'Backspace') {
            e.preventDefault();
            onBackspace();
            return;
          }
          if (e.key.length === 1 && !e.ctrlKey && !e.metaKey && !e.altKey) {
            e.preventDefault();
            onChar(e.key);
          }
        }}
        onPaste={(e) => e.preventDefault()}
      />
      <motion.span
        data-testid="caret"
        className="absolute w-0.5 rounded bg-accent"
        animate={{ x: caret.x, y: caret.y, height: caret.h }}
        transition={{ type: 'spring', stiffness: 500, damping: 40 }}
      />
      {state.text.split('').map((ch, i) => (
        <span
          // eslint-disable-next-line react/no-array-index-key
          key={i}
          data-char
          className={
            state.statuses[i] === 'correct'
              ? 'text-ok'
              : state.statuses[i] === 'incorrect'
                ? 'rounded-sm bg-err/15 text-err'
                : 'text-muted'
          }
        >
          {ch}
        </span>
      ))}
    </div>
  );
}
```

- [ ] **Step 6: Vérifier que les tests passent**

Run: `npx vitest run src/state/ src/ui/`
Expected: tous les tests PASS

- [ ] **Step 7: Commit**

```bash
git add src/state/runStore.ts src/ui/hooks/ src/ui/components/TypingArea.tsx src/state/runStore.test.ts src/ui/components/TypingArea.test.tsx
git commit -m "feat(run): run store, focus guard and typing area with animated caret"
```

---

### Task 12: Page Entraînement (mode libre) + HUD + écran de résultats

**Files:**
- Create: `src/ui/components/RunHud.tsx`, `src/ui/components/ResultsScreen.tsx`, `src/ui/components/Sparkline.tsx`
- Modify: `src/ui/pages/PlayPage.tsx` (remplace le stub de la tâche 10)
- Test: `src/ui/components/Sparkline.test.tsx`, `src/ui/pages/PlayPage.test.tsx`

**Interfaces:**
- Consumes: `useRunStore`, `useSettings`, `useToasts`, `useFocusGuard`, `TypingArea`, `Toggle`, `playKey/playError/playSuccess` (tâches 10-11) ; `pickText`, `applyOptions`, `ALL_OPTIONS_ON`, `Language`, `TextLength`, `TextOptions` (tâches 4-5) ; `RunResult` (tâche 9) ; `liveWpm`, `computeAccuracy`, `elapsedMs` (tâches 2-3).
- Produces (consommé par tâche 13) :
  - `<RunHud typing />` : WPM live, précision live, temps écoulé, progression % (tick 500 ms)
  - `<ResultsScreen result onReplay onExit />` : temps, WPM, précision, points, sparkline, records, montée de tier éventuelle, succès inline
  - `<Sparkline data width? height? />` : polyline SVG, `null` si `data` vide

- [ ] **Step 1: Écrire les tests qui échouent**

`src/ui/components/Sparkline.test.tsx` :

```tsx
import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { Sparkline } from './Sparkline';

describe('Sparkline', () => {
  it('rend une polyline normalisée', () => {
    const { container } = render(<Sparkline data={[10, 20, 10]} width={100} height={40} />);
    const line = container.querySelector('polyline');
    // y = 40 - (v/20)*36 - 2 -> v=10 : 20 ; v=20 : 2
    expect(line?.getAttribute('points')).toBe('0,20 50,2 100,20');
  });

  it('ne rend rien sans données', () => {
    const { container } = render(<Sparkline data={[]} />);
    expect(container.querySelector('svg')).toBeNull();
  });
});
```

`src/ui/pages/PlayPage.test.tsx` :

```tsx
import 'fake-indexeddb/auto';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { beforeEach, describe, expect, it } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import { db } from '@/db/db';
import { useRunStore } from '@/state/runStore';
import { PlayPage } from './PlayPage';

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
  useRunStore.getState().reset();
});

describe('PlayPage', () => {
  it('affiche le setup puis démarre une run', async () => {
    render(<MemoryRouter><PlayPage /></MemoryRouter>);
    expect(screen.getByRole('button', { name: 'Démarrer' })).toBeInTheDocument();
    expect(screen.getByText('Ponctuation')).toBeInTheDocument();
    await userEvent.click(screen.getByRole('button', { name: 'Démarrer' }));
    expect(await screen.findByTestId('typing-area')).toBeInTheDocument();
    expect(useRunStore.getState().config?.mode).toBe('free');
  });
});
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run src/ui/components/Sparkline.test.tsx src/ui/pages/PlayPage.test.tsx`
Expected: FAIL (modules introuvables)

- [ ] **Step 3: Implémenter Sparkline et RunHud**

`src/ui/components/Sparkline.tsx` :

```tsx
interface SparklineProps {
  data: number[];
  width?: number;
  height?: number;
}

export function Sparkline({ data, width = 280, height = 48 }: SparklineProps) {
  if (data.length === 0) return null;
  const max = Math.max(1, ...data);
  const step = width / Math.max(1, data.length - 1);
  const points = data
    .map((v, i) => `${Math.round(i * step)},${Math.round(height - (v / max) * (height - 4) - 2)}`)
    .join(' ');
  return (
    <svg width={width} height={height} role="img" aria-label="Évolution du WPM" className="overflow-visible">
      <polyline
        points={points}
        fill="none"
        stroke="var(--accent)"
        strokeWidth="2"
        strokeLinejoin="round"
        strokeLinecap="round"
      />
    </svg>
  );
}
```

`src/ui/components/RunHud.tsx` :

```tsx
import { useEffect, useState } from 'react';
import { computeAccuracy, liveWpm } from '@/engine/stats';
import { elapsedMs } from '@/engine/typingEngine';
import type { TypingState } from '@/engine/types';

function Cell({ label, value }: { label: string; value: string }) {
  return (
    <div className="rounded-xl border border-line bg-surface px-4 py-2 text-center backdrop-blur">
      <p className="text-xs uppercase tracking-wide text-muted">{label}</p>
      <p className="font-type text-xl font-bold">{value}</p>
    </div>
  );
}

export function RunHud({ typing }: { typing: TypingState }) {
  const [, setTick] = useState(0);
  useEffect(() => {
    const id = setInterval(() => setTick((t) => t + 1), 500);
    return () => clearInterval(id);
  }, []);
  const now = Date.now();
  const wpm = liveWpm(typing, now);
  const accuracy = computeAccuracy(typing.keystrokes, typing.errors);
  const seconds = elapsedMs(typing, now) / 1000;
  const progress = Math.round((typing.cursor / typing.text.length) * 100);
  return (
    <div className="grid grid-cols-4 gap-3">
      <Cell label="WPM" value={wpm.toFixed(0)} />
      <Cell label="Précision" value={`${Math.round(accuracy * 100)} %`} />
      <Cell label="Temps" value={`${seconds.toFixed(1)} s`} />
      <Cell label="Progression" value={`${progress} %`} />
    </div>
  );
}
```

- [ ] **Step 4: Implémenter l'écran de résultats**

`src/ui/components/ResultsScreen.tsx` :

```tsx
import { motion } from 'framer-motion';
import type { RunResult } from '@/game/runFlow';
import { Sparkline } from './Sparkline';
import { TierBadge } from './TierBadge';
import type { AchievementDef } from '@/achievements/definitions';

const RECORD_LABELS: Record<string, string> = {
  wpm: 'Nouveau record de WPM !',
  accuracy: 'Nouvelle meilleure précision !',
  longest: 'Ta plus longue run !',
};

interface ResultsScreenProps {
  result: RunResult;
  onReplay: () => void;
  onExit: () => void;
}

export function ResultsScreen({ result, onReplay, onExit }: ResultsScreenProps) {
  const { run } = result;
  return (
    <motion.div
      initial={{ opacity: 0, y: 16 }}
      animate={{ opacity: 1, y: 0 }}
      className="flex flex-col gap-6 rounded-2xl border border-line bg-surface p-8 backdrop-blur"
    >
      <div className="grid grid-cols-2 gap-4 sm:grid-cols-4">
        <div>
          <p className="text-xs uppercase tracking-wide text-muted">WPM</p>
          <p className="font-type text-4xl font-extrabold text-accent">{run.wpm.toFixed(1)}</p>
        </div>
        <div>
          <p className="text-xs uppercase tracking-wide text-muted">Précision</p>
          <p className="font-type text-4xl font-extrabold">{(run.accuracy * 100).toFixed(1)} %</p>
        </div>
        <div>
          <p className="text-xs uppercase tracking-wide text-muted">Temps</p>
          <p className="font-type text-4xl font-extrabold">{(run.durationMs / 1000).toFixed(1)} s</p>
        </div>
        <div>
          <p className="text-xs uppercase tracking-wide text-muted">Points</p>
          <p className="font-type text-4xl font-extrabold">{run.points}</p>
        </div>
      </div>

      <div>
        <p className="mb-1 text-xs uppercase tracking-wide text-muted">WPM par seconde</p>
        <Sparkline data={result.timeline} width={560} height={64} />
      </div>

      {result.newRecords.length > 0 && (
        <ul className="flex flex-wrap gap-2">
          {result.newRecords.map((r) => (
            <li key={r} className="rounded-full border border-ok/40 bg-ok/10 px-3 py-1 text-sm text-ok">
              {RECORD_LABELS[r]}
            </li>
          ))}
        </ul>
      )}

      {result.tierUp && (
        <motion.div
          initial={{ scale: 0.8 }}
          animate={{ scale: 1 }}
          className="flex items-center gap-3 rounded-xl border border-accent/40 bg-accent/10 p-4"
        >
          <span className="font-bold">Nouveau tier atteint :</span>
          <TierBadge tier={result.tierUp} />
        </motion.div>
      )}

      {result.newAchievements.length > 0 && (
        <div>
          <p className="mb-2 text-xs uppercase tracking-wide text-muted">Succès débloqués</p>
          <ul className="flex flex-wrap gap-2">
            {result.newAchievements.map((a: AchievementDef) => (
              <li key={a.id} className="rounded-full border border-line bg-bg px-3 py-1 text-sm">
                {a.title}
              </li>
            ))}
          </ul>
        </div>
      )}

      <div className="flex gap-3">
        <button
          type="button"
          onClick={onReplay}
          className="rounded-xl bg-accent px-6 py-2 font-bold text-white transition-opacity hover:opacity-90"
        >
          Rejouer
        </button>
        <button
          type="button"
          onClick={onExit}
          className="rounded-xl border border-line px-6 py-2 text-muted transition-colors hover:text-text"
        >
          Retour
        </button>
      </div>
    </motion.div>
  );
}
```

- [ ] **Step 5: Implémenter la page Entraînement**

`src/ui/pages/PlayPage.tsx` :

```tsx
import { useEffect, useState } from 'react';
import { useRunStore } from '@/state/runStore';
import { useSettings } from '@/state/settingsStore';
import { useToasts } from '@/state/toastStore';
import { useFocusGuard } from '@/ui/hooks/useFocusGuard';
import { TypingArea } from '@/ui/components/TypingArea';
import { RunHud } from '@/ui/components/RunHud';
import { ResultsScreen } from '@/ui/components/ResultsScreen';
import { Toggle } from '@/ui/components/Toggle';
import { playError, playKey, playSuccess } from '@/ui/sounds';
import { pickText } from '@/texts/corpus';
import { ALL_OPTIONS_ON, applyOptions } from '@/texts/normalize';
import type { Language, TextLength, TextOptions } from '@/texts/types';

const LENGTHS: { value: TextLength | 'quote'; label: string }[] = [
  { value: 'short', label: 'Court' },
  { value: 'medium', label: 'Moyen' },
  { value: 'long', label: 'Long' },
  { value: 'quote', label: 'Citation' },
];

const TOGGLES: { key: keyof TextOptions; label: string }[] = [
  { key: 'punctuation', label: 'Ponctuation' },
  { key: 'specialChars', label: 'Caractères spéciaux' },
  { key: 'digits', label: 'Chiffres' },
  { key: 'accents', label: 'Accents' },
];

export function PlayPage() {
  const { status, typing, result, start, key, backspace, reset } = useRunStore();
  const profile = useSettings((s) => s.profile);
  const push = useToasts((s) => s.push);
  const [language, setLanguage] = useState<Language>(profile.defaultLanguage);
  const [length, setLength] = useState<TextLength | 'quote'>('short');
  const [options, setOptions] = useState<TextOptions>(ALL_OPTIONS_ON);
  useFocusGuard();

  useEffect(() => {
    if (!result) return;
    result.newAchievements.forEach((a) =>
      push({ title: a.title, description: a.description, kind: 'achievement' }),
    );
    if (result.newRecords.length > 0) playSuccess();
  }, [result, push]);

  const begin = () => {
    const entry = pickText(language, length);
    const text = applyOptions(entry.text, options);
    start({ mode: 'free', language, textId: entry.id, options }, text);
  };

  const handleChar = (c: string) => {
    const t = useRunStore.getState().typing;
    if (t && profile.sounds) {
      if (t.text[t.cursor] === c) playKey();
      else playError();
    }
    key(c);
  };

  if (status === 'finished' && result) {
    return <ResultsScreen result={result} onReplay={begin} onExit={reset} />;
  }

  if (status === 'running' || status === 'paused' || status === 'invalidated') {
    return (
      <div className="flex flex-col gap-6">
        {typing && <RunHud typing={typing} />}
        {status === 'invalidated' ? (
          <div className="rounded-xl border border-err/40 bg-err/10 p-6 text-center">
            <p className="mb-4 font-bold text-err">Run invalidée : la fenêtre a perdu le focus trop longtemps.</p>
            <button
              type="button"
              onClick={reset}
              className="rounded-xl bg-accent px-6 py-2 font-bold text-white"
            >
              Nouvelle run
            </button>
          </div>
        ) : (
          typing && (
            <>
              {status === 'paused' && (
                <p className="text-center text-sm text-muted">En pause — clique dans le texte pour reprendre.</p>
              )}
              <TypingArea state={typing} disabled={status !== 'running'} onChar={handleChar} onBackspace={backspace} />
            </>
          )
        )}
      </div>
    );
  }

  return (
    <div className="mx-auto flex max-w-xl flex-col gap-8">
      <h1 className="text-2xl font-bold">Entraînement libre</h1>

      <section>
        <p className="mb-2 text-sm font-semibold text-muted">Langue</p>
        <div className="flex gap-2">
          {(['fr', 'en'] as const).map((l) => (
            <button
              key={l}
              type="button"
              onClick={() => setLanguage(l)}
              className={`rounded-xl border px-4 py-2 font-bold transition-colors ${
                language === l ? 'border-accent bg-accent/15 text-text' : 'border-line text-muted'
              }`}
            >
              {l === 'fr' ? 'Français' : 'English'}
            </button>
          ))}
        </div>
      </section>

      <section>
        <p className="mb-2 text-sm font-semibold text-muted">Longueur</p>
        <div className="flex flex-wrap gap-2">
          {LENGTHS.map(({ value, label }) => (
            <button
              key={value}
              type="button"
              onClick={() => setLength(value)}
              className={`rounded-xl border px-4 py-2 transition-colors ${
                length === value ? 'border-accent bg-accent/15 text-text' : 'border-line text-muted'
              }`}
            >
              {label}
            </button>
          ))}
        </div>
      </section>

      <section>
        <p className="mb-2 text-sm font-semibold text-muted">Difficulté</p>
        <div className="flex flex-col gap-3">
          {TOGGLES.map(({ key: k, label }) => (
            <div key={k} className="flex items-center justify-between rounded-xl border border-line bg-surface px-4 py-2">
              <span>{label}</span>
              <Toggle
                checked={options[k]}
                onChange={(v) => setOptions((o) => ({ ...o, [k]: v }))}
                label={label}
              />
            </div>
          ))}
        </div>
      </section>

      <button
        type="button"
        onClick={begin}
        className="rounded-xl bg-accent px-8 py-3 text-lg font-bold text-white transition-opacity hover:opacity-90"
      >
        Démarrer
      </button>
    </div>
  );
}
```

- [ ] **Step 6: Vérifier que les tests passent**

Run: `npx vitest run src/ui/`
Expected: tous les tests PASS

- [ ] **Step 7: Commit**

```bash
git add src/ui/components/RunHud.tsx src/ui/components/ResultsScreen.tsx src/ui/components/Sparkline.tsx src/ui/pages/PlayPage.tsx src/ui/components/Sparkline.test.tsx src/ui/pages/PlayPage.test.tsx
git commit -m "feat(play): free mode page with hud, results screen and sparkline"
```

---

### Task 13: Page Challenger (ranked par langue)

**Files:**
- Modify: `src/ui/pages/ChallengerPage.tsx` (remplace le stub)
- Test: `src/ui/pages/ChallengerPage.test.tsx`

**Interfaces:**
- Consumes: `getOfficialTexts` (tâche 5) ; `getProgress`, `ChallengerProgress` (tâche 7) ; `nextTier`, `TIER_THRESHOLDS`, `tierForPoints` (tâche 6) ; `useRunStore`, `useFocusGuard`, `TypingArea`, `RunHud`, `ResultsScreen`, `TierBadge`, `tierLabel` (tâches 10-12) ; `ALL_OPTIONS_ON` (tâche 4).
- Produces: page `/challenger` complète (aucune interface réutilisée ailleurs).

Comportement : vue ligue = sélecteur de langue + carte (badge tier ou « Non classé », total, barre de progression vers le tier suivant) + liste des 10 textes officiels avec meilleur score par texte et bouton « Jouer ». Une run Challenger démarre **toujours** avec `ALL_OPTIONS_ON`. Fin de run → `ResultsScreen` ; si `tierUp`, toast « Nouveau tier : X » + `playSuccess()`. La progression se recharge après chaque résultat.

- [ ] **Step 1: Écrire le test qui échoue**

`src/ui/pages/ChallengerPage.test.tsx` :

```tsx
import 'fake-indexeddb/auto';
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { beforeEach, describe, expect, it } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import { db } from '@/db/db';
import { recordChallengerResult } from '@/db/challengerRepo';
import { useRunStore } from '@/state/runStore';
import { ChallengerPage } from './ChallengerPage';

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
  useRunStore.getState().reset();
});

describe('ChallengerPage', () => {
  it('affiche la ligue fr non classée puis les 10 textes officiels', async () => {
    render(<MemoryRouter><ChallengerPage /></MemoryRouter>);
    expect(await screen.findByText('Non classé')).toBeInTheDocument();
    await waitFor(() => expect(screen.getAllByRole('button', { name: 'Jouer' })).toHaveLength(10));
  });

  it('affiche le tier et le total après des résultats', async () => {
    await recordChallengerResult('fr', 'fr-101', 60, Date.now());
    await recordChallengerResult('fr', 'fr-102', 55, Date.now());
    render(<MemoryRouter><ChallengerPage /></MemoryRouter>);
    expect(await screen.findByText('Bronze')).toBeInTheDocument();
    expect(screen.getAllByText(/115/).length).toBeGreaterThan(0);
    expect(screen.getByText(/Bronze atteint le/)).toBeInTheDocument();
  });

  it('démarre une run challenger avec tous les toggles actifs', async () => {
    render(<MemoryRouter><ChallengerPage /></MemoryRouter>);
    const buttons = await screen.findAllByRole('button', { name: 'Jouer' });
    await userEvent.click(buttons[0]);
    expect(await screen.findByTestId('typing-area')).toBeInTheDocument();
    const config = useRunStore.getState().config;
    expect(config?.mode).toBe('challenger');
    expect(config?.textId).toBe('fr-101');
    expect(config?.options).toEqual({ punctuation: true, specialChars: true, digits: true, accents: true });
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/ui/pages/ChallengerPage.test.tsx`
Expected: FAIL (« Non classé » introuvable dans le stub)

- [ ] **Step 3: Implémenter la page Challenger**

`src/ui/pages/ChallengerPage.tsx` :

```tsx
import { useCallback, useEffect, useState } from 'react';
import { useRunStore } from '@/state/runStore';
import { useToasts } from '@/state/toastStore';
import { useSettings } from '@/state/settingsStore';
import { useFocusGuard } from '@/ui/hooks/useFocusGuard';
import { TypingArea } from '@/ui/components/TypingArea';
import { RunHud } from '@/ui/components/RunHud';
import { ResultsScreen } from '@/ui/components/ResultsScreen';
import { TierBadge, tierLabel } from '@/ui/components/TierBadge';
import { playError, playKey, playSuccess } from '@/ui/sounds';
import { getOfficialTexts } from '@/texts/corpus';
import { ALL_OPTIONS_ON, applyOptions } from '@/texts/normalize';
import { getProgress } from '@/db/challengerRepo';
import type { ChallengerProgress } from '@/db/types';
import { nextTier, TIER_THRESHOLDS } from '@/scoring/league';
import type { Language } from '@/texts/types';

const dateFmt = new Intl.DateTimeFormat('fr-FR', { dateStyle: 'medium' });

function LeagueCard({ progress }: { progress: ChallengerProgress }) {
  const next = nextTier(progress.tier);
  const base = progress.tier ? TIER_THRESHOLDS[progress.tier] : 0;
  const pct = next ? Math.min(100, Math.round(((progress.total - base) / (next.threshold - base)) * 100)) : 100;
  return (
    <div className="flex flex-col gap-3 rounded-2xl border border-line bg-surface p-6 backdrop-blur">
      <div className="flex items-center justify-between">
        {progress.tier ? <TierBadge tier={progress.tier} /> : (
          <span className="rounded-full border border-line px-3 py-0.5 text-xs font-bold uppercase text-muted">
            Non classé
          </span>
        )}
        <p className="font-type text-3xl font-extrabold">{progress.total} <span className="text-sm text-muted">pts</span></p>
      </div>
      {next && (
        <div>
          <div className="h-2 overflow-hidden rounded-full bg-line">
            <div className="h-full rounded-full bg-accent transition-all" style={{ width: `${pct}%` }} />
          </div>
          <p className="mt-1 text-xs text-muted">
            {progress.total} / {next.threshold} pts pour {tierLabel(next.tier)}
          </p>
        </div>
      )}
      {progress.tierHistory.length > 0 && (
        <ul className="flex flex-col gap-1 border-t border-line pt-2 text-xs text-muted">
          {progress.tierHistory.map((h) => (
            <li key={h.at}>
              {tierLabel(h.tier)} atteint le {dateFmt.format(h.at)}
            </li>
          ))}
        </ul>
      )}
    </div>
  );
}

export function ChallengerPage() {
  const { status, typing, result, start, key, backspace, reset } = useRunStore();
  const profile = useSettings((s) => s.profile);
  const push = useToasts((s) => s.push);
  const [language, setLanguage] = useState<Language>(profile.defaultLanguage);
  const [progress, setProgress] = useState<ChallengerProgress | null>(null);
  useFocusGuard();

  const refresh = useCallback(() => {
    void getProgress(language).then(setProgress);
  }, [language]);
  useEffect(refresh, [refresh]);

  useEffect(() => {
    if (!result) return;
    refresh();
    result.newAchievements.forEach((a) =>
      push({ title: a.title, description: a.description, kind: 'achievement' }),
    );
    if (result.tierUp) {
      push({ title: `Nouveau tier : ${tierLabel(result.tierUp)}`, kind: 'info' });
      playSuccess();
    }
  }, [result, refresh, push]);

  const play = (textId: string) => {
    const entry = getOfficialTexts(language).find((t) => t.id === textId);
    if (!entry) return;
    const text = applyOptions(entry.text, ALL_OPTIONS_ON);
    start({ mode: 'challenger', language, textId, options: ALL_OPTIONS_ON }, text);
  };

  const handleChar = (c: string) => {
    const t = useRunStore.getState().typing;
    if (t && profile.sounds) {
      if (t.text[t.cursor] === c) playKey();
      else playError();
    }
    key(c);
  };

  if (status === 'finished' && result) {
    return <ResultsScreen result={result} onReplay={() => result.run && play(result.run.textId)} onExit={reset} />;
  }

  if (status === 'running' || status === 'paused' || status === 'invalidated') {
    return (
      <div className="flex flex-col gap-6">
        {typing && <RunHud typing={typing} />}
        {status === 'invalidated' ? (
          <div className="rounded-xl border border-err/40 bg-err/10 p-6 text-center">
            <p className="mb-4 font-bold text-err">Run classée invalidée : perte de focus trop longue.</p>
            <button type="button" onClick={reset} className="rounded-xl bg-accent px-6 py-2 font-bold text-white">
              Retour à la ligue
            </button>
          </div>
        ) : (
          typing && (
            <>
              {status === 'paused' && (
                <p className="text-center text-sm text-muted">En pause — clique dans le texte pour reprendre.</p>
              )}
              <TypingArea state={typing} disabled={status !== 'running'} onChar={handleChar} onBackspace={backspace} />
            </>
          )
        )}
      </div>
    );
  }

  const texts = getOfficialTexts(language);
  return (
    <div className="flex flex-col gap-6">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">Mode Challenger</h1>
        <div className="flex gap-2">
          {(['fr', 'en'] as const).map((l) => (
            <button
              key={l}
              type="button"
              onClick={() => setLanguage(l)}
              className={`rounded-xl border px-4 py-1.5 font-bold transition-colors ${
                language === l ? 'border-accent bg-accent/15 text-text' : 'border-line text-muted'
              }`}
            >
              {l === 'fr' ? 'Français' : 'English'}
            </button>
          ))}
        </div>
      </div>

      {progress && <LeagueCard progress={progress} />}

      <ul className="flex flex-col gap-2">
        {texts.map((t, i) => (
          <li
            key={t.id}
            className="flex items-center gap-4 rounded-xl border border-line bg-surface px-4 py-3 backdrop-blur"
          >
            <span className="w-6 text-center font-type font-bold text-muted">{i + 1}</span>
            <p className="flex-1 truncate text-sm text-muted">{t.text.slice(0, 80)}…</p>
            <span className="w-20 text-right font-type font-bold">
              {progress?.bestByText[t.id] ?? 0} <span className="text-xs text-muted">pts</span>
            </span>
            <button
              type="button"
              onClick={() => play(t.id)}
              className="rounded-lg bg-accent px-4 py-1.5 text-sm font-bold text-white transition-opacity hover:opacity-90"
            >
              Jouer
            </button>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/ui/pages/ChallengerPage.test.tsx`
Expected: 3 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/ui/pages/ChallengerPage.tsx src/ui/pages/ChallengerPage.test.tsx
git commit -m "feat(challenger): ranked league page with tier progression"
```

---

### Task 14: Page Classements

**Files:**
- Modify: `src/ui/pages/LeaderboardPage.tsx` (remplace le stub)
- Test: `src/ui/pages/LeaderboardPage.test.tsx`

**Interfaces:**
- Consumes: `topRuns`, `personalBests`, `rankFor`, `RunRecord`, `GameMode` (tâche 7) ; `getTexts`, `getOfficialTexts` (tâche 5) ; `Language` (tâche 4).
- Produces: page `/leaderboard` complète (aucune interface réutilisée ailleurs).

Comportement : filtres mode / langue / texte (« Tous les textes » par défaut ; liste = textes libres en mode free, officiels en challenger) ; tableau top 10 (Rang, WPM, Précision, Points, Temps, Date) ; si un texte est choisi et que la meilleure run du joueur dépasse le top 10, ligne « Ta meilleure run : rang N » ; cartes records personnels (meilleur WPM, meilleure précision ≥ 10 s, plus longue run). Dates via `Intl.DateTimeFormat('fr-FR', { dateStyle: 'short', timeStyle: 'short' })`.

- [ ] **Step 1: Écrire le test qui échoue**

`src/ui/pages/LeaderboardPage.test.tsx` :

```tsx
import 'fake-indexeddb/auto';
import { render, screen } from '@testing-library/react';
import { beforeEach, describe, expect, it } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import { db } from '@/db/db';
import { saveRun } from '@/db/runsRepo';
import { LeaderboardPage } from './LeaderboardPage';

const opts = { punctuation: true, specialChars: true, digits: true, accents: true };

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
  await saveRun({
    date: Date.now(), mode: 'free', language: 'fr', textId: 'fr-001', options: opts,
    durationMs: 25_000, wpm: 62.5, accuracy: 0.98, points: 88, errors: 2,
    backspaces: 1, chars: 260, noBackspace: false,
  });
});

describe('LeaderboardPage', () => {
  it('affiche les runs et les records personnels', async () => {
    render(<MemoryRouter><LeaderboardPage /></MemoryRouter>);
    expect(await screen.findByText('62.5')).toBeInTheDocument();
    expect(screen.getByText('98 %')).toBeInTheDocument();
    expect(screen.getByText('Meilleur WPM')).toBeInTheDocument();
  });

  it('affiche un état vide sans runs', async () => {
    await Promise.all(db.tables.map((t) => t.clear()));
    render(<MemoryRouter><LeaderboardPage /></MemoryRouter>);
    expect(await screen.findByText(/Aucune run enregistrée/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/ui/pages/LeaderboardPage.test.tsx`
Expected: FAIL (stub sans tableau)

- [ ] **Step 3: Implémenter la page Classements**

`src/ui/pages/LeaderboardPage.tsx` :

```tsx
import { useEffect, useState } from 'react';
import { personalBests, rankFor, topRuns } from '@/db/runsRepo';
import type { GameMode, RunRecord } from '@/db/types';
import { getOfficialTexts, getTexts } from '@/texts/corpus';
import type { Language } from '@/texts/types';

const dateFmt = new Intl.DateTimeFormat('fr-FR', { dateStyle: 'short', timeStyle: 'short' });

function BestCard({ label, value, sub }: { label: string; value: string; sub: string }) {
  return (
    <div className="rounded-xl border border-line bg-surface p-4 backdrop-blur">
      <p className="text-xs uppercase tracking-wide text-muted">{label}</p>
      <p className="font-type text-2xl font-extrabold">{value}</p>
      <p className="text-xs text-muted">{sub}</p>
    </div>
  );
}

export function LeaderboardPage() {
  const [mode, setMode] = useState<GameMode>('free');
  const [language, setLanguage] = useState<Language>('fr');
  const [textId, setTextId] = useState('');
  const [rows, setRows] = useState<RunRecord[]>([]);
  const [myRank, setMyRank] = useState<number | null>(null);
  const [bests, setBests] = useState<Awaited<ReturnType<typeof personalBests>> | null>(null);

  const textChoices = mode === 'challenger' ? getOfficialTexts(language) : getTexts(language);

  useEffect(() => {
    void (async () => {
      const list = await topRuns({ mode, language, textId: textId || undefined });
      setRows(list);
      setBests(await personalBests());
      if (textId) {
        const all = await topRuns({ mode, language, textId }, 1000);
        const best = mode === 'challenger'
          ? Math.max(0, ...all.map((r) => r.points))
          : Math.max(0, ...all.map((r) => r.wpm));
        setMyRank(await rankFor({ mode, language, textId }, best));
      } else {
        setMyRank(null);
      }
    })();
  }, [mode, language, textId]);

  return (
    <div className="flex flex-col gap-6">
      <h1 className="text-2xl font-bold">Classements</h1>

      {bests && (
        <div className="grid grid-cols-3 gap-3">
          <BestCard
            label="Meilleur WPM"
            value={bests.bestWpm ? bests.bestWpm.wpm.toFixed(1) : '—'}
            sub={bests.bestWpm ? dateFmt.format(bests.bestWpm.date) : 'aucune run'}
          />
          <BestCard
            label="Meilleure précision"
            value={bests.bestAccuracy ? `${(bests.bestAccuracy.accuracy * 100).toFixed(1)} %` : '—'}
            sub={bests.bestAccuracy ? dateFmt.format(bests.bestAccuracy.date) : 'runs ≥ 10 s'}
          />
          <BestCard
            label="Plus longue run"
            value={bests.longestRun ? `${bests.longestRun.chars} car.` : '—'}
            sub={bests.longestRun ? dateFmt.format(bests.longestRun.date) : 'aucune run'}
          />
        </div>
      )}

      <div className="flex flex-wrap gap-2">
        {(['free', 'challenger'] as const).map((m) => (
          <button
            key={m}
            type="button"
            onClick={() => { setMode(m); setTextId(''); }}
            className={`rounded-xl border px-4 py-1.5 font-bold transition-colors ${
              mode === m ? 'border-accent bg-accent/15 text-text' : 'border-line text-muted'
            }`}
          >
            {m === 'free' ? 'Libre' : 'Challenger'}
          </button>
        ))}
        {(['fr', 'en'] as const).map((l) => (
          <button
            key={l}
            type="button"
            onClick={() => { setLanguage(l); setTextId(''); }}
            className={`rounded-xl border px-4 py-1.5 font-bold transition-colors ${
              language === l ? 'border-accent bg-accent/15 text-text' : 'border-line text-muted'
            }`}
          >
            {l === 'fr' ? 'Français' : 'English'}
          </button>
        ))}
        <select
          aria-label="Texte"
          value={textId}
          onChange={(e) => setTextId(e.target.value)}
          className="rounded-xl border border-line bg-bg px-3 py-1.5 text-sm"
        >
          <option value="">Tous les textes</option>
          {textChoices.map((t) => (
            <option key={t.id} value={t.id}>{t.id} — {t.text.slice(0, 40)}…</option>
          ))}
        </select>
      </div>

      {rows.length === 0 ? (
        <p className="rounded-xl border border-line bg-surface p-6 text-center text-muted">
          Aucune run enregistrée pour ces filtres. À toi de jouer !
        </p>
      ) : (
        <div className="overflow-x-auto rounded-xl border border-line">
          <table className="w-full text-left text-sm">
            <thead className="bg-surface text-xs uppercase tracking-wide text-muted">
              <tr>
                <th className="px-4 py-2">Rang</th>
                <th className="px-4 py-2">WPM</th>
                <th className="px-4 py-2">Précision</th>
                <th className="px-4 py-2">Points</th>
                <th className="px-4 py-2">Temps</th>
                <th className="px-4 py-2">Texte</th>
                <th className="px-4 py-2">Date</th>
              </tr>
            </thead>
            <tbody>
              {rows.map((r, i) => (
                <tr key={r.id} className="border-t border-line">
                  <td className="px-4 py-2 font-type font-bold">{i + 1}</td>
                  <td className="px-4 py-2 font-type">{r.wpm.toFixed(1)}</td>
                  <td className="px-4 py-2">{Math.round(r.accuracy * 100)} %</td>
                  <td className="px-4 py-2 font-type">{r.points}</td>
                  <td className="px-4 py-2">{(r.durationMs / 1000).toFixed(1)} s</td>
                  <td className="px-4 py-2 text-muted">{r.textId}</td>
                  <td className="px-4 py-2 text-muted">{dateFmt.format(r.date)}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}

      {myRank !== null && myRank > 10 && (
        <p className="text-sm text-muted">Ta meilleure run sur ce texte : rang {myRank}.</p>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/ui/pages/LeaderboardPage.test.tsx`
Expected: 2 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/ui/pages/LeaderboardPage.tsx src/ui/pages/LeaderboardPage.test.tsx
git commit -m "feat(leaderboard): local rankings with filters and personal records"
```

---

### Task 15: Page Succès

**Files:**
- Modify: `src/ui/pages/AchievementsPage.tsx` (remplace le stub)
- Test: `src/ui/pages/AchievementsPage.test.tsx`

**Interfaces:**
- Consumes: `ACHIEVEMENTS`, `AchievementDef`, `AchievementCategory`, `AchievementContext` (tâche 8) ; `computeStreak` (tâche 8) ; `allRuns`, `getProgress`, `db`, `RunRecord` (tâche 7) ; `Language` (tâche 4).
- Produces: page `/achievements` complète.

Comportement : construit un `AchievementContext` à partir des runs stockées (`newRun` = dernière run, ou une run synthétique à zéro si aucune — `isUnlocked` n'est jamais appelé ici, seuls `progress` et les ids débloqués en DB comptent) ; groupes par catégorie (Vitesse, Précision, Volume, Challenger, Fun) ; carte par succès : débloqué = bordure accent + date, verrouillé = grisé + barre de progression si `target` et `progress` définis. Compteur global « X / 25 ».

- [ ] **Step 1: Écrire le test qui échoue**

`src/ui/pages/AchievementsPage.test.tsx` :

```tsx
import 'fake-indexeddb/auto';
import { render, screen } from '@testing-library/react';
import { beforeEach, describe, expect, it } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import { db } from '@/db/db';
import { AchievementsPage } from './AchievementsPage';

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
  await db.achievements.add({ id: 'wpm-40', unlockedAt: Date.now() });
});

describe('AchievementsPage', () => {
  it('affiche les 25 succès et le compteur de déblocage', async () => {
    render(<MemoryRouter><AchievementsPage /></MemoryRouter>);
    expect(await screen.findByText('1 / 25')).toBeInTheDocument();
    expect(screen.getByText('Échauffement')).toBeInTheDocument();
    expect(screen.getByText('Inhumain')).toBeInTheDocument();
    expect(screen.getByText('Challenger')).toBeInTheDocument();
  });

  it('affiche la progression des succès verrouillés', async () => {
    await db.runs.add({
      date: Date.now(), mode: 'free', language: 'fr', textId: 'fr-001',
      options: { punctuation: true, specialChars: true, digits: true, accents: true },
      durationMs: 20_000, wpm: 45, accuracy: 0.99, points: 62, errors: 1,
      backspaces: 0, chars: 200, noBackspace: true,
    });
    render(<MemoryRouter><AchievementsPage /></MemoryRouter>);
    // sharpshooter-10 (1 run ≥ 98%) et runs-10 (1 run) affichent tous deux « 1 / 10 »
    expect((await screen.findAllByText('1 / 10')).length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/ui/pages/AchievementsPage.test.tsx`
Expected: FAIL (stub)

- [ ] **Step 3: Implémenter la page Succès**

`src/ui/pages/AchievementsPage.tsx` :

```tsx
import { useEffect, useState } from 'react';
import {
  ACHIEVEMENTS, type AchievementCategory, type AchievementContext, type AchievementDef,
} from '@/achievements/definitions';
import { computeStreak } from '@/achievements/check';
import { db } from '@/db/db';
import { allRuns } from '@/db/runsRepo';
import { getProgress } from '@/db/challengerRepo';
import type { RunRecord } from '@/db/types';

const CATEGORIES: { id: AchievementCategory; label: string }[] = [
  { id: 'vitesse', label: 'Vitesse' },
  { id: 'precision', label: 'Précision' },
  { id: 'volume', label: 'Volume' },
  { id: 'challenger', label: 'Challenger' },
  { id: 'fun', label: 'Fun' },
];

const dateFmt = new Intl.DateTimeFormat('fr-FR', { dateStyle: 'medium' });

const SYNTHETIC_RUN: RunRecord = {
  date: 0, mode: 'free', language: 'fr', textId: '',
  options: { punctuation: true, specialChars: true, digits: true, accents: true },
  durationMs: 0, wpm: 0, accuracy: 1, points: 0, errors: 0, backspaces: 0, chars: 0, noBackspace: false,
};

function AchievementCard({ def, unlockedAt, ctx }: {
  def: AchievementDef;
  unlockedAt: number | null;
  ctx: AchievementContext;
}) {
  const progress = !unlockedAt && def.target && def.progress ? def.progress(ctx) : null;
  const pct = progress !== null && def.target ? Math.min(100, Math.round((progress / def.target) * 100)) : null;
  return (
    <li
      className={`rounded-xl border p-4 backdrop-blur ${
        unlockedAt ? 'border-accent/50 bg-accent/10' : 'border-line bg-surface opacity-80'
      }`}
    >
      <p className="font-bold">{def.title}</p>
      <p className="text-sm text-muted">{def.description}</p>
      {unlockedAt ? (
        <p className="mt-2 text-xs text-accent">Débloqué le {dateFmt.format(unlockedAt)}</p>
      ) : (
        progress !== null && def.target && (
          <div className="mt-2">
            <div className="h-1.5 overflow-hidden rounded-full bg-line">
              <div className="h-full rounded-full bg-accent" style={{ width: `${pct}%` }} />
            </div>
            <p className="mt-1 text-xs text-muted">{progress} / {def.target}</p>
          </div>
        )
      )}
    </li>
  );
}

export function AchievementsPage() {
  const [ctx, setCtx] = useState<AchievementContext | null>(null);
  const [unlocked, setUnlocked] = useState<Map<string, number>>(new Map());

  useEffect(() => {
    void (async () => {
      const [runs, unlocks, fr, en] = await Promise.all([
        allRuns(), db.achievements.toArray(), getProgress('fr'), getProgress('en'),
      ]);
      setUnlocked(new Map(unlocks.map((u) => [u.id, u.unlockedAt])));
      const sorted = runs.slice().sort((a, b) => a.date - b.date);
      setCtx({
        newRun: sorted[sorted.length - 1] ?? SYNTHETIC_RUN,
        runs,
        totalChars: runs.reduce((s, r) => s + r.chars, 0),
        streakDays: computeStreak(runs.map((r) => r.date), Date.now()),
        progress: { fr, en },
        now: Date.now(),
      });
    })();
  }, []);

  return (
    <div className="flex flex-col gap-8">
      <div className="flex items-baseline gap-4">
        <h1 className="text-2xl font-bold">Succès</h1>
        <p className="font-type text-lg text-muted">{unlocked.size} / {ACHIEVEMENTS.length}</p>
      </div>
      {CATEGORIES.map(({ id, label }) => (
        <section key={id}>
          <h2 className="mb-3 text-sm font-semibold uppercase tracking-wide text-muted">{label}</h2>
          <ul className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
            {ACHIEVEMENTS.filter((a) => a.category === id).map((a) => (
              ctx && (
                <AchievementCard
                  key={a.id}
                  def={a}
                  unlockedAt={unlocked.get(a.id) ?? null}
                  ctx={ctx}
                />
              )
            ))}
          </ul>
        </section>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/ui/pages/AchievementsPage.test.tsx`
Expected: 2 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/ui/pages/AchievementsPage.tsx src/ui/pages/AchievementsPage.test.tsx
git commit -m "feat(achievements): achievements page with progress bars"
```

---

### Task 16: Page Stats

**Files:**
- Create: `src/ui/pages/statsUtils.ts`
- Modify: `src/ui/pages/StatsPage.tsx` (remplace le stub)
- Test: `src/ui/pages/statsUtils.test.ts`

**Interfaces:**
- Consumes: `allRuns`, `RunRecord` (tâche 7) ; `computeStreak` (tâche 8) ; `Sparkline` (tâche 12).
- Produces: `interface DayStat { day: string; avgWpm: number; avgAccuracy: number; runs: number }` ; `dailyAverages(runs: RunRecord[], days?: number, now?: number): DayStat[]` ; page `/stats` complète.

Comportement : cartes (runs totales, caractères tapés, WPM moyen, précision moyenne, série de jours en cours) + 2 sparklines sur les 30 derniers jours (WPM moyen/jour, précision moyenne/jour ; jours sans run = 0). `dailyAverages` : un seau par jour calendaire local sur `days` jours se terminant à `now`, étiquette `JJ/MM`, tri chronologique.

- [ ] **Step 1: Écrire le test qui échoue**

`src/ui/pages/statsUtils.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import { dailyAverages } from './statsUtils';
import type { RunRecord } from '@/db/types';

const opts = { punctuation: true, specialChars: true, digits: true, accents: true };

function run(date: number, wpm: number, accuracy: number): RunRecord {
  return {
    date, mode: 'free', language: 'fr', textId: 'fr-001', options: opts,
    durationMs: 30_000, wpm, accuracy, points: 0, errors: 0, backspaces: 0, chars: 100, noBackspace: true,
  };
}

describe('dailyAverages', () => {
  const now = new Date(2026, 5, 15, 20, 0).getTime(); // 15 juin 2026

  it('produit un seau par jour, 0 les jours sans run', () => {
    const stats = dailyAverages([], 3, now);
    expect(stats).toHaveLength(3);
    expect(stats.map((s) => s.runs)).toEqual([0, 0, 0]);
    expect(stats[2].day).toBe('15/06');
    expect(stats[0].day).toBe('13/06');
  });

  it('moyenne les runs du jour', () => {
    const morning = new Date(2026, 5, 15, 8, 0).getTime();
    const stats = dailyAverages([run(morning, 40, 0.9), run(morning, 60, 1)], 3, now);
    expect(stats[2].runs).toBe(2);
    expect(stats[2].avgWpm).toBeCloseTo(50);
    expect(stats[2].avgAccuracy).toBeCloseTo(0.95);
  });

  it('ignore les runs plus vieilles que la fenêtre', () => {
    const old = new Date(2026, 4, 1, 8, 0).getTime();
    const stats = dailyAverages([run(old, 99, 1)], 30, now);
    expect(stats.every((s) => s.runs === 0)).toBe(true);
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/ui/pages/statsUtils.test.ts`
Expected: FAIL (module introuvable)

- [ ] **Step 3: Implémenter `statsUtils.ts`**

`src/ui/pages/statsUtils.ts` :

```ts
import type { RunRecord } from '@/db/types';

export interface DayStat {
  day: string;
  avgWpm: number;
  avgAccuracy: number;
  runs: number;
}

export function dailyAverages(runs: RunRecord[], days = 30, now = Date.now()): DayStat[] {
  const buckets = new Map<string, RunRecord[]>();
  const order: string[] = [];
  for (let i = days - 1; i >= 0; i--) {
    const d = new Date(now - i * 86_400_000);
    const key = `${String(d.getDate()).padStart(2, '0')}/${String(d.getMonth() + 1).padStart(2, '0')}`;
    buckets.set(key, []);
    order.push(key);
  }
  for (const r of runs) {
    const d = new Date(r.date);
    const key = `${String(d.getDate()).padStart(2, '0')}/${String(d.getMonth() + 1).padStart(2, '0')}`;
    if (buckets.has(key) && now - r.date < days * 86_400_000) buckets.get(key)!.push(r);
  }
  return order.map((key) => {
    const list = buckets.get(key)!;
    const mean = (f: (r: RunRecord) => number) =>
      list.length === 0 ? 0 : list.reduce((s, r) => s + f(r), 0) / list.length;
    return { day: key, avgWpm: mean((r) => r.wpm), avgAccuracy: mean((r) => r.accuracy), runs: list.length };
  });
}
```

Note : deux années différentes avec le même `JJ/MM` dans une fenêtre de 30 jours est impossible ; la clé jour/mois suffit.

- [ ] **Step 4: Implémenter la page Stats**

`src/ui/pages/StatsPage.tsx` :

```tsx
import { useEffect, useState } from 'react';
import { allRuns } from '@/db/runsRepo';
import type { RunRecord } from '@/db/types';
import { computeStreak } from '@/achievements/check';
import { Sparkline } from '@/ui/components/Sparkline';
import { dailyAverages } from './statsUtils';

function Card({ label, value }: { label: string; value: string }) {
  return (
    <div className="rounded-xl border border-line bg-surface p-4 backdrop-blur">
      <p className="text-xs uppercase tracking-wide text-muted">{label}</p>
      <p className="font-type text-2xl font-extrabold">{value}</p>
    </div>
  );
}

export function StatsPage() {
  const [runs, setRuns] = useState<RunRecord[] | null>(null);
  useEffect(() => {
    void allRuns().then(setRuns);
  }, []);

  if (!runs) return null;
  const totalChars = runs.reduce((s, r) => s + r.chars, 0);
  const mean = (f: (r: RunRecord) => number) =>
    runs.length === 0 ? 0 : runs.reduce((s, r) => s + f(r), 0) / runs.length;
  const streak = computeStreak(runs.map((r) => r.date), Date.now());
  const daily = dailyAverages(runs, 30);

  return (
    <div className="flex flex-col gap-8">
      <h1 className="text-2xl font-bold">Statistiques</h1>

      <div className="grid grid-cols-2 gap-3 sm:grid-cols-5">
        <Card label="Runs" value={String(runs.length)} />
        <Card label="Caractères" value={totalChars.toLocaleString('fr-FR')} />
        <Card label="WPM moyen" value={mean((r) => r.wpm).toFixed(1)} />
        <Card label="Précision moyenne" value={`${(mean((r) => r.accuracy) * 100).toFixed(1)} %`} />
        <Card label="Série" value={`${streak} j`} />
      </div>

      <section>
        <h2 className="mb-2 text-sm font-semibold uppercase tracking-wide text-muted">
          WPM moyen — 30 derniers jours
        </h2>
        <div className="rounded-xl border border-line bg-surface p-4 backdrop-blur">
          <Sparkline data={daily.map((d) => d.avgWpm)} width={760} height={80} />
        </div>
      </section>

      <section>
        <h2 className="mb-2 text-sm font-semibold uppercase tracking-wide text-muted">
          Précision moyenne — 30 derniers jours
        </h2>
        <div className="rounded-xl border border-line bg-surface p-4 backdrop-blur">
          <Sparkline data={daily.map((d) => d.avgAccuracy * 100)} width={760} height={80} />
        </div>
      </section>
    </div>
  );
}
```

- [ ] **Step 5: Vérifier que les tests passent + page compile**

Run: `npx vitest run src/ui/pages/statsUtils.test.ts && npm run build`
Expected: 3 tests PASS, build OK

- [ ] **Step 6: Commit**

```bash
git add src/ui/pages/statsUtils.ts src/ui/pages/statsUtils.test.ts src/ui/pages/StatsPage.tsx
git commit -m "feat(stats): stats page with daily averages sparklines"
```

---

### Task 17: Page Réglages

**Files:**
- Modify: `src/ui/pages/SettingsPage.tsx` (remplace le stub)
- Test: `src/ui/pages/SettingsPage.test.tsx`

**Interfaces:**
- Consumes: `useSettings` (tâche 10) ; `Toggle` (tâche 10) ; `db` (tâche 7) ; `Language` (tâche 4).
- Produces: page `/settings` complète + réinitialisation totale des données.

Comportement : pseudo (input, sauvegarde au blur, 1-30 car.) ; thème sombre/clair (Toggle) ; sons (Toggle) ; langue par défaut (boutons fr/en) ; délai d'invalidation focus (input nombre 1-60 s) ; zone danger : bouton « Réinitialiser toutes les données » → devient « Confirmer ? » pendant 3 s → `db.close()` puis `db.delete()` puis `location.reload()`.

- [ ] **Step 1: Écrire le test qui échoue**

`src/ui/pages/SettingsPage.test.tsx` :

```tsx
import 'fake-indexeddb/auto';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { beforeEach, describe, expect, it } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import { db } from '@/db/db';
import { useSettings } from '@/state/settingsStore';
import { getProfile } from '@/db/profileRepo';
import { SettingsPage } from './SettingsPage';

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
  await useSettings.getState().load();
});

describe('SettingsPage', () => {
  it('modifie le pseudo et persiste', async () => {
    render(<MemoryRouter><SettingsPage /></MemoryRouter>);
    const input = await screen.findByLabelText('Pseudo');
    await userEvent.clear(input);
    await userEvent.type(input, 'Ludo');
    await userEvent.tab(); // blur -> sauvegarde
    expect((await getProfile()).pseudo).toBe('Ludo');
    expect(useSettings.getState().profile.pseudo).toBe('Ludo');
  });

  it('bascule le thème', async () => {
    render(<MemoryRouter><SettingsPage /></MemoryRouter>);
    await userEvent.click(await screen.findByRole('switch', { name: 'Thème clair' }));
    expect(useSettings.getState().profile.theme).toBe('light');
    expect(document.documentElement.dataset.theme).toBe('light');
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/ui/pages/SettingsPage.test.tsx`
Expected: FAIL (stub)

- [ ] **Step 3: Implémenter la page Réglages**

`src/ui/pages/SettingsPage.tsx` :

```tsx
import { useState, type ReactNode } from 'react';
import { db } from '@/db/db';
import { useSettings } from '@/state/settingsStore';
import { Toggle } from '@/ui/components/Toggle';

function Row({ label, children }: { label: string; children: ReactNode }) {
  return (
    <div className="flex items-center justify-between gap-4 rounded-xl border border-line bg-surface px-4 py-3 backdrop-blur">
      <span className="text-sm font-semibold">{label}</span>
      {children}
    </div>
  );
}

export function SettingsPage() {
  const profile = useSettings((s) => s.profile);
  const update = useSettings((s) => s.update);
  const [pseudo, setPseudo] = useState(profile.pseudo);
  const [confirming, setConfirming] = useState(false);

  const savePseudo = () => {
    const clean = pseudo.trim().slice(0, 30);
    if (clean.length > 0 && clean !== profile.pseudo) void update({ pseudo: clean });
    else setPseudo(profile.pseudo);
  };

  const resetAll = async () => {
    if (!confirming) {
      setConfirming(true);
      setTimeout(() => setConfirming(false), 3000);
      return;
    }
    db.close();
    await db.delete();
    location.reload();
  };

  return (
    <div className="mx-auto flex max-w-xl flex-col gap-4">
      <h1 className="mb-2 text-2xl font-bold">Réglages</h1>

      <Row label="Pseudo">
        <input
          aria-label="Pseudo"
          value={pseudo}
          onChange={(e) => setPseudo(e.target.value)}
          onBlur={savePseudo}
          onKeyDown={(e) => e.key === 'Enter' && (e.target as HTMLInputElement).blur()}
          maxLength={30}
          className="w-40 rounded-lg border border-line bg-bg px-3 py-1.5 text-right"
        />
      </Row>

      <Row label="Thème clair">
        <Toggle
          checked={profile.theme === 'light'}
          onChange={(v) => void update({ theme: v ? 'light' : 'dark' })}
          label="Thème clair"
        />
      </Row>

      <Row label="Sons">
        <Toggle
          checked={profile.sounds}
          onChange={(v) => void update({ sounds: v })}
          label="Sons"
        />
      </Row>

      <Row label="Langue par défaut">
        <div className="flex gap-2">
          {(['fr', 'en'] as const).map((l) => (
            <button
              key={l}
              type="button"
              onClick={() => void update({ defaultLanguage: l })}
              className={`rounded-lg border px-3 py-1 text-sm font-bold transition-colors ${
                profile.defaultLanguage === l ? 'border-accent bg-accent/15 text-text' : 'border-line text-muted'
              }`}
            >
              {l === 'fr' ? 'Français' : 'English'}
            </button>
          ))}
        </div>
      </Row>

      <Row label="Invalidation après perte de focus (s)">
        <input
          aria-label="Délai d'invalidation"
          type="number"
          min={1}
          max={60}
          value={profile.focusTimeoutSec}
          onChange={(e) => {
            const v = Math.round(Number(e.target.value));
            if (v >= 1 && v <= 60) void update({ focusTimeoutSec: v });
          }}
          className="w-20 rounded-lg border border-line bg-bg px-3 py-1.5 text-right"
        />
      </Row>

      <div className="mt-6 rounded-xl border border-err/40 p-4">
        <p className="mb-3 text-sm font-bold text-err">Zone danger</p>
        <button
          type="button"
          onClick={() => void resetAll()}
          className={`rounded-xl px-4 py-2 text-sm font-bold transition-colors ${
            confirming ? 'bg-err text-white' : 'border border-err/50 text-err'
          }`}
        >
          {confirming ? 'Confirmer la suppression ?' : 'Réinitialiser toutes les données'}
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/ui/pages/SettingsPage.test.tsx`
Expected: 2 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/ui/pages/SettingsPage.tsx src/ui/pages/SettingsPage.test.tsx
git commit -m "feat(settings): settings page with theme, sounds, danger zone"
```

---

### Task 18: Page Accueil

**Files:**
- Modify: `src/ui/pages/HomePage.tsx` (remplace le stub)
- Test: `src/ui/pages/HomePage.test.tsx`

**Interfaces:**
- Consumes: `Link` (react-router-dom) ; `getProgress` (tâche 7) ; `allRuns`, `RunRecord` (tâche 7) ; `TierBadge` (tâche 10) ; `useSettings` (tâche 10).
- Produces: page `/` complète (aucune interface réutilisée ailleurs).

Comportement : hero (titre « Dacty », baseline « Le speedrun dactylo. », 2 CTA : « Entraînement libre » → `/play`, « Mode Challenger » → `/challenger`) ; cartes de ligue fr/en (badge tier ou « Non classé » + total) ; liste des 5 dernières runs (WPM, précision, mode, date). Salutation avec le pseudo du profil.

- [ ] **Step 1: Écrire le test qui échoue**

`src/ui/pages/HomePage.test.tsx` :

```tsx
import 'fake-indexeddb/auto';
import { render, screen } from '@testing-library/react';
import { beforeEach, describe, expect, it } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import { db } from '@/db/db';
import { saveRun } from '@/db/runsRepo';
import { HomePage } from './HomePage';

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
});

describe('HomePage', () => {
  it('affiche le hero et les deux accès aux modes', () => {
    render(<MemoryRouter><HomePage /></MemoryRouter>);
    expect(screen.getByRole('heading', { name: 'Dacty' })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: 'Entraînement libre' })).toHaveAttribute('href', '/play');
    expect(screen.getByRole('link', { name: 'Mode Challenger' })).toHaveAttribute('href', '/challenger');
  });

  it('affiche les dernières runs', async () => {
    await saveRun({
      date: Date.now(), mode: 'free', language: 'fr', textId: 'fr-001',
      options: { punctuation: true, specialChars: true, digits: true, accents: true },
      durationMs: 20_000, wpm: 55.4, accuracy: 0.97, points: 74, errors: 2,
      backspaces: 1, chars: 200, noBackspace: false,
    });
    render(<MemoryRouter><HomePage /></MemoryRouter>);
    expect(await screen.findByText(/55\.4/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/ui/pages/HomePage.test.tsx`
Expected: FAIL (stub)

- [ ] **Step 3: Implémenter la page Accueil**

`src/ui/pages/HomePage.tsx` :

```tsx
import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { motion } from 'framer-motion';
import { allRuns } from '@/db/runsRepo';
import { getProgress } from '@/db/challengerRepo';
import type { ChallengerProgress, RunRecord } from '@/db/types';
import { TierBadge } from '@/ui/components/TierBadge';
import { useSettings } from '@/state/settingsStore';
import type { Language } from '@/texts/types';

const dateFmt = new Intl.DateTimeFormat('fr-FR', { dateStyle: 'short', timeStyle: 'short' });

function LeagueMini({ label, progress }: { label: string; progress: ChallengerProgress | null }) {
  return (
    <div className="flex items-center justify-between rounded-xl border border-line bg-surface px-4 py-3 backdrop-blur">
      <span className="font-semibold">{label}</span>
      {progress?.tier ? <TierBadge tier={progress.tier} /> : (
        <span className="text-sm text-muted">Non classé</span>
      )}
      <span className="font-type font-bold">{progress?.total ?? 0} pts</span>
    </div>
  );
}

export function HomePage() {
  const pseudo = useSettings((s) => s.profile.pseudo);
  const [progress, setProgress] = useState<Record<Language, ChallengerProgress | null>>({ fr: null, en: null });
  const [recent, setRecent] = useState<RunRecord[]>([]);

  useEffect(() => {
    void getProgress('fr').then((p) => setProgress((s) => ({ ...s, fr: p })));
    void getProgress('en').then((p) => setProgress((s) => ({ ...s, en: p })));
    void allRuns().then((runs) =>
      setRecent(runs.sort((a, b) => b.date - a.date).slice(0, 5)),
    );
  }, []);

  return (
    <div className="flex flex-col gap-10">
      <motion.section
        initial={{ opacity: 0, y: 12 }}
        animate={{ opacity: 1, y: 0 }}
        className="flex flex-col items-center gap-6 py-10 text-center"
      >
        <h1 className="text-5xl font-extrabold tracking-tight">
          <span className="text-accent">Dacty</span>
        </h1>
        <p className="max-w-md text-lg text-muted">
          Le speedrun dactylo. Tape des textes le plus vite possible, grimpe la ligue, débloque les succès.
        </p>
        <div className="flex gap-3">
          <Link
            to="/play"
            className="rounded-xl bg-accent px-6 py-3 font-bold text-white transition-opacity hover:opacity-90"
          >
            Entraînement libre
          </Link>
          <Link
            to="/challenger"
            className="rounded-xl border border-line px-6 py-3 font-bold transition-colors hover:bg-surface"
          >
            Mode Challenger
          </Link>
        </div>
        <p className="text-sm text-muted">Bienvenue, {pseudo}.</p>
      </motion.section>

      <section className="grid gap-3 sm:grid-cols-2">
        <LeagueMini label="Ligue française" progress={progress.fr} />
        <LeagueMini label="Ligue anglaise" progress={progress.en} />
      </section>

      {recent.length > 0 && (
        <section>
          <h2 className="mb-3 text-sm font-semibold uppercase tracking-wide text-muted">Dernières runs</h2>
          <ul className="flex flex-col gap-2">
            {recent.map((r) => (
              <li
                key={r.id}
                className="flex items-center gap-4 rounded-xl border border-line bg-surface px-4 py-2 text-sm backdrop-blur"
              >
                <span className="font-type font-bold">{r.wpm.toFixed(1)} WPM</span>
                <span className="text-muted">{Math.round(r.accuracy * 100)} %</span>
                <span className="rounded-full border border-line px-2 py-0.5 text-xs text-muted">
                  {r.mode === 'free' ? 'Libre' : 'Challenger'} · {r.language.toUpperCase()}
                </span>
                <span className="ml-auto text-xs text-muted">{dateFmt.format(r.date)}</span>
              </li>
            ))}
          </ul>
        </section>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run src/ui/pages/HomePage.test.tsx`
Expected: 2 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/ui/pages/HomePage.tsx src/ui/pages/HomePage.test.tsx
git commit -m "feat(home): landing page with leagues overview and recent runs"
```

---

### Task 19: QA finale + README

**Files:**
- Create: `README.md`
- Modify: tout fichier nécessaire si un test ou le build casse

**Interfaces:**
- Consumes: tout le projet.

- [ ] **Step 1: Écrire le README**

`README.md` :

````markdown
# Dacty

Le speedrun dactylo : tape des textes le plus vite possible, grimpe la ligue Challenger, débloque les succès. 100 % local — tes données restent dans ton navigateur (IndexedDB).

## Lancer le projet

```bash
npm install
npm run dev        # http://localhost:5173
```

## Scripts

- `npm run dev` — serveur de dev
- `npm test` — tests unitaires (Vitest)
- `npm run build` — validation du corpus + build de production (`dist/`)
- `npm run validate:corpus` — valide les textes (longueurs, ids, ponctuation)

## Modes de jeu

- **Entraînement libre** (`/play`) : texte au choix (français/anglais, court/moyen/long/citation) avec toggles de difficulté (ponctuation, caractères spéciaux, chiffres, accents).
- **Challenger** (`/challenger`) : la ranked. 10 textes officiels par langue, tous les toggles forcés. Points = `WPM × précision² × 1,4`. Total des meilleurs scores → tiers : Bronze (100) → Argent (400) → Or (750) → Platine (950) → Diamant (1100) → Challenger (1300).
- **Succès** (`/achievements`) : 25 succès en 5 catégories.
- **Classements** (`/leaderboard`) : top 10 local par mode/langue/texte + records personnels.
- **Stats** (`/stats`) : WPM et précision moyens sur 30 jours, séries de jeu.

## Règles de frappe

- Le chrono démarre à la première touche. WPM = (caractères / 5) / minutes.
- Backspace autorisé, mais chaque erreur compte dans la précision finale, même corrigée. Il faut un texte 100 % exact pour finir.
- Collage bloqué. Perte de focus > 5 s (configurable) = run invalidée.

## Stack

Vite + React 19 + TypeScript strict, Tailwind CSS v4, Framer Motion, Zustand, Dexie (IndexedDB), Vitest + Testing Library. Le moteur de frappe (`src/engine/`) est pur TypeScript, sans dépendance React.

## Structure

- `src/engine/` — machine à états de frappe + stats (pur, testé)
- `src/texts/` — corpus fr/en (JSON) + normalisation des toggles
- `src/scoring/` — points Challenger + tiers de ligue
- `src/db/` — Dexie : runs, profil, succès, progression Challenger
- `src/achievements/` — 25 succès déclaratifs + moteur de déblocage
- `src/game/` — orchestration de fin de run
- `src/state/` — stores Zustand (run, settings, toasts)
- `src/ui/` — composants et pages
````

- [ ] **Step 2: Suite de tests complète**

Run: `npm test`
Expected: tous les tests PASS. Si un test casse : corriger la cause (jamais le test pour masquer un bug réel).

- [ ] **Step 3: Build de production**

Run: `npm run build`
Expected: validation du corpus OK, `tsc --noEmit` OK, `vite build` OK.

- [ ] **Step 4: Smoke manuel**

Run: `npm run dev`, puis vérifier dans le navigateur :
1. `/` s'affiche avec le hero et les CTA.
2. `/play` : démarrer une run, taper le texte, vérifier HUD live, caret animé, écran de résultats avec sparkline.
3. Rejouer avec toggles désactivés : le texte est normalisé.
4. `/challenger` : jouer un texte officiel, vérifier les points et la barre de ligue.
5. `/leaderboard`, `/achievements`, `/stats`, `/settings` : affichage correct, changement de thème persistant après reload.
6. Recharger la page : les données sont toujours là.

- [ ] **Step 5: Commit final**

```bash
git add -A
git commit -m "docs: readme with rules, scripts and project structure"
```

---

## Récapitulatif des tâches

| # | Tâche | Livrable testable |
|---|---|---|
| 1 | Scaffold | dev server + vitest verts |
| 2 | Engine état de frappe | 7 tests |
| 3 | Engine stats | 6 tests |
| 4 | Normalisation textes | 6 tests |
| 5 | Corpus + validateur | validateur + 5 tests |
| 6 | Scoring + ligue | 5 tests |
| 7 | DB Dexie | 10 tests |
| 8 | Achievements | 13 tests |
| 9 | Orchestration fin de run | 4 tests |
| 10 | Shell (router, stores, sons) | 4 tests + build |
| 11 | Run store + TypingArea | 9 tests |
| 12 | Page Entraînement | 4 tests |
| 13 | Page Challenger | 3 tests |
| 14 | Page Classements | 2 tests |
| 15 | Page Succès | 2 tests |
| 16 | Page Stats | 3 tests |
| 17 | Page Réglages | 2 tests |
| 18 | Page Accueil | 2 tests |
| 19 | QA + README | suite complète + build + smoke |
| 20 | Types 4 langages + zod (dev) | 3 tests |
| 21 | Corpus C/Python + validateur v2 | validateur + tests corpus |
| 22 | Multi-lignes + auto-indentation | 5 tests ajoutés |
| 23 | Page Dev + route + lien | 2 tests |
| 24 | 4 ligues + succès dev (28) + intégrations | suite complète |
| 25 | README + QA mode dev | suite + build + smoke dev |

---

## Extension — Mode Dev (tâches 20-25)

Ajout décidé après la rédaction initiale du plan (spec §4.7) : taper du code C et Python en speedrun, avec ligues Challenger par langage. Langages de code : toujours tapés bruts (pas de toggles, multiplicateur fixe ×1,4), multi-lignes avec `Entrée` = `\n` et auto-indentation.

### Task 20: Types — 4 langages, helper code, labels, schéma zod

**Files:**
- Modify: `src/texts/types.ts`, `src/db/schemas.ts`
- Test: `src/texts/types.test.ts` (nouveau)

**Interfaces:**
- Produces (consommé par tâches 21-24) :
  - `type Language = 'fr' | 'en' | 'c' | 'python'` (remplace l'union à 2 valeurs)
  - `CODE_LANGUAGES: readonly Language[]` = `['c', 'python']`
  - `isCodeLanguage(language: Language): boolean`
  - `LANGUAGE_LABELS: Record<Language, string>` = `{ fr: 'Français', en: 'English', c: 'C', python: 'Python' }`
  - `languageSchema` zod accepte les 4 valeurs

- [ ] **Step 1: Écrire le test qui échoue**

`src/texts/types.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import { CODE_LANGUAGES, isCodeLanguage, LANGUAGE_LABELS } from './types';
import { languageSchema } from '@/db/schemas';

describe('types', () => {
  it('identifie les langages de code', () => {
    expect(CODE_LANGUAGES).toEqual(['c', 'python']);
    expect(isCodeLanguage('c')).toBe(true);
    expect(isCodeLanguage('python')).toBe(true);
    expect(isCodeLanguage('fr')).toBe(false);
    expect(isCodeLanguage('en')).toBe(false);
  });

  it('a un label par langage', () => {
    expect(LANGUAGE_LABELS).toEqual({ fr: 'Français', en: 'English', c: 'C', python: 'Python' });
  });

  it('le schéma zod accepte les 4 langages', () => {
    for (const lang of ['fr', 'en', 'c', 'python']) {
      expect(languageSchema.safeParse(lang).success).toBe(true);
    }
    expect(languageSchema.safeParse('rust').success).toBe(false);
  });
});
```

- [ ] **Step 2: Vérifier que le test échoue**

Run: `npx vitest run src/texts/types.test.ts`
Expected: FAIL (exports manquants / schéma rejette 'c')

- [ ] **Step 3: Modifier les types et le schéma**

`src/texts/types.ts` — remplacer la ligne `Language` et ajouter les exports :

```ts
export type Language = 'fr' | 'en' | 'c' | 'python';
export type TextLength = 'short' | 'medium' | 'long';

export const CODE_LANGUAGES: readonly Language[] = ['c', 'python'];

export function isCodeLanguage(language: Language): boolean {
  return CODE_LANGUAGES.includes(language);
}

export const LANGUAGE_LABELS: Record<Language, string> = {
  fr: 'Français',
  en: 'English',
  c: 'C',
  python: 'Python',
};

export interface TextEntry {
  id: string;
  language: Language;
  length: TextLength;
  official: boolean;
  quote?: boolean;
  source?: string;
  text: string;
}

export interface TextOptions {
  punctuation: boolean;
  specialChars: boolean;
  digits: boolean;
  accents: boolean;
}
```

`src/db/schemas.ts` — remplacer :

```ts
const languageSchema = z.enum(['fr', 'en']);
```

par :

```ts
export const languageSchema = z.enum(['fr', 'en', 'c', 'python']);
```

(note : `languageSchema` devient exporté pour le test)

- [ ] **Step 4: Vérifier tests + compilation**

Run: `npx vitest run src/texts/types.test.ts src/db/ && npm run build`
Expected: tests PASS, build OK (le reste du code compile car les unions sont exhaustives via `Record<Language, ...>` déjà typés — si une erreur d'exhaustivité apparaît dans une page, elle sera traitée à la tâche 24 ; si le build échoue sur `LANGUAGE_LABELS` manquant dans une page existante, ne rien faire ici)

- [ ] **Step 5: Commit**

```bash
git add src/texts/types.ts src/texts/types.test.ts src/db/schemas.ts
git commit -m "feat(texts): four languages with code-language helpers"
```

---

### Task 21: Corpus dev C/Python + validateur v2

**Files:**
- Create: `src/texts/c.json`, `src/texts/python.json`
- Modify: `scripts/validateCorpus.mjs`, `src/texts/corpus.ts`, `src/texts/corpus.test.ts`

**Interfaces:**
- Consumes: `Language`, `isCodeLanguage` (tâche 20).
- Produces: corpus total = 130 textes (80 prose + 50 code) ; `getTexts('c')` = 15, `getOfficialTexts('python')` = 10 ; validateur avec règles prose/code distinctes.

Règles validateur v2 :
- Prose (fr, en) : inchangé (30 libres ids 001-030 dont 5 quotes, 10 officiels 101-110, fourchettes 110-200/320-480/650-950, officiels avec ponctuation).
- Code (c, python) : 15 libres ids 001-015 (0 quote), 10 officiels 101-110, fourchettes **short 60-300 / medium 280-800 / long 650-2500**, officiels multi-lignes (`\n` présent), **aucun** texte code avec `\t`, `\r` ou espace en fin de ligne.

- [ ] **Step 1: Écrire le corpus C**

`src/texts/c.json` (complet — 15 libres + 10 officiels ; `\n` = saut de ligne, `\\n` = le littéral C backslash-n) :

```json
{
  "language": "c",
  "texts": [
    { "id": "c-001", "length": "short", "official": false, "text": "#include <stdio.h>\n\nint main(void) {\n    printf(\"Hello, World!\\n\");\n    return 0;\n}" },
    { "id": "c-002", "length": "short", "official": false, "text": "#include <stdio.h>\n\nint main(void) {\n    int age = 36;\n    double pi = 3.14159;\n    printf(\"Age: %d, Pi: %.2f\\n\", age, pi);\n    return 0;\n}" },
    { "id": "c-003", "length": "short", "official": false, "text": "#include <stdio.h>\n\nint main(void) {\n    for (int i = 0; i < 5; i++) {\n        printf(\"%d squared is %d\\n\", i, i * i);\n    }\n    return 0;\n}" },
    { "id": "c-004", "length": "short", "official": false, "text": "#include <stdio.h>\n\nint main(void) {\n    int scores[] = {12, 7, 19, 4};\n    int total = 0;\n    for (int i = 0; i < 4; i++) {\n        total += scores[i];\n    }\n    printf(\"Total: %d\\n\", total);\n    return 0;\n}" },
    { "id": "c-005", "length": "short", "official": false, "text": "#include <stdio.h>\n\nint main(void) {\n    char name[] = \"Ada\";\n    printf(\"Name: %s (%zu letters)\\n\", name, sizeof(name) - 1);\n    return 0;\n}" },
    { "id": "c-006", "length": "medium", "official": false, "text": "#include <stdio.h>\n\nvoid swap(int *a, int *b) {\n    int temp = *a;\n    *a = *b;\n    *b = temp;\n}\n\nint main(void) {\n    int x = 3;\n    int y = 8;\n    printf(\"Before: x=%d, y=%d\\n\", x, y);\n    swap(&x, &y);\n    printf(\"After: x=%d, y=%d\\n\", x, y);\n    return 0;\n}" },
    { "id": "c-007", "length": "medium", "official": false, "text": "#include <stdio.h>\n\nlong factorial(int n) {\n    if (n <= 1) {\n        return 1;\n    }\n    return n * factorial(n - 1);\n}\n\nint main(void) {\n    for (int i = 0; i <= 10; i++) {\n        printf(\"%d! = %ld\\n\", i, factorial(i));\n    }\n    return 0;\n}" },
    { "id": "c-008", "length": "medium", "official": false, "text": "#include <stdio.h>\n\nstruct Point {\n    double x;\n    double y;\n};\n\ndouble distance_squared(struct Point a, struct Point b) {\n    double dx = b.x - a.x;\n    double dy = b.y - a.y;\n    return dx * dx + dy * dy;\n}\n\nint main(void) {\n    struct Point p1 = {0.0, 0.0};\n    struct Point p2 = {3.0, 4.0};\n    printf(\"Distance squared: %.1f\\n\", distance_squared(p1, p2));\n    return 0;\n}" },
    { "id": "c-009", "length": "medium", "official": false, "text": "#include <stdio.h>\n\nint string_length(const char *text) {\n    int length = 0;\n    while (text[length] != '\\0') {\n        length++;\n    }\n    return length;\n}\n\nint main(void) {\n    char message[] = \"Dactylo\";\n    printf(\"'%s' has %d characters\\n\", message, string_length(message));\n    return 0;\n}" },
    { "id": "c-010", "length": "medium", "official": false, "text": "#include <stdio.h>\n#include <stdlib.h>\n\nint main(void) {\n    int size = 5;\n    int *values = malloc(size * sizeof(int));\n    if (values == NULL) {\n        return 1;\n    }\n    for (int i = 0; i < size; i++) {\n        values[i] = i * 10;\n        printf(\"values[%d] = %d\\n\", i, values[i]);\n    }\n    free(values);\n    return 0;\n}" },
    { "id": "c-011", "length": "long", "official": false, "text": "#include <stdio.h>\n\nvoid bubble_sort(int array[], int size) {\n    for (int i = 0; i < size - 1; i++) {\n        for (int j = 0; j < size - 1 - i; j++) {\n            if (array[j] > array[j + 1]) {\n                int temp = array[j];\n                array[j] = array[j + 1];\n                array[j + 1] = temp;\n            }\n        }\n    }\n}\n\nvoid print_array(const int array[], int size) {\n    for (int i = 0; i < size; i++) {\n        printf(\"%d \", array[i]);\n    }\n    printf(\"\\n\");\n}\n\nint main(void) {\n    int values[] = {42, 7, 19, 3, 88, 14, 56, 1, 30, 23};\n    int size = sizeof(values) / sizeof(values[0]);\n    printf(\"Before: \");\n    print_array(values, size);\n    bubble_sort(values, size);\n    printf(\"After:  \");\n    print_array(values, size);\n    return 0;\n}" },
    { "id": "c-012", "length": "long", "official": false, "text": "#include <stdio.h>\n\nvoid fizzbuzz(int limit, int *fizz_count, int *buzz_count) {\n    for (int number = 1; number <= limit; number++) {\n        if (number % 15 == 0) {\n            printf(\"FizzBuzz\\n\");\n            (*fizz_count)++;\n            (*buzz_count)++;\n        } else if (number % 3 == 0) {\n            printf(\"Fizz\\n\");\n            (*fizz_count)++;\n        } else if (number % 5 == 0) {\n            printf(\"Buzz\\n\");\n            (*buzz_count)++;\n        } else {\n            printf(\"%d\\n\", number);\n        }\n    }\n}\n\nint main(void) {\n    int fizz = 0;\n    int buzz = 0;\n    fizzbuzz(100, &fizz, &buzz);\n    printf(\"Fizz: %d, Buzz: %d\\n\", fizz, buzz);\n    return 0;\n}" },
    { "id": "c-013", "length": "long", "official": false, "text": "#include <stdio.h>\n\nstruct Student {\n    char name[30];\n    int grades[5];\n};\n\ndouble average(const struct Student *student) {\n    int total = 0;\n    for (int i = 0; i < 5; i++) {\n        total += student->grades[i];\n    }\n    return total / 5.0;\n}\n\nint main(void) {\n    struct Student students[3] = {\n        {\"Ada\", {15, 18, 12, 19, 14}},\n        {\"Bob\", {9, 11, 13, 10, 12}},\n        {\"Eve\", {17, 16, 18, 20, 15}}\n    };\n    for (int i = 0; i < 3; i++) {\n        printf(\"%s: %.1f/20\\n\", students[i].name, average(&students[i]));\n    }\n    return 0;\n}" },
    { "id": "c-014", "length": "long", "official": false, "text": "#include <stdio.h>\n\nint string_length(const char *text) {\n    int length = 0;\n    while (text[length] != '\\0') {\n        length++;\n    }\n    return length;\n}\n\nvoid reverse(char *text) {\n    int length = string_length(text);\n    for (int i = 0; i < length / 2; i++) {\n        char temp = text[i];\n        text[i] = text[length - 1 - i];\n        text[length - 1 - i] = temp;\n    }\n}\n\nint main(void) {\n    char word[] = \"dactylo\";\n    printf(\"Before: %s\\n\", word);\n    reverse(word);\n    printf(\"After:  %s\\n\", word);\n    return 0;\n}" },
    { "id": "c-015", "length": "long", "official": false, "text": "#include <stdio.h>\n\nint is_prime(int n) {\n    if (n < 2) {\n        return 0;\n    }\n    for (int divisor = 2; divisor * divisor <= n; divisor++) {\n        if (n % divisor == 0) {\n            return 0;\n        }\n    }\n    return 1;\n}\n\nint main(void) {\n    int count = 0;\n    for (int candidate = 2; candidate < 100; candidate++) {\n        if (is_prime(candidate)) {\n            printf(\"%4d\", candidate);\n            count++;\n            if (count % 8 == 0) {\n                printf(\"\\n\");\n            }\n        }\n    }\n    printf(\"\\nFound %d primes below 100.\\n\", count);\n    return 0;\n}" },
    { "id": "c-101", "length": "short", "official": true, "text": "#include <stdio.h>\n\nint main(void) {\n    printf(\"Ready, set, type!\\n\");\n    return 0;\n}" },
    { "id": "c-102", "length": "short", "official": true, "text": "#include <stdio.h>\n\nint main(void) {\n    int year = 2026;\n    char level = 'A';\n    printf(\"Year %d, level %c\\n\", year, level);\n    return 0;\n}" },
    { "id": "c-103", "length": "short", "official": true, "text": "#include <stdio.h>\n\nint main(void) {\n    int sum = 0;\n    for (int i = 1; i <= 100; i++) {\n        sum += i;\n    }\n    printf(\"Sum 1..100 = %d\\n\", sum);\n    return 0;\n}" },
    { "id": "c-104", "length": "medium", "official": true, "text": "#include <stdio.h>\n\nint max3(int a, int b, int c) {\n    int max = a;\n    if (b > max) {\n        max = b;\n    }\n    if (c > max) {\n        max = c;\n    }\n    return max;\n}\n\nint main(void) {\n    printf(\"%d\\n\", max3(4, 9, 2));\n    printf(\"%d\\n\", max3(11, 5, 17));\n    return 0;\n}" },
    { "id": "c-105", "length": "medium", "official": true, "text": "#include <stdio.h>\n\nint main(void) {\n    int values[] = {34, 7, 89, 12, 45, 3};\n    int size = 6;\n    int min = values[0];\n    int max = values[0];\n    for (int i = 1; i < size; i++) {\n        if (values[i] < min) {\n            min = values[i];\n        }\n        if (values[i] > max) {\n            max = values[i];\n        }\n    }\n    printf(\"Min: %d, Max: %d\\n\", min, max);\n    return 0;\n}" },
    { "id": "c-106", "length": "medium", "official": true, "text": "#include <stdio.h>\n\ndouble power(double base, int exponent) {\n    double result = 1.0;\n    for (int i = 0; i < exponent; i++) {\n        result *= base;\n    }\n    return result;\n}\n\nint main(void) {\n    for (int i = 0; i <= 8; i++) {\n        printf(\"2^%d = %.0f\\n\", i, power(2.0, i));\n    }\n    return 0;\n}" },
    { "id": "c-107", "length": "medium", "official": true, "text": "#include <stdio.h>\n\nvoid increment_all(int *array, int size) {\n    for (int i = 0; i < size; i++) {\n        (*(array + i))++;\n    }\n}\n\nint main(void) {\n    int values[4] = {1, 2, 3, 4};\n    increment_all(values, 4);\n    for (int i = 0; i < 4; i++) {\n        printf(\"%d \", values[i]);\n    }\n    printf(\"\\n\");\n    return 0;\n}" },
    { "id": "c-108", "length": "medium", "official": true, "text": "#include <stdio.h>\n\nstruct Rectangle {\n    double width;\n    double height;\n};\n\ndouble area(struct Rectangle rect) {\n    return rect.width * rect.height;\n}\n\ndouble perimeter(struct Rectangle rect) {\n    return 2 * (rect.width + rect.height);\n}\n\nint main(void) {\n    struct Rectangle rect = {5.0, 3.0};\n    printf(\"Area: %.1f\\n\", area(rect));\n    printf(\"Perimeter: %.1f\\n\", perimeter(rect));\n    return 0;\n}" },
    { "id": "c-109", "length": "long", "official": true, "text": "#include <stdio.h>\n\nint is_vowel(char c) {\n    return c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u' || c == 'y';\n}\n\nint count_vowels(const char *text) {\n    int count = 0;\n    for (int i = 0; text[i] != '\\0'; i++) {\n        if (is_vowel(text[i])) {\n            count++;\n        }\n    }\n    return count;\n}\n\nint count_consonants(const char *text) {\n    int count = 0;\n    for (int i = 0; text[i] != '\\0'; i++) {\n        char c = text[i];\n        if (c >= 'a' && c <= 'z' && !is_vowel(c)) {\n            count++;\n        }\n    }\n    return count;\n}\n\nint main(void) {\n    char sentence[] = \"programming is a superpower\";\n    printf(\"\\\"%s\\\"\\n\", sentence);\n    printf(\"Vowels: %d\\n\", count_vowels(sentence));\n    printf(\"Consonants: %d\\n\", count_consonants(sentence));\n    return 0;\n}" },
    { "id": "c-110", "length": "long", "official": true, "text": "#include <stdio.h>\n\ndouble compute(double a, double b, char op) {\n    switch (op) {\n    case '+':\n        return a + b;\n    case '-':\n        return a - b;\n    case '*':\n        return a * b;\n    case '/':\n        if (b == 0.0) {\n            printf(\"Error: division by zero\\n\");\n            return 0.0;\n        }\n        return a / b;\n    default:\n        printf(\"Unknown operator %c\\n\", op);\n        return 0.0;\n    }\n}\n\nint main(void) {\n    double left[] = {12.0, 7.5, 100.0, 9.0};\n    double right[] = {3.0, 2.5, 4.0, 0.0};\n    char ops[] = {'+', '*', '/', '/'};\n    for (int i = 0; i < 4; i++) {\n        double result = compute(left[i], right[i], ops[i]);\n        printf(\"%.1f %c %.1f = %.2f\\n\", left[i], ops[i], right[i], result);\n    }\n    return 0;\n}" }
  ]
}
```

- [ ] **Step 2: Écrire le corpus Python**

`src/texts/python.json` (complet — 15 libres + 10 officiels) :

```json
{
  "language": "python",
  "texts": [
    { "id": "python-001", "length": "short", "official": false, "text": "print(\"Hello, World!\")\nname = \"Ada\"\nprint(f\"Welcome, {name}!\")" },
    { "id": "python-002", "length": "short", "official": false, "text": "scores = [12, 7, 19, 4]\ntotal = sum(scores)\naverage = total / len(scores)\nprint(f\"Average: {average:.1f}\")" },
    { "id": "python-003", "length": "short", "official": false, "text": "squares = [x * x for x in range(10)]\nevens = [n for n in squares if n % 2 == 0]\nprint(evens)" },
    { "id": "python-004", "length": "short", "official": false, "text": "for i in range(5):\n    if i % 2 == 0:\n        print(f\"{i} is even\")\n    else:\n        print(f\"{i} is odd\")" },
    { "id": "python-005", "length": "short", "official": false, "text": "user = {\"name\": \"Ada\", \"age\": 36}\nfor key, value in user.items():\n    print(f\"{key}: {value}\")" },
    { "id": "python-006", "length": "medium", "official": false, "text": "def greet(name, greeting=\"Hello\", excited=False):\n    message = f\"{greeting}, {name}\"\n    return message + (\"!\" if excited else \".\")\n\n\nnames = [\"Ada\", \"Bob\", \"Eve\", \"Max\"]\nfor index, name in enumerate(names):\n    excited = index % 2 == 0\n    print(greet(name, excited=excited))\n    print(greet(name, \"Hi\", not excited))" },
    { "id": "python-007", "length": "medium", "official": false, "text": "class Counter:\n    def __init__(self):\n        self.value = 0\n\n    def increment(self, step=1):\n        self.value += step\n        return self.value\n\n    def reset(self):\n        self.value = 0\n\n\ncounter = Counter()\ncounter.increment()\ncounter.increment(5)\nprint(f\"Value: {counter.value}\")\ncounter.reset()\nprint(f\"After reset: {counter.value}\")" },
    { "id": "python-008", "length": "medium", "official": false, "text": "lines = [\"alpha\", \"beta\", \"gamma\"]\n\nwith open(\"notes.txt\", \"w\", encoding=\"utf-8\") as file:\n    for line in lines:\n        file.write(line + \"\\n\")\n\nwith open(\"notes.txt\", \"r\", encoding=\"utf-8\") as file:\n    for number, line in enumerate(file, start=1):\n        print(f\"{number}: {line.strip()}\")" },
    { "id": "python-009", "length": "medium", "official": false, "text": "def divide(a, b):\n    try:\n        return round(a / b, 2)\n    except ZeroDivisionError:\n        return None\n\n\npairs = [(10, 2), (5, 0), (9, 3), (7, 4)]\nfor a, b in pairs:\n    result = divide(a, b)\n    print(f\"{a} / {b} = {result}\")\n\nvalid = [divide(a, b) for a, b in pairs]\nprint(f\"Results: {valid}\")" },
    { "id": "python-010", "length": "medium", "official": false, "text": "words = [\"banana\", \"kiwi\", \"strawberry\", \"fig\", \"cherry\"]\n\nby_length = sorted(words, key=len)\nby_last_letter = sorted(words, key=lambda word: word[-1])\nuppercase = [word.upper() for word in words]\n\nprint(f\"By length: {by_length}\")\nprint(f\"By last letter: {by_last_letter}\")\nprint(f\"Uppercase: {uppercase}\")" },
    { "id": "python-011", "length": "long", "official": false, "text": "def fizzbuzz(limit):\n    \"\"\"Return the FizzBuzz sequence from 1 to limit.\"\"\"\n    results = []\n    for number in range(1, limit + 1):\n        if number % 15 == 0:\n            results.append(\"FizzBuzz\")\n        elif number % 3 == 0:\n            results.append(\"Fizz\")\n        elif number % 5 == 0:\n            results.append(\"Buzz\")\n        else:\n            results.append(str(number))\n    return results\n\n\ndef main():\n    \"\"\"Print FizzBuzz up to 100, one value per line.\"\"\"\n    values = fizzbuzz(100)\n    for value in values:\n        print(value)\n    print(f\"Printed {len(values)} values.\")\n\n\nif __name__ == \"__main__\":\n    main()" },
    { "id": "python-012", "length": "long", "official": false, "text": "import time\nfrom functools import lru_cache\n\n\n@lru_cache(maxsize=None)\ndef fibonacci(n):\n    \"\"\"Return the n-th Fibonacci number.\"\"\"\n    if n < 2:\n        return n\n    return fibonacci(n - 1) + fibonacci(n - 2)\n\n\ndef main():\n    start = time.perf_counter()\n    numbers = [fibonacci(i) for i in range(35)]\n    elapsed = time.perf_counter() - start\n    print(\"The first 35 Fibonacci numbers:\")\n    for index, value in enumerate(numbers):\n        print(f\"F({index}) = {value}\")\n    print(f\"Sum: {sum(numbers)}\")\n    print(f\"Computed in {elapsed:.4f} seconds.\")\n\n\nif __name__ == \"__main__\":\n    main()" },
    { "id": "python-013", "length": "long", "official": false, "text": "def count_words(text):\n    \"\"\"Count how often each word appears in the text.\"\"\"\n    counts = {}\n    for word in text.lower().split():\n        word = word.strip(\".,;:!?\")\n        if word:\n            counts[word] = counts.get(word, 0) + 1\n    return counts\n\n\ndef main():\n    text = \"the quick brown fox jumps over the lazy dog, and the dog barks\"\n    counts = count_words(text)\n    ranked = sorted(counts.items(), key=lambda item: item[1], reverse=True)\n    print(\"Word frequencies:\")\n    for word, count in ranked:\n        print(f\"{word:>10}: {count}\")\n    print(f\"Distinct words: {len(counts)}\")\n\n\nif __name__ == \"__main__\":\n    main()" },
    { "id": "python-014", "length": "long", "official": false, "text": "class BankAccount:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n        self.history = []\n\n    def deposit(self, amount):\n        if amount <= 0:\n            raise ValueError(\"Amount must be positive\")\n        self.balance += amount\n        self.history.append(f\"+{amount}\")\n\n    def withdraw(self, amount):\n        if amount > self.balance:\n            print(\"Insufficient funds\")\n            return False\n        self.balance -= amount\n        self.history.append(f\"-{amount}\")\n        return True\n\n    def statement(self):\n        print(f\"Account of {self.owner}\")\n        for entry in self.history:\n            print(f\"  {entry}\")\n        print(f\"Balance: {self.balance}\")\n\n\ndef main():\n    account = BankAccount(\"Ada\", 100)\n    account.deposit(50)\n    account.withdraw(30)\n    account.withdraw(500)\n    account.statement()\n\n\nif __name__ == \"__main__\":\n    main()" },
    { "id": "python-015", "length": "long", "official": false, "text": "import random\n\n\ndef play_round(secret, attempts):\n    \"\"\"Return the number of guesses needed, or None if failed.\"\"\"\n    for attempt in range(1, attempts + 1):\n        guess = random.randint(1, 100)\n        if guess == secret:\n            return attempt\n    return None\n\n\ndef main():\n    random.seed(42)\n    wins = 0\n    rounds = 10\n    for game in range(rounds):\n        secret = random.randint(1, 100)\n        result = play_round(secret, 7)\n        if result is not None:\n            wins += 1\n            print(f\"Game {game + 1}: found {secret} in {result} tries\")\n        else:\n            print(f\"Game {game + 1}: lost, secret was {secret}\")\n    print(f\"Won {wins}/{rounds} games\")\n\n\nif __name__ == \"__main__\":\n    main()" },
    { "id": "python-101", "length": "short", "official": true, "text": "message = \"Ready, set, type!\"\nprint(message)\nprint(f\"Length: {len(message)}\")" },
    { "id": "python-102", "length": "short", "official": true, "text": "name = \"Ada\"\nage = 36\npi = 3.14159\nprint(f\"{name} is {age} years old\")\nprint(f\"Pi is about {pi:.2f}\")" },
    { "id": "python-103", "length": "short", "official": true, "text": "total = 0\nfor i in range(1, 101):\n    total += i\nprint(f\"Sum 1..100 = {total}\")" },
    { "id": "python-104", "length": "medium", "official": true, "text": "def is_prime(n):\n    if n < 2:\n        return False\n    divisor = 2\n    while divisor * divisor <= n:\n        if n % divisor == 0:\n            return False\n        divisor += 1\n    return True\n\n\nfor number in range(2, 20):\n    if is_prime(number):\n        print(f\"{number} is prime\")" },
    { "id": "python-105", "length": "medium", "official": true, "text": "numbers = list(range(1, 21))\n\nsquares = [n * n for n in numbers]\nevens = [n for n in numbers if n % 2 == 0]\nodds = [n for n in numbers if n % 2 == 1]\nbig_squares = [s for s in squares if s > 100]\n\nprint(f\"Squares: {squares}\")\nprint(f\"Evens: {evens}\")\nprint(f\"Odds: {odds}\")\nprint(f\"Big squares: {big_squares}\")" },
    { "id": "python-106", "length": "medium", "official": true, "text": "text = \"the quick brown fox jumps over the lazy dog\"\n\ncounts = {}\nfor letter in text:\n    if letter != \" \":\n        counts[letter] = counts.get(letter, 0) + 1\n\ntop = sorted(counts.items(), key=lambda item: item[1], reverse=True)\nfor letter, count in top[:5]:\n    print(f\"{letter}: {count}\")" },
    { "id": "python-107", "length": "medium", "official": true, "text": "class Vector:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\n    def __add__(self, other):\n        return Vector(self.x + other.x, self.y + other.y)\n\n    def length_squared(self):\n        return self.x ** 2 + self.y ** 2\n\n    def __repr__(self):\n        return f\"Vector({self.x}, {self.y})\"\n\n\nv1 = Vector(3, 4)\nv2 = Vector(1, 2)\nprint(v1 + v2)\nprint(f\"Length squared: {v1.length_squared()}\")" },
    { "id": "python-108", "length": "long", "official": true, "text": "import random\n\n\ndef roll_dice(rolls, sides=6):\n    \"\"\"Return a list of dice results.\"\"\"\n    return [random.randint(1, sides) for _ in range(rolls)]\n\n\ndef main():\n    random.seed(7)\n    results = roll_dice(20)\n    counts = {face: results.count(face) for face in range(1, 7)}\n    for face, count in counts.items():\n        bar = \"*\" * count\n        print(f\"{face}: {bar} ({count})\")\n    doubles = sum(1 for count in counts.values() if count >= 4)\n    print(f\"Faces rolled 4+ times: {doubles}\")\n    print(f\"Average roll: {sum(results) / len(results):.2f}\")\n\n\nif __name__ == \"__main__\":\n    main()" },
    { "id": "python-109", "length": "long", "official": true, "text": "def parse_scores(raw):\n    \"\"\"Parse 'name:score' lines into a dictionary.\"\"\"\n    scores = {}\n    for line in raw.strip().splitlines():\n        name, value = line.split(\":\")\n        scores[name.strip()] = int(value.strip())\n    return scores\n\n\ndef main():\n    raw = \"ada: 15\\nbob: 9\\neve: 18\\nmax: 12\"\n    scores = parse_scores(raw)\n    best = max(scores, key=scores.get)\n    worst = min(scores, key=scores.get)\n    average = sum(scores.values()) / len(scores)\n    print(f\"Best score: {best}\")\n    print(f\"Worst score: {worst}\")\n    print(f\"Average: {average:.1f}\")\n    for name, score in sorted(scores.items()):\n        print(f\"{name:>5}: {score}\")\n\n\nif __name__ == \"__main__\":\n    main()" },
    { "id": "python-110", "length": "long", "official": true, "text": "class ContactBook:\n    def __init__(self):\n        self.contacts = {}\n\n    def add(self, name, email):\n        if name in self.contacts:\n            print(f\"{name} already exists\")\n            return False\n        self.contacts[name] = email\n        return True\n\n    def find(self, name):\n        return self.contacts.get(name, \"not found\")\n\n    def remove(self, name):\n        if name in self.contacts:\n            del self.contacts[name]\n            return True\n        return False\n\n    def show_all(self):\n        for name, email in sorted(self.contacts.items()):\n            print(f\"{name:>10}: {email}\")\n\n\ndef main():\n    book = ContactBook()\n    book.add(\"ada\", \"ada@example.com\")\n    book.add(\"bob\", \"bob@example.com\")\n    book.add(\"ada\", \"duplicate@example.com\")\n    print(book.find(\"ada\"))\n    print(book.find(\"eve\"))\n    book.show_all()\n    book.remove(\"bob\")\n    print(f\"Remaining: {len(book.contacts)}\")\n\n\nif __name__ == \"__main__\":\n    main()" }
  ]
}
```

- [ ] **Step 3: Réécrire le validateur (v2)**

`scripts/validateCorpus.mjs` (remplacement complet) :

```js
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const root = join(dirname(fileURLToPath(import.meta.url)), '..');

const KINDS = [
  {
    name: 'prose',
    langs: ['fr', 'en'],
    freeCount: 30,
    freeLastId: 30,
    quotes: 5,
    ranges: { short: [110, 200], medium: [320, 480], long: [650, 950] },
    multiLine: false,
  },
  {
    name: 'code',
    langs: ['c', 'python'],
    freeCount: 15,
    freeLastId: 15,
    quotes: 0,
    ranges: { short: [60, 300], medium: [280, 800], long: [650, 2500] },
    multiLine: true,
  },
];

const errors = [];
const allIds = new Set();

for (const kind of KINDS) {
  for (const lang of kind.langs) {
    const file = join(root, 'src', 'texts', `${lang}.json`);
    const data = JSON.parse(readFileSync(file, 'utf8'));
    const texts = data.texts;
    const free = texts.filter((t) => !t.official);
    const official = texts.filter((t) => t.official);

    if (free.length !== kind.freeCount) {
      errors.push(`${lang}: ${free.length} textes libres (${kind.freeCount} attendus)`);
    }
    if (official.length !== 10) errors.push(`${lang}: ${official.length} textes officiels (10 attendus)`);
    if (free.filter((t) => t.quote).length !== kind.quotes) {
      errors.push(`${lang}: il faut exactement ${kind.quotes} citations (quote: true)`);
    }

    for (const t of texts) {
      if (!new RegExp(`^${lang}-\\d{3}$`).test(t.id)) errors.push(`${lang}: id invalide ${t.id}`);
      if (allIds.has(t.id)) errors.push(`${lang}: id dupliqué ${t.id}`);
      allIds.add(t.id);

      const range = kind.ranges[t.length];
      if (!range) errors.push(`${t.id}: length invalide "${t.length}"`);
      else if (t.text.length < range[0] || t.text.length > range[1]) {
        errors.push(`${t.id}: ${t.text.length} caractères, hors fourchette ${t.length} [${range[0]}-${range[1]}]`);
      }

      if (kind.multiLine) {
        if (t.official && !t.text.includes('\n')) errors.push(`${t.id}: texte officiel code sur une seule ligne`);
        if (/\t/.test(t.text)) errors.push(`${t.id}: tabulation interdite (espaces uniquement)`);
        if (/\r/.test(t.text)) errors.push(`${t.id}: retour chariot interdit`);
        if (t.text.split('\n').some((line) => line !== line.trimEnd())) {
          errors.push(`${t.id}: espace en fin de ligne`);
        }
      } else {
        if (t.official && !/[.,;:!?]/.test(t.text)) errors.push(`${t.id}: texte officiel sans ponctuation`);
        if (/\n/.test(t.text)) errors.push(`${t.id}: texte de prose multi-lignes`);
      }
    }

    const freeIds = free.map((t) => t.id).sort();
    const officialIds = official.map((t) => t.id).sort();
    const lastFree = String(kind.freeLastId).padStart(3, '0');
    if (freeIds[0] !== `${lang}-001` || freeIds[freeIds.length - 1] !== `${lang}-${lastFree}`) {
      errors.push(`${lang}: les ids libres doivent couvrir 001-${lastFree}`);
    }
    if (officialIds[0] !== `${lang}-101` || officialIds[9] !== `${lang}-110`) {
      errors.push(`${lang}: les ids officiels doivent couvrir 101-110`);
    }
  }
}

if (errors.length > 0) {
  console.error('Corpus invalide :');
  for (const e of errors) console.error(`  - ${e}`);
  process.exit(1);
}
console.log('Corpus valide : 2 langues × (30+10) + 2 langages × (15+10).');
```

- [ ] **Step 4: Valider et corriger si besoin**

Run: `npm run validate:corpus`
Expected: « Corpus valide : 2 langues × (30+10) + 2 langages × (15+10). »
Si des textes sont hors fourchette : ajuster **uniquement les textes signalés** (allonger/raccourcir en gardant du code idiomatique et valide), relancer jusqu'au succès.

- [ ] **Step 5: Mettre à jour corpus.ts et ses tests**

`src/texts/corpus.ts` — ajouter les imports et entrées :

```ts
import frData from './fr.json';
import enData from './en.json';
import cData from './c.json';
import pythonData from './python.json';
import type { Language, TextEntry, TextLength } from './types';
```

```ts
export const CORPUS: TextEntry[] = [
  // les imports JSON sont typés `string` partout : on caste après validation au build
  ...load('fr', frData as unknown as { texts: RawEntry[] }),
  ...load('en', enData as unknown as { texts: RawEntry[] }),
  ...load('c', cData as unknown as { texts: RawEntry[] }),
  ...load('python', pythonData as unknown as { texts: RawEntry[] }),
];
```

`src/texts/corpus.test.ts` — remplacer le premier test et en ajouter un :

```ts
  it('contient 130 textes avec le champ language injecté', () => {
    expect(CORPUS).toHaveLength(130);
    expect(CORPUS.every((t) => ['fr', 'en', 'c', 'python'].includes(t.language))).toBe(true);
  });

  it('getTexts retourne 15 textes par langage de code', () => {
    expect(getTexts('c')).toHaveLength(15);
    expect(getTexts('python')).toHaveLength(15);
    expect(getTexts('c').every((t) => !t.official && t.language === 'c')).toBe(true);
  });
```

et compléter les tests existants avec :

```ts
  it('pickText fonctionne pour les langages de code', () => {
    expect(pickText('python', 'short').language).toBe('python');
    expect(pickText('c', 'long').length).toBe('long');
  });
```

- [ ] **Step 6: Vérifier que les tests passent**

Run: `npx vitest run src/texts/`
Expected: tous les tests PASS

- [ ] **Step 7: Commit**

```bash
git add src/texts/ scripts/validateCorpus.mjs
git commit -m "feat(texts): c and python corpora (25 texts each) with validator v2"
```

---

### Task 22: Multi-lignes — touche Entrée, rendu `\n`, auto-indentation

**Files:**
- Modify: `src/ui/components/TypingArea.tsx`, `src/state/runStore.ts`
- Test: `src/engine/typingEngine.test.ts` (ajout), `src/state/runStore.test.ts` (ajouts), `src/ui/components/TypingArea.test.tsx` (ajouts)

**Interfaces:**
- Consumes: engine (tâche 2), `useRunStore` (tâche 11), `isFinished` (tâche 2).
- Produces (consommé par tâche 23) : saisie multi-lignes complète — `Entrée` envoie `onChar('\n')` ; les `\n` sont rendus avec un marqueur `↵` suivi du saut de ligne (conteneur en `whitespace-pre-wrap`) ; après un `\n` **correct**, `runStore.key` tape automatiquement les espaces attendus qui suivent (auto-indentation).

- [ ] **Step 1: Écrire les tests qui échouent**

Ajout dans `src/engine/typingEngine.test.ts` :

```ts
  it('accepte le caractère nouvelle ligne comme tout autre caractère', () => {
    let s = createRun('a\nb');
    s = typeChar(s, 'a', 1000);
    s = typeChar(s, '\n', 1100);
    expect(s.statuses).toEqual(['correct', 'correct', 'pending']);
    s = typeChar(s, 'b', 1200);
    expect(isFinished(s)).toBe(true);
  });
```

Ajouts dans `src/state/runStore.test.ts` :

```ts
  it('auto-indente les espaces après un saut de ligne correct', () => {
    useRunStore.getState().start(config, 'a\n    b');
    useRunStore.getState().key('a');
    useRunStore.getState().key('\n');
    // le curseur a sauté les 4 espaces d'indentation automatiquement
    expect(useRunStore.getState().typing?.cursor).toBe(6);
    expect(useRunStore.getState().typing?.statuses.slice(0, 6)).toEqual(
      ['correct', 'correct', 'correct', 'correct', 'correct', 'correct'],
    );
  });

  it("n'auto-indente pas après un saut de ligne incorrect", () => {
    useRunStore.getState().start(config, 'ab\n  c');
    useRunStore.getState().key('\n'); // attendu: 'a' -> erreur, pas d'indentation
    const typing = useRunStore.getState().typing;
    expect(typing?.statuses[0]).toBe('incorrect');
    expect(typing?.cursor).toBe(1);
    expect(typing?.statuses[1]).toBe('pending');
  });
```

Ajouts dans `src/ui/components/TypingArea.test.tsx` :

```tsx
  it('la touche Entrée envoie un caractère nouvelle ligne', async () => {
    const onChar = vi.fn();
    render(<TypingArea state={createRun('a\nb')} onChar={onChar} onBackspace={() => {}} />);
    screen.getByLabelText('Zone de saisie').focus();
    await userEvent.keyboard('{Enter}');
    expect(onChar).toHaveBeenCalledWith('\n');
  });

  it('affiche un marqueur pour les sauts de ligne', () => {
    render(<TypingArea state={createRun('a\nb')} onChar={() => {}} onBackspace={() => {}} />);
    const chars = screen.getByTestId('typing-area').querySelectorAll('[data-char]');
    expect(chars).toHaveLength(3);
    expect(chars[1].textContent).toBe('↵\n');
  });
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run src/state/runStore.test.ts src/ui/components/TypingArea.test.tsx`
Expected: FAIL (auto-indent et Entrée absents)

- [ ] **Step 3: Modifier TypingArea**

Dans `src/ui/components/TypingArea.tsx` :

1. Ajouter la classe `whitespace-pre-wrap` au conteneur (avec les classes existantes).
2. Dans `onKeyDown`, ajouter le cas Entrée avant le cas générique :

```tsx
        onKeyDown={(e) => {
          if (disabled) return;
          if (e.key === 'Backspace') {
            e.preventDefault();
            onBackspace();
            return;
          }
          if (e.key === 'Enter') {
            e.preventDefault();
            onChar('\n');
            return;
          }
          if (e.key.length === 1 && !e.ctrlKey && !e.metaKey && !e.altKey) {
            e.preventDefault();
            onChar(e.key);
          }
        }}
```

3. Dans le rendu des caractères, afficher le marqueur pour `\n` :

```tsx
        <span
          // eslint-disable-next-line react/no-array-index-key
          key={i}
          data-char
          className={
            state.statuses[i] === 'correct'
              ? 'text-ok'
              : state.statuses[i] === 'incorrect'
                ? 'rounded-sm bg-err/15 text-err'
                : 'text-muted'
          }
        >
          {ch === '\n' ? '↵\n' : ch}
        </span>
```

- [ ] **Step 4: Modifier runStore (auto-indentation)**

Dans `src/state/runStore.ts`, remplacer la méthode `key` par :

```ts
  key(char) {
    const { typing, config, status } = get();
    if (!typing || !config || status !== 'running') return;
    let next = typeChar(typing, char, Date.now());
    if (next === typing) return;
    // auto-indentation : après un saut de ligne correct, tape les espaces attendus
    if (char === '\n' && next.statuses[next.cursor - 1] === 'correct') {
      while (next.text[next.cursor] === ' ' && !isFinished(next)) {
        next = typeChar(next, ' ', Date.now());
      }
    }
    if (isFinished(next)) {
      set({ typing: next, status: 'finished' });
      void completeRun(next, config, Date.now())
        .then((result) => set({ result }))
        .catch(() => {
          // IndexedDB indisponible (navigation privée) : résultats affichés, rien n'est persisté
          const now = Date.now();
          set({
            result: {
              run: buildRunRecord(next, config, now),
              timeline: wpmTimeline(next.events, next.startedAt ?? now, next.finishedAt ?? now),
              newAchievements: [],
              tierUp: null,
              progress: null,
              newRecords: [],
            },
          });
          useToasts.getState().push({
            title: 'Sauvegarde impossible',
            description: 'Le stockage local est indisponible : cette run ne sera pas enregistrée.',
            kind: 'info',
          });
        });
    } else {
      set({ typing: next });
    }
  },
```

- [ ] **Step 5: Vérifier que les tests passent**

Run: `npx vitest run src/engine/ src/state/ src/ui/components/TypingArea.test.tsx`
Expected: tous les tests PASS

- [ ] **Step 6: Commit**

```bash
git add src/engine/typingEngine.test.ts src/state/runStore.ts src/state/runStore.test.ts src/ui/components/TypingArea.tsx src/ui/components/TypingArea.test.tsx
git commit -m "feat(run): multiline typing with enter key and auto-indent"
```

---

### Task 23: Page Dev

**Files:**
- Create: `src/ui/pages/DevPage.tsx`
- Modify: `src/ui/components/Header.tsx` (lien Dev), `src/App.tsx` (route `/dev`)
- Test: `src/ui/pages/DevPage.test.tsx`, `src/ui/components/Layout.test.tsx` (ajout)

**Interfaces:**
- Consumes: `pickText` (tâche 5) ; `CODE_LANGUAGES`, `LANGUAGE_LABELS`, `Language`, `TextLength` (tâche 20) ; `useRunStore`, `useSettings`, `useToasts`, `useFocusGuard`, `TypingArea`, `RunHud`, `ResultsScreen`, `playKey/playError/playSuccess`, `ALL_OPTIONS_ON` (tâches 10-12).
- Produces: page `/dev` complète.

Comportement : setup = choix du langage (C / Python), longueur (**Extrait** = short, **Fonction** = medium, **Programme** = long), note « Code tapé brut — multiplicateur ×1,4 » ; la run démarre avec `entry.text` **brut** (jamais `applyOptions` — il détruirait les sauts de ligne) et `options: ALL_OPTIONS_ON`. Vue run et résultats identiques à PlayPage.

- [ ] **Step 1: Écrire les tests qui échouent**

`src/ui/pages/DevPage.test.tsx` :

```tsx
import 'fake-indexeddb/auto';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { beforeEach, describe, expect, it } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import { db } from '@/db/db';
import { useRunStore } from '@/state/runStore';
import { DevPage } from './DevPage';

beforeEach(async () => {
  await Promise.all(db.tables.map((t) => t.clear()));
  useRunStore.getState().reset();
});

describe('DevPage', () => {
  it('affiche le setup avec C et Python puis démarre une run de code brut', async () => {
    render(<MemoryRouter><DevPage /></MemoryRouter>);
    expect(screen.getByRole('button', { name: 'C' })).toBeInTheDocument();
    expect(screen.getByRole('button', { name: 'Python' })).toBeInTheDocument();
    expect(screen.getByText('Programme')).toBeInTheDocument();
    await userEvent.click(screen.getByRole('button', { name: 'Démarrer' }));
    expect(await screen.findByTestId('typing-area')).toBeInTheDocument();
    const state = useRunStore.getState();
    expect(state.config?.mode).toBe('free');
    expect(['c', 'python']).toContain(state.config?.language);
    expect(state.typing?.text).toContain('\n'); // code brut, multi-lignes préservé
  });
});
```

Ajout dans `src/ui/components/Layout.test.tsx` :

```tsx
    expect(screen.getByRole('link', { name: 'Dev' })).toBeInTheDocument();
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run src/ui/pages/DevPage.test.tsx`
Expected: FAIL (module introuvable)

- [ ] **Step 3: Implémenter la page Dev**

`src/ui/pages/DevPage.tsx` :

```tsx
import { useEffect, useState } from 'react';
import { useRunStore } from '@/state/runStore';
import { useSettings } from '@/state/settingsStore';
import { useToasts } from '@/state/toastStore';
import { useFocusGuard } from '@/ui/hooks/useFocusGuard';
import { TypingArea } from '@/ui/components/TypingArea';
import { RunHud } from '@/ui/components/RunHud';
import { ResultsScreen } from '@/ui/components/ResultsScreen';
import { playError, playKey, playSuccess } from '@/ui/sounds';
import { pickText } from '@/texts/corpus';
import { ALL_OPTIONS_ON } from '@/texts/normalize';
import { CODE_LANGUAGES, LANGUAGE_LABELS } from '@/texts/types';
import type { Language, TextLength } from '@/texts/types';

const LENGTHS: { value: TextLength; label: string }[] = [
  { value: 'short', label: 'Extrait' },
  { value: 'medium', label: 'Fonction' },
  { value: 'long', label: 'Programme' },
];

export function DevPage() {
  const { status, typing, result, start, key, backspace, reset } = useRunStore();
  const profile = useSettings((s) => s.profile);
  const push = useToasts((s) => s.push);
  const [language, setLanguage] = useState<Language>('python');
  const [length, setLength] = useState<TextLength>('short');
  useFocusGuard();

  useEffect(() => {
    if (!result) return;
    result.newAchievements.forEach((a) =>
      push({ title: a.title, description: a.description, kind: 'achievement' }),
    );
    if (result.newRecords.length > 0) playSuccess();
  }, [result, push]);

  const begin = () => {
    const entry = pickText(language, length);
    // code brut : jamais applyOptions (détruirait les sauts de ligne)
    start({ mode: 'free', language, textId: entry.id, options: ALL_OPTIONS_ON }, entry.text);
  };

  const handleChar = (c: string) => {
    const t = useRunStore.getState().typing;
    if (t && profile.sounds) {
      if (t.text[t.cursor] === c) playKey();
      else playError();
    }
    key(c);
  };

  if (status === 'finished' && result) {
    return <ResultsScreen result={result} onReplay={begin} onExit={reset} />;
  }

  if (status === 'running' || status === 'paused' || status === 'invalidated') {
    return (
      <div className="flex flex-col gap-6">
        {typing && <RunHud typing={typing} />}
        {status === 'invalidated' ? (
          <div className="rounded-xl border border-err/40 bg-err/10 p-6 text-center">
            <p className="mb-4 font-bold text-err">Run invalidée : la fenêtre a perdu le focus trop longtemps.</p>
            <button
              type="button"
              onClick={reset}
              className="rounded-xl bg-accent px-6 py-2 font-bold text-white"
            >
              Nouvelle run
            </button>
          </div>
        ) : (
          typing && (
            <>
              {status === 'paused' && (
                <p className="text-center text-sm text-muted">En pause — clique dans le texte pour reprendre.</p>
              )}
              <TypingArea state={typing} disabled={status !== 'running'} onChar={handleChar} onBackspace={backspace} />
            </>
          )
        )}
      </div>
    );
  }

  return (
    <div className="mx-auto flex max-w-xl flex-col gap-8">
      <div>
        <h1 className="text-2xl font-bold">Mode Dev</h1>
        <p className="mt-1 text-sm text-muted">
          Tape du code le plus vite possible. Code tapé brut — multiplicateur ×1,4. Entrée valide les sauts de ligne, l'indentation est automatique.
        </p>
      </div>

      <section>
        <p className="mb-2 text-sm font-semibold text-muted">Langage</p>
        <div className="flex gap-2">
          {CODE_LANGUAGES.map((l) => (
            <button
              key={l}
              type="button"
              onClick={() => setLanguage(l)}
              className={`rounded-xl border px-4 py-2 font-bold transition-colors ${
                language === l ? 'border-accent bg-accent/15 text-text' : 'border-line text-muted'
              }`}
            >
              {LANGUAGE_LABELS[l]}
            </button>
          ))}
        </div>
      </section>

      <section>
        <p className="mb-2 text-sm font-semibold text-muted">Longueur</p>
        <div className="flex flex-wrap gap-2">
          {LENGTHS.map(({ value, label }) => (
            <button
              key={value}
              type="button"
              onClick={() => setLength(value)}
              className={`rounded-xl border px-4 py-2 transition-colors ${
                length === value ? 'border-accent bg-accent/15 text-text' : 'border-line text-muted'
              }`}
            >
              {label}
            </button>
          ))}
        </div>
      </section>

      <button
        type="button"
        onClick={begin}
        className="rounded-xl bg-accent px-8 py-3 text-lg font-bold text-white transition-opacity hover:opacity-90"
      >
        Démarrer
      </button>
    </div>
  );
}
```

- [ ] **Step 4: Brancher le lien et la route**

`src/ui/components/Header.tsx` — dans `LINKS`, insérer après l'entrée Entraînement :

```tsx
  { to: '/play', label: 'Entraînement' },
  { to: '/dev', label: 'Dev' },
```

`src/App.tsx` — ajouter l'import et la route :

```tsx
import { DevPage } from '@/ui/pages/DevPage';
```

```tsx
          <Route path="/play" element={<PlayPage />} />
          <Route path="/dev" element={<DevPage />} />
```

- [ ] **Step 5: Vérifier que les tests passent**

Run: `npx vitest run src/ui/`
Expected: tous les tests PASS

- [ ] **Step 6: Commit**

```bash
git add src/ui/pages/DevPage.tsx src/ui/pages/DevPage.test.tsx src/ui/components/Header.tsx src/ui/components/Layout.test.tsx src/App.tsx
git commit -m "feat(dev): dev mode page with c/python code typing"
```

---

### Task 24: Intégrations — 4 ligues, succès dev, pages existantes

**Files:**
- Modify: `src/achievements/definitions.ts`, `src/achievements/check.ts`, `src/achievements/definitions.test.ts`, `src/ui/pages/ChallengerPage.tsx`, `src/ui/pages/LeaderboardPage.tsx`, `src/ui/pages/HomePage.tsx`, `src/ui/pages/AchievementsPage.tsx`, `src/ui/pages/AchievementsPage.test.tsx`
- Test: les tests modifiés ci-dessous

**Interfaces:**
- Consumes: `isCodeLanguage` (tâche 20) ; `getProgress` (tâche 7) ; `ACHIEVEMENTS` (tâche 8).
- Produces :
  - `AchievementCategory` gagne `'dev'` ; `ACHIEVEMENTS` passe à **28** succès :
    - `hello-world` « Hello, World! » — terminer une run en C ou Python (catégorie dev)
    - `dev-10` « Apprenti programmeur » — 10 runs en langages de code (progress = nb de runs dev, target 10)
    - `dev-perfect` « Sans un warning » — run dev ≥ 100 car. à 100 % (catégorie dev)
  - `gold-any` / `diamond-any` / `challenger-any` évaluent **toutes** les ligues (`Object.values(ctx.progress)`)
  - `buildContext` charge les 4 progressions (`fr`, `en`, `c`, `python`)
  - Pages Challenger, Classements, Accueil : sélecteur/cartes pour les 4 langages via `LANGUAGE_LABELS`

- [ ] **Step 1: Mettre à jour les tests (rouges)**

`src/achievements/definitions.test.ts` — remplacer le helper `ctx` et le test de comptage, ajouter les tests dev :

```ts
function ctx(patch: Partial<AchievementContext>): AchievementContext {
  const r = run({});
  return {
    newRun: r, runs: [r], totalChars: 250, streakDays: 1,
    progress: {
      fr: emptyProgress('fr'),
      en: emptyProgress('en'),
      c: emptyProgress('c'),
      python: emptyProgress('python'),
    },
    now: 1_000_000, ...patch,
  };
}
```

```ts
  it('contient 28 succès aux ids uniques', () => {
    expect(ACHIEVEMENTS).toHaveLength(28);
    expect(new Set(ACHIEVEMENTS.map((a) => a.id)).size).toBe(28);
  });

  it('hello-world se débloque sur une run en langage de code', () => {
    expect(def('hello-world').isUnlocked(ctx({ newRun: run({ language: 'python' }) }))).toBe(true);
    expect(def('hello-world').isUnlocked(ctx({ newRun: run({ language: 'fr' }) }))).toBe(false);
  });

  it('dev-10 compte les runs en langages de code', () => {
    const runs = Array.from({ length: 9 }, () => run({ language: 'c' }));
    expect(def('dev-10').isUnlocked(ctx({ runs }))).toBe(false);
    expect(def('dev-10').isUnlocked(ctx({ runs: [...runs, run({ language: 'python' })] }))).toBe(true);
    expect(def('dev-10').progress?.(ctx({ runs }))).toBe(9);
  });

  it('dev-perfect exige 100% sur une run code d\'au moins 100 caractères', () => {
    expect(def('dev-perfect').isUnlocked(ctx({ newRun: run({ language: 'python', accuracy: 1, chars: 100 }) }))).toBe(true);
    expect(def('dev-perfect').isUnlocked(ctx({ newRun: run({ language: 'en', accuracy: 1, chars: 100 }) }))).toBe(false);
    expect(def('dev-perfect').isUnlocked(ctx({ newRun: run({ language: 'c', accuracy: 1, chars: 99 }) }))).toBe(false);
  });

  it('gold-any considère aussi les ligues de code', () => {
    const goldC = { ...emptyProgress('c' as Language), tier: 'or' as const };
    const progress = { fr: emptyProgress('fr'), en: emptyProgress('en'), c: goldC, python: emptyProgress('python') };
    expect(def('gold-any').isUnlocked(ctx({ progress }))).toBe(true);
    expect(def('gold-both').isUnlocked(ctx({ progress }))).toBe(false); // gold-both reste fr+en
  });
```

`src/ui/pages/AchievementsPage.test.tsx` — remplacer `'1 / 25'` par `'1 / 28'`.

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run src/achievements/ src/ui/pages/AchievementsPage.test.tsx`
Expected: FAIL (28 ≠ 25, succès dev absents)

- [ ] **Step 3: Modifier les définitions et le moteur**

`src/achievements/definitions.ts` :

1. Ajouter l'import :

```ts
import { isCodeLanguage } from '@/texts/types';
```

(l'import existant `import type { Language } from '@/texts/types';` reste)

2. Étendre la catégorie :

```ts
export type AchievementCategory = 'vitesse' | 'precision' | 'volume' | 'challenger' | 'dev' | 'fun';
```

3. Remplacer les trois règles de ligue « any » par des versions multi-ligues :

```ts
  {
    id: 'gold-any', category: 'challenger', title: 'En or',
    description: 'Atteindre le tier Or dans une ligue',
    isUnlocked: (ctx) => Object.values(ctx.progress).some((p) => tierAtLeast(p, 'or')),
  },
  {
    id: 'diamond-any', category: 'challenger', title: 'Diamanté',
    description: 'Atteindre le tier Diamant dans une ligue',
    isUnlocked: (ctx) => Object.values(ctx.progress).some((p) => tierAtLeast(p, 'diamant')),
  },
  {
    id: 'challenger-any', category: 'challenger', title: 'Challenger',
    description: 'Atteindre le tier Challenger dans une ligue',
    isUnlocked: (ctx) => Object.values(ctx.progress).some((p) => tierAtLeast(p, 'challenger')),
  },
```

(`gold-both` et `enter-league` inchangés : `enter-league` = `Object.values(ctx.progress).some((p) => p.tier !== null)` — remplacer aussi pour couvrir les ligues de code.)

4. Ajouter les 3 succès dev (après `gold-both`) :

```ts
  {
    id: 'hello-world', category: 'dev', title: 'Hello, World!',
    description: 'Terminer une run en C ou Python',
    isUnlocked: (ctx) => isCodeLanguage(ctx.newRun.language),
  },
  {
    id: 'dev-10', category: 'dev', title: 'Apprenti programmeur',
    description: 'Terminer 10 runs en langages de code',
    target: 10,
    isUnlocked: (ctx) => ctx.runs.filter((r) => isCodeLanguage(r.language)).length >= 10,
    progress: (ctx) => ctx.runs.filter((r) => isCodeLanguage(r.language)).length,
  },
  {
    id: 'dev-perfect', category: 'dev', title: 'Sans un warning',
    description: 'Terminer une run dev d\'au moins 100 caractères à 100 % de précision',
    isUnlocked: (ctx) =>
      isCodeLanguage(ctx.newRun.language) && ctx.newRun.accuracy === 1 && ctx.newRun.chars >= 100,
  },
```

`src/achievements/check.ts` — charger les 4 progressions :

```ts
export async function buildContext(newRun: RunRecord): Promise<AchievementContext> {
  const [runs, fr, en, c, python] = await Promise.all([
    allRuns(), getProgress('fr'), getProgress('en'), getProgress('c'), getProgress('python'),
  ]);
  return {
    newRun,
    runs,
    totalChars: runs.reduce((sum, r) => sum + r.chars, 0),
    streakDays: computeStreak(runs.map((r) => r.date), Date.now()),
    progress: { fr, en, c, python },
    now: Date.now(),
  };
}
```

- [ ] **Step 4: Mettre à jour les pages**

`src/ui/pages/ChallengerPage.tsx` — remplacer le sélecteur de langue par les 4 langages :

```tsx
import { LANGUAGE_LABELS, type Language } from '@/texts/types';
```

```tsx
        <div className="flex gap-2">
          {(['fr', 'en', 'c', 'python'] as const).map((l) => (
            <button
              key={l}
              type="button"
              onClick={() => setLanguage(l)}
              className={`rounded-xl border px-4 py-1.5 font-bold transition-colors ${
                language === l ? 'border-accent bg-accent/15 text-text' : 'border-line text-muted'
              }`}
            >
              {LANGUAGE_LABELS[l]}
            </button>
          ))}
        </div>
```

(supprimer l'ancien bloc `(['fr', 'en'] as const)` et son label ternaire)

`src/ui/pages/LeaderboardPage.tsx` — même remplacement du sélecteur `(['fr', 'en'] as const)` par `(['fr', 'en', 'c', 'python'] as const)` avec `LANGUAGE_LABELS[l]` (ajouter l'import).

`src/ui/pages/HomePage.tsx` — passer à 4 cartes de ligue :

```tsx
      <section className="grid gap-3 sm:grid-cols-2">
        <LeagueMini label="Ligue française" progress={progress.fr} />
        <LeagueMini label="Ligue anglaise" progress={progress.en} />
        <LeagueMini label="Ligue C" progress={progress.c} />
        <LeagueMini label="Ligue Python" progress={progress.python} />
      </section>
```

avec l'état étendu :

```tsx
  const [progress, setProgress] = useState<Record<Language, ChallengerProgress | null>>({
    fr: null, en: null, c: null, python: null,
  });

  useEffect(() => {
    for (const lang of ['fr', 'en', 'c', 'python'] as const) {
      void getProgress(lang).then((p) => setProgress((s) => ({ ...s, [lang]: p })));
    }
    void allRuns().then((runs) =>
      setRecent(runs.sort((a, b) => b.date - a.date).slice(0, 5)),
    );
  }, []);
```

`src/ui/pages/AchievementsPage.tsx` — ajouter la catégorie Dev et charger les 4 progressions :

```tsx
const CATEGORIES: { id: AchievementCategory; label: string }[] = [
  { id: 'vitesse', label: 'Vitesse' },
  { id: 'precision', label: 'Précision' },
  { id: 'volume', label: 'Volume' },
  { id: 'challenger', label: 'Challenger' },
  { id: 'dev', label: 'Dev' },
  { id: 'fun', label: 'Fun' },
];
```

```tsx
      const [runs, unlocks, fr, en, c, python] = await Promise.all([
        allRuns(), db.achievements.toArray(),
        getProgress('fr'), getProgress('en'), getProgress('c'), getProgress('python'),
      ]);
```

```tsx
        progress: { fr, en, c, python },
```

- [ ] **Step 5: Vérifier que les tests passent**

Run: `npm test`
Expected: toute la suite PASS

- [ ] **Step 6: Commit**

```bash
git add src/achievements/ src/ui/pages/
git commit -m "feat(dev): four leagues, dev achievements, language selectors everywhere"
```

---

### Task 25: Extension — README + QA finale

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: tout le projet.

- [ ] **Step 1: Mettre à jour le README**

Dans `README.md`, section « Modes de jeu », ajouter après l'entrée Entraînement libre :

```markdown
- **Mode Dev** (`/dev`) : tape du code C ou Python (extraits, fonctions, programmes complets). Code toujours brut, multiplicateur ×1,4 ; Entrée valide les sauts de ligne et l'indentation est automatique.
```

et mettre à jour l'entrée Challenger :

```markdown
- **Challenger** (`/challenger`) : la ranked. 10 textes officiels par langue (français, anglais) **et par langage (C, Python)**, conditions officielles. Points = `WPM × précision² × 1,4`. Total des meilleurs scores → tiers : Bronze (100) → Argent (400) → Or (750) → Platine (950) → Diamant (1100) → Challenger (1300).
```

et la ligne Succès : `25 succès` → `28 succès`.

- [ ] **Step 2: Suite de tests complète**

Run: `npm test`
Expected: tous les tests PASS.

- [ ] **Step 3: Build de production**

Run: `npm run build`
Expected: validation du corpus OK (130 textes), `tsc --noEmit` OK, `vite build` OK.

- [ ] **Step 4: Smoke manuel mode dev**

Run: `npm run dev`, puis vérifier :
1. `/dev` : démarrer une run Python « Extrait », taper le code avec Entrée en fin de ligne, vérifier l'auto-indentation et le marqueur ↵.
2. `/challenger` : onglets C et Python présents, jouer un programme officiel C, points et ligue mises à jour.
3. `/leaderboard` : filtre C/Python fonctionne.
4. `/` : 4 cartes de ligue.
5. `/achievements` : catégorie Dev visible, « Hello, World! » débloqué après la première run dev.

- [ ] **Step 5: Commit final**

```bash
git add -A
git commit -m "docs: readme covers dev mode and four leagues"
```
